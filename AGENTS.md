<!-- GSD:project-start source:PROJECT.md -->

## Project

**Hermes Agent Extension for Muxy**

A development-only Muxy extension that proves a user with an existing Hermes Gateway can securely connect from a native-feeling Muxy panel, start a run, observe its streamed activity, and use the run controls the Gateway advertises. The same client contract must work across host-native, local Docker, SSH-forwarded, direct HTTPS, and remote-Muxy-workspace deployments. V1 is a transport and control-surface proof, not a polished general-purpose chat client or a multi-repository integration project.

**Core Value:** Prove secure, authenticated, streamed Hermes run control across representative Hermes deployment shapes inside a native-feeling Muxy panel before building the surrounding product.

### Constraints

- **Scope**: Extension-only development proof — prevents early multi-repository work and isolates the riskiest assumption.
- **Dependency**: One pinned, user-operated Hermes development Gateway at a time, with representative fixtures across the validation matrix — the extension never starts, stops, updates, or infers the Gateway's deployment.
- **Deployment contract**: Host-native, Docker, tunnel, and HTTPS endpoints share one protocol path; remote Muxy workspaces are a separate namespace/lifecycle test — topology-specific client behavior is prohibited.
- **Security**: Bearer token remains in panel memory; use an exact CORS origin; never use `*`; never auto-approve — the Gateway exposes terminal and file tools.
- **Transport**: Prove direct WebKit streaming before surrounding product work — Muxy's native HTTP bridge cannot serve the local SSE use case.
- **Lifecycle**: Live status and approvals exist only while the panel is open — durable background ownership is post-v1.
- **Capability compatibility**: Drive controls from `/v1/capabilities` and captured protocol fixtures — Hermes and Muxy interfaces are evolving.
- **UI**: Follow Muxy theme tokens, interface scale, control sizing, keyboard focus, and reduced-motion behavior — the proof must read as a native Muxy surface.
- **Packaging**: The build must copy `package.json` into `dist/` — Muxy's publishing and validation path ships the build output.

<!-- GSD:project-end -->

<!-- GSD:stack-start source:research/STACK.md -->

## Technology Stack

## Recommended Stack

### Core Framework

| Technology | Version | Purpose | Why |
|------------|---------|---------|-----|
| Muxy extension panel (`WKWebView`) | Current Muxy build under test; pin in fixtures | Native-feeling, open-panel control surface | A panel is an isolated `WKWebView` with `window.muxy`, so it is the only extension surface with both DOM `fetch()`/`ReadableStream` and the native Muxy visual contract. [Muxy Panels](https://muxy.app/docs/extensions/panels) **HIGH** |
| Vanilla TypeScript + DOM APIs | TypeScript strict mode; browser APIs | Small protocol/UI client | Use no UI framework in v1. The proof needs a transparent state machine and reproducible protocol fixtures, not a component platform. Parse streamed `fetch()` bytes with `TextDecoderStream`/`ReadableStream`, which the web platform exposes incrementally. [MDN Streams](https://developer.mozilla.org/en-US/docs/Web/API/Streams_API/Using_readable_streams) **HIGH** |
| Muxy vanilla Vite scaffold | Preserve the version generated by the current Muxy starter and lock it; current docs illustrate `vite ^5.0.0` | Build/package extension | Muxy is explicitly an npm + Vite extension host. Its package version is evolving separately from Muxy, so the supportable decision is to start from Muxy's current scaffold and commit its lockfile—not select a Vite major speculatively. The build must copy `package.json` into `dist/`. [Muxy Manifest](https://muxy.app/docs/extensions/manifest) **HIGH** |
| Hermes Gateway Runs API | Pin one tested Gateway build; official release evidence: v0.17.0 / `v2026.6.19`; discover capabilities at runtime | Run submission, status, controls, and SSE | Use `/v1/capabilities` as the compatibility contract rather than private Hermes internals or inferred versions. Runs provide submission, pollable status, a five-minute detached-event buffer, stop, and approval controls. [Hermes API Server](https://github.com/NousResearch/hermes-agent/blob/main/website/docs/user-guide/features/api-server.md), [release](https://github.com/NousResearch/hermes-agent/releases) **HIGH** |

### Database

| Technology | Version | Purpose | Why |
|------------|---------|---------|-----|
| None in the extension | n/a | v1 state | The Gateway already owns runs, sessions, and short-lived event replay. Keep connection token and live run state in the open panel's memory. Adding SQLite, a local daemon, or profile persistence would obscure the transport verdict. **HIGH** |

### Infrastructure

| Technology | Version | Purpose | Why |
|------------|---------|---------|-----|
| Existing user-operated Hermes Gateway | Exact version/commit captured per fixture | The sole agent backend | Model it as an HTTPS or HTTP-loopback service address plus bearer token. Do not install, start, inspect, or infer whether it is host-native, Docker, SSH-tunneled, or remote. Hermes defaults to `127.0.0.1:8642`, requires `API_SERVER_KEY`, and defaults browser CORS to disabled. [Hermes configuration](https://github.com/NousResearch/hermes-agent/blob/main/website/docs/user-guide/features/api-server.md) **HIGH** |
| Direct panel `fetch()` + streaming reader | Web platform baseline | Unified v1 transport | It is the only supported candidate that can carry `Authorization: Bearer …` and process a live SSE body incrementally. Use it for capabilities, runs, controls, and `/events`; a single transport means every deployment variant has the same client. Browser CORS and TLS policy apply. [MDN Fetch/CORS](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CORS), [MDN response streams](https://developer.mozilla.org/en-US/docs/Web/API/Response/body) **HIGH** |
| Exact Hermes CORS allowlist | Gateway config `API_SERVER_CORS_ORIGINS` | Browser authorization boundary | At first connection, capture the actual `Origin` sent by the panel and require the Gateway to allow exactly that origin. Hermes documents a comma-separated explicit list, CORS disabled by default, 10-minute preflight caching, and CORS headers on SSE responses. [Hermes CORS](https://github.com/NousResearch/hermes-agent/blob/main/website/docs/user-guide/features/api-server.md) **HIGH** |

### Supporting Libraries

| Library | Version | Purpose | When to Use |
|---------|---------|---------|-------------|
| `AbortController`, `TextDecoderStream`, `ReadableStream` | Web platform APIs | Cancel and decode fetch-backed SSE | Use directly. A readable `Response.body` lets the panel process bytes as they arrive; abort it when the user stops observing a run or the panel unloads. [MDN Fetch streaming](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch) **HIGH** |
| Tiny in-repo SSE frame parser | Project code, fixture-tested | Turn decoded event-stream lines into typed Hermes events | Prefer this to a dependency for v1: Hermes event framing is a contract to capture and test, and the parser needs only the standard `event:`, `data:`, `id:`, blank-line framing. Do not use `EventSource`: its constructor exposes only URL and `withCredentials`, not arbitrary request headers, so it cannot send the required bearer `Authorization` header. [MDN EventSource constructor](https://developer.mozilla.org/en-US/docs/Web/API/EventSource/EventSource) **HIGH** |
| `muxy.storage` | Current Muxy API | Post-v1 non-secret preferences only | Use only after v1 for labels, URLs, mappings, and UI preferences. It is persistent JSON storage, not a keychain or credential vault. Keep bearer material out of it. [Muxy Storage](https://muxy.app/docs/extensions/storage) **MEDIUM** |

## Transport Decision

### Transport comparison

| Candidate | Local / private Gateway | Remote HTTPS Gateway | Live authenticated SSE | CORS / token behavior | Decision |
|-----------|-------------------------|----------------------|------------------------|-----------------------|----------|
| Panel `fetch()` with `Authorization: Bearer` and `response.body` reader | **Candidate; must prove.** Works only if the panel's emitted origin can be explicitly allowed. | **Candidate; preferred** with a valid certificate and exact CORS allowlist. | Yes, using a streamed fetch reader and a fixture-tested SSE parser. | Browser CORS applies; bearer header triggers preflight. Do not use cookies or `credentials: include`. | **Use for all v1 traffic if the origin proof passes.** |
| `muxy.http.fetch` native HTTP | No: Muxy rejects loopback, private ranges, `.local`, and aliases resolving there before consent. | Yes for request/response calls after per-host consent. | No suitable API: it resolves to `{ status, headers, body, truncated }`, with body text capped/truncated at 10 MB; no incremental response reader is exposed. | Native request is not CORS-bound and can carry headers, but this does not solve streamed local runs. | **Do not use as the Gateway client.** A capabilities-only side path adds divergent auth/error behavior and proves nothing about the SSE route. [Muxy HTTP](https://muxy.app/docs/extensions/http) **HIGH** |
| Muxy `background.js` + native HTTP | No native HTTP API exists in background scripts. | No native HTTP API exists in background scripts. | No supported streaming ownership path. | The documented escape hatch is `muxy.exec(['curl', …])`, which requires subprocess permission and gives the extension a deployment-specific host-side helper. | **Do not use in v1.** Background scripts cannot rescue the local SSE constraint and would broaden token/command authority. [Muxy Manifest](https://muxy.app/docs/extensions/manifest), [Muxy HTTP](https://muxy.app/docs/extensions/http) **HIGH** |
| `curl`/Node/Python helper or bundled sidecar | Technically possible only with new local-process authority. | Technically possible but duplicates TLS, reconnect, and secret handling. | Could stream, but Muxy exposes no supported native helper-stream channel to an open panel. | Requires command consent, secure IPC, process lifecycle, and a second token boundary. | **Do not build in v1.** It changes the project from an extension proof into a local-service product. **MEDIUM** |
| Small upstream Muxy native streaming bridge | Potential fallback if direct panel fetch cannot be safely allowed. | Could be designed for either destination class. | Yes only after Muxy exposes a chunked/streaming response contract. | Bridge must own host consent, SSRF policy, TLS validation, and token redaction. | **Post-v1 / negative-result contract only.** Specify this only after reproducibly demonstrating that the panel's origin is not safely allowlistable. **MEDIUM** |

### Deployment matrix

| Deployment supplied by user | Example base URL | Client behavior | Gateway/operator obligations | v1 result |
|-----------------------------|------------------|-----------------|------------------------------|-----------|
| Host-native Hermes | `http://127.0.0.1:8642` | Same direct panel fetch path; never call `hermes` or inspect the host. | API key, exact panel CORS origin, loopback bind. | Supported **only after local WebKit CORS proof**. |
| Local Docker Hermes | `http://127.0.0.1:<published-port>` | Identical; Docker is invisible to the extension. | Publish a loopback host port; API key; exact CORS origin. | Supported **only after local WebKit CORS proof**. |
| SSH-tunneled remote Hermes | `http://127.0.0.1:<tunnel-port>` | Identical; the user owns tunnel lifecycle. | Tunnel terminates locally; remote Gateway remains private; API key; exact panel CORS origin. | Supported **only after local WebKit CORS proof**. |
| Direct remote Gateway | `https://gateway.example` | Identical direct fetch. No native Muxy HTTP detour. | Publicly trusted TLS certificate, API key, exact CORS origin, network access control/rate limits. | Supported after exact-origin CORS and TLS test. |
| Remote Muxy workspace | Any one of the above | No transport branch. A Muxy workspace is not evidence the Gateway shares its filesystem. | Same as selected URL deployment. | Supported for chat/run control; no active-worktree execution claim. |

## Required Request and Security Contract

## V1 Proof Requirements vs. Post-V1 Product Requirements

| V1 must prove | Explicitly defer |
|---------------|------------------|
| A publish-valid Muxy panel built from the current scaffold, with `package.json` copied to `dist/`. | Marketplace delivery, backward compatibility across unpinned Muxy releases, or a framework migration. |
| One runtime-entered URL/token works with exact observed CORS origin, bearer auth, capabilities, streamed token/tool/approval/terminal events, and advertised stop/steer/approval controls. | Multiple profiles, stored configuration, keychain integration, pairing, or token refresh. |
| Reproducible results for host-native, Docker, SSH local-forward, and direct HTTPS fixtures, plus remote-Muxy-workspace lifecycle/namespace behavior; each fixture records Muxy version, Hermes version/commit, origin, capabilities, preflight, SSE frames, reconnect, and error outcome. | A promise that untested deployment variants work, or any topology-specific client implementation. |
| Close/reopen behavior: token re-entry, status reconciliation, and documented replay window while the panel remains the owner. | Durable background run ownership, closed-panel approvals, notifications, or background SSE. |
| A negative direct-transport result ends with a minimal Muxy streaming-bridge contract. | Implementing a sidecar, `curl` helper, Node service, Muxy core patch, Hermes plugin, or provider registration during v1. |

## Alternatives Considered

| Category | Recommended | Alternative | Why Not |
|----------|-------------|-------------|---------|
| Network path | Direct panel `fetch()` | `muxy.http.fetch` | Native HTTP is excellent for public, buffered APIs but its SSRF guard excludes the local/tunneled deployments and it provides only a completed text body. |
| SSE API | Fetch stream + in-repo parser | `EventSource` | `EventSource` exposes `withCredentials` but not a headers option, so it cannot transmit Hermes's bearer header. |
| Streaming owner | Open panel | `background.js` | Background has no `muxy.http.fetch` and must use an authorized subprocess; it is not a supported native SSE owner. |
| Runtime helper | No helper in v1 | `curl`, Node, Python, Docker companion | Adds subprocess consent, OS-specific lifecycle/IPC, duplicate TLS/auth parsing, and a persistent secret boundary before proving the simpler architecture. |
| Deployment detection | One Gateway URL contract | Detect Docker/SSH/remote workspace | URL topology does not establish tool filesystem semantics and leads to unsafe host assumptions. |
| Credentials | In-memory per panel | `muxy.storage`, manifest settings, browser storage | Persistent extension storage is not a secret store; raw bearer tokens grant full tool access. |
| CORS | Exact observed extension origin | `*`, `null`, origin reflection | The browser Authorization flow needs server-approved CORS. A wildcard/null/reflection policy turns the Gateway into an ambient authority. |

## Installation

# Create the extension with Muxy's current vanilla starter, then preserve its lockfile.

# In Muxy: Extensions → Create

# The build must copy package.json into dist/ before Reload / publish validation.

## Sources

- [Muxy HTTP documentation — current, crawled 2026-08-12](https://muxy.app/docs/extensions/http) — **HIGH**: native URLSession path, SSRF/private-host policy, API response shape, webview-only availability.
- [Muxy extension manifest — current, crawled 2026-08-09](https://muxy.app/docs/extensions/manifest) — **HIGH**: Vite/`dist` contract and background-script capability boundary.
- [Muxy panels — current, crawled 2026-07-16](https://muxy.app/docs/extensions/panels) — **HIGH**: panel is a `WKWebView` surface.
- [Hermes API server documentation — current `main`, fetched 2026-08-16](https://github.com/NousResearch/hermes-agent/blob/main/website/docs/user-guide/features/api-server.md) — **HIGH**: bearer auth, CORS, capabilities, Runs/SSE/replay, approvals, stop, profile routing.
- [Hermes release history](https://github.com/NousResearch/hermes-agent/releases) — **MEDIUM**: version anchor; client compatibility must still be capability-driven and fixture-pinned.
- [MDN CORS](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CORS), [Fetch streaming](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch), [EventSource constructor](https://developer.mozilla.org/en-US/docs/Web/API/EventSource/EventSource) — **HIGH**: browser transport, preflight, streams, and EventSource header limitation.

<!-- GSD:stack-end -->

<!-- GSD:conventions-start source:CONVENTIONS.md -->

## Conventions

Conventions not yet established. Will populate as patterns emerge during development.
<!-- GSD:conventions-end -->

<!-- GSD:architecture-start source:ARCHITECTURE.md -->

## Architecture

Architecture not yet mapped. Follow existing patterns found in the codebase.
<!-- GSD:architecture-end -->

<!-- GSD:skills-start source:skills/ -->

## Project Skills

| Skill | Description | Path |
|-------|-------------|------|
| muxy-extension | Best-practice guide for authoring a Muxy extension — how it should look and behave so it reads as a native part of the app. Covers theming (follow the theme, never hardcode colors), the sizing scale, and which surface to use. Mechanics (manifest fields, permissions, the window.muxy API) live in the linked docs. | `.agents/skills/muxy-extension/SKILL.md` |
<!-- GSD:skills-end -->

<!-- GSD:workflow-start source:GSD defaults -->

## GSD Workflow Enforcement

Before using Edit, Write, or other file-changing tools, start work through a GSD command so planning artifacts and execution context stay in sync.

Use these entry points:

- `/gsd-quick` for small fixes, doc updates, and ad-hoc tasks
- `/gsd-debug` for investigation and bug fixing
- `/gsd-execute-phase` for planned phase work

Do not make direct repo edits outside a GSD workflow unless the user explicitly asks to bypass it.
<!-- GSD:workflow-end -->

<!-- GSD:profile-start -->

## Developer Profile

> Profile not yet configured. Run `/gsd-profile-user` to generate your developer profile.
> This section is managed by `generate-claude-profile` -- do not edit manually.
<!-- GSD:profile-end -->
