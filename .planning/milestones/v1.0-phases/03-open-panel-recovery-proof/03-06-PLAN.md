---
phase: 03-open-panel-recovery-proof
plan: 06
type: execute
wave: 6
depends_on:
  - 03-04
  - 03-05
files_modified:
  - src/run-controller.js
  - src/panel/app.js
  - test/run-controller.test.js
  - scripts/validate-phase.mjs
  - test/phase-boundary.test.js
  - test/ui-contract.test.js
autonomous: true
gap_closure: true
requirements:
  - RECV-01
  - RECV-02
  - RECV-05
  - EVID-01
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A status GET failure after the initial observer, first reattach, or final reattach/exhaustion always yields `status_unavailable` plus `disconnected`, starts no later observer, and leaves manual Refresh as the only automatic-recovery exit."
    - "Release during any retry delay/status/observer boundary invalidates the old generation, clears the bearer, publishes no stale completion, and leaves exactly one relay owner across replacement."
    - "The aggregate phase gate requires receipt-backed host/Docker rows, executable Unverified analogue rows, every required rendered signature, strict redaction, and cleanup provenance."
    - "The corrected MVP user story and the final native/fixture evidence describe status-authoritative recovery without a lossless-history promise."
  artifacts:
    - path: "test/run-controller.test.js"
      provides: "All interruption-boundary status-failure and generation/release state-machine regressions"
    - path: "src/run-controller.js"
      provides: "Bounded status-before-observer recovery semantics if a new red test exposes a defect"
    - path: "scripts/validate-phase.mjs"
      provides: "Final provenance, signature, simulation, authority, and cleanup aggregate gate"
    - path: "test/phase-boundary.test.js"
      provides: "Production-authority and no-topology/no-TLS-bypass boundary assertions"
    - path: "test/ui-contract.test.js"
      provides: "Rendered signature and permanent replay-limit copy assertions"
  key_links:
    - from: "test/run-controller.test.js"
      to: "src/run-controller.js"
      via: "Deferred status/stream/sleep promises exercise every generation guard and status-before-observe boundary"
      pattern: "status_unavailable|release|observe"
    - from: "scripts/validate-phase.mjs"
      to: "public/evidence/recovery-v1.json"
      via: "Complete strict schema plus required provenance/signature predicates are mandatory for aggregate success"
      pattern: "sanitizeRecoveryEvidence|requireComplete"
    - from: "scripts/validate-phase.mjs"
      to: "test/simulated-relay.test.js"
      via: "The default phase suite executes the real analogue behavior and cleanup tests rather than label-only metadata checks"
      pattern: "simulated-relay"
  prohibitions:
    - "No deployment selector, topology inference, workspace-path coupling, or topology-specific production transport branch may be added."
    - "No bearer, URL, run ID, headers, raw event/output/error text, subprocess output, journal path, workspace path, or filesystem path may persist in receipts or public evidence."
    - "No simulated observation may be presented as real deployment qualification or a positive support verdict."
---

## Resume status — 2026-08-18

Controller-boundary and fail-closed validation code already exists in commits `969ea70`, `3312dba`, and `15c0254`. Do not repeat those implementation tasks. This plan remains incomplete because 03-04 has not generated the required current host/Docker receipt-backed rows. After 03-04 completes, run the final aggregate gate, rerun goal verification, and write `03-06-SUMMARY.md`.

<objective>
Close the remaining recovery state-machine coverage and make the full evidence/provenance/simulation contract an aggregate release gate.

Purpose: Remove the verifier's behavior-unverified controller items and prevent future regression of the completed Phase 3 proof.
Output: Deterministic retry-boundary/release tests and a fail-closed phase validator covering all gap-closure artifacts.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/muxy-extension/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-open-panel-recovery-proof/03-RESEARCH.md
@.planning/phases/03-open-panel-recovery-proof/03-VALIDATION.md
@.planning/phases/03-open-panel-recovery-proof/03-VERIFICATION.md
@.planning/phases/03-open-panel-recovery-proof/03-PATTERNS.md
@.planning/phases/03-open-panel-recovery-proof/03-03-SUMMARY.md
@.planning/phases/03-open-panel-recovery-proof/03-04-SUMMARY.md
@.planning/phases/03-open-panel-recovery-proof/03-05-SUMMARY.md
</context>

<interfaces>
Do not introduce a new controller API. Tests use the existing injectable `client`, `recoveryDelays`, and `sleep` seams plus deferred promises. The required order remains stream settlement → authoritative status → bounded delay → at most one fresh observer. Any status failure ends automatic recovery. `release()` increments generation and clears bearer before awaiting teardown, so every later status/sleep/observer completion must be inert. The aggregate validator consumes only public safe artifacts and source/test commands; it does not read ephemeral token, certificate, receipt, or runtime roots after cleanup.
</interfaces>

## Artifacts this phase produces

- A complete interruption-boundary status-failure truth table.
- A deterministic release/replacement concurrency regression for the single-relay-owner invariant.
- A final aggregate gate tying receipts, signatures, simulations, native proof, cleanup, and authority boundaries together.
- Evidence-ready verification output for re-running the Phase 3 goal verifier.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove every retry-boundary status failure and release invalidation path</name>
  <files>src/run-controller.js, src/panel/app.js, test/run-controller.test.js, test/ui-contract.test.js</files>
  <read_first>
    - src/run-controller.js — preserve the current generation checks after every awaited status/sleep/observer boundary and the exact two-attempt budget.
    - src/panel/app.js — preserve `submit()` → awaited `disconnectRun()` → probe/new controller ordering so replacement never races old teardown.
    - test/run-controller.test.js — extend the existing deferred-promise tests and call-order assertions.
    - .planning/phases/03-open-panel-recovery-proof/03-VALIDATION.md — implement V-05 and V-05a exactly, including final exhaustion.
    - .planning/phases/03-open-panel-recovery-proof/03-VERIFICATION.md — close the two behavior-unverified controller truths rather than refactoring unrelated run controls.
  </read_first>
  <behavior>
    - "Initial observer settles, first status rejects: no observer starts and state is status_unavailable/disconnected/manualRefresh."
    - "Initial status succeeds, first observer starts and settles, second status rejects: no second reattach starts and state is status_unavailable/disconnected/manualRefresh."
    - "Both reattach statuses succeed, final observer settles, exhaustion status rejects: no later observer exists and no terminal/reconciled claim is published."
    - "Release during injected backoff, pending status, and pending observer completion invalidates all stale work, invokes teardown once, and no old listener receives a later update."
    - "A replacement controller can start one observer only after the released controller's teardown resolves; total active relay ownership never exceeds one."
  </behavior>
  <action>Write a table-driven red suite covering status failure at attempt indexes 0, 1, and 2/final exhaustion. Each case must record exact `start`/`status`/`observe` order, resolve/reject only one deferred boundary at a time, assert the normalized snapshot, then resolve every stale promise and prove the call/state history does not change. Add separate release cases during pending sleep, status, and observer settlement; use a shared fake relay-owner counter for old/new controllers to prove teardown completes before replacement observation and maximum concurrency is one. Add a panel contract regression that `submit()` awaits `disconnectRun()`/old `release()` before probing or constructing the replacement controller; refactor only behind an injected factory if dynamic testing requires it. Assert manual Refresh can later adopt authoritative status without restarting SSE. Change production code only if a red case exposes missing generation/serialization behavior; preserve exactly two reattach attempts, status-before-observe, bearer clearing, advertised-control behavior, and user-visible replay warning. Do not add timers, background ownership, persistent cursor/history, or a second relay stream.</action>
  <verify>
    <automated>node --test test/run-controller.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Named tests cover all three status-failure boundaries and all three release timing boundaries with deterministic deferred promises."
    - "Every failure case has zero observer calls after the failing status and manual Refresh remains status-only."
    - "Every release/replacement case records maximum active relay ownership of one, one teardown for the released controller, and no stale published state."
  </acceptance_criteria>
  <done>RECV-01/02 recovery ordering, failure termination, and generation invalidation are behaviorally proven at every verifier-identified boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the complete receipt-backed recovery proof in aggregate validation</name>
  <files>scripts/validate-phase.mjs, test/phase-boundary.test.js, test/ui-contract.test.js</files>
  <read_first>
    - scripts/validate-phase.mjs — extend its current build/all-tests/dist/evidence/boundary/sentinel sequence without suppressing command failures.
    - src/recovery-evidence.js — consume the strict completed schema and controlled renderer; do not duplicate sanitization logic in the validator.
    - public/evidence/recovery-v1.json — require the projected canonical document from Plans 03-04/03-05.
    - fixtures/simulations/docker-compose.yml — validate loopback publication, pinned images, and fixture-only TLS proxy configuration.
    - test/phase-boundary.test.js and test/ui-contract.test.js — add source/DOM contract assertions at the existing authority boundaries.
  </read_first>
  <behavior>
    - "Validation fails unless host and Docker rows use verifier recovery receipt bundles, required native predicates, post-cleanup digests, and all required real signatures."
    - "Validation fails unless SSH/HTTPS/workspace rows use executed simulation receipts, contain their required safe signatures, and remain actual/native false plus Unverified."
    - "Validation fails if the renderer omits refusal/unreachable, interruption/restoration, buffering/delay, or panel-recreation wording, or weakens the incomplete-history warning."
    - "Validation fails on new production simulation/topology/Docker/SSH/TLS-CA branches, storage/background authority, certificate bypass, secret/path sentinel survival, unpinned images, or residual-owned-resource markers."
    - "The default full command executes controller, provenance, fixture, simulation, UI, build, dist, evidence, and boundary checks with no opt-in skip that could report a false pass."
  </behavior>
  <action>Strengthen `validateRecoveryEvidence` with exact proof-source/provenance and required-signature assertions after calling the shared sanitizer. Make the aggregate command execute the new recovery provenance and real analogue tests and validate compose configuration; do not hide Docker/TLS test failures behind environment skips or shell fallbacks. Extend phase-boundary tests to prove the Node projector, simulation runner, Docker, OpenSSL, certificate CA input, and proxy script stay outside the manifest/production browser graph, and prohibit certificate-verification disablement while allowing fixture-only explicit CA trust. Extend UI contract tests to inspect the safe renderer/view model for the five required observed signatures and permanent status-authoritative/no-history wording. Add sentinel checks for bearer, endpoint, run ID, event/output text, workspace path, certificate/private-key material, temp roots, and subprocess data across public evidence and `dist`. Keep the corrected ROADMAP user story intact; do not mutate milestone scope or mark Phase 3 complete from this task—the goal verifier owns that verdict.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm test &amp;&amp; npm run validate &amp;&amp; docker compose -f fixtures/simulations/docker-compose.yml config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - "Focused mutation tests demonstrate a validator failure for each missing provenance, signature, cleanup, forced-Unverified, native predicate, authority, and redaction condition."
    - "The complete default command passes against the projected canonical evidence and executable fixtures with no skipped required scenario."
    - "`dist/package.json` remains present, permissions remain least-privilege, and no test-only Node/Docker/TLS module is reachable from the browser bundle."
  </acceptance_criteria>
  <done>The repository has one fail-closed command that proves the implemented recovery behavior, native receipt provenance, executable analogues, visible limitations, cleanup, and authority boundaries before re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| deferred async completion → current controller | Old status/sleep/observer results must not cross a release/generation boundary. |
| test/fixture artifacts → aggregate verdict | Skipped or metadata-only checks must not be mistaken for executed proof. |
| test-only infrastructure → browser bundle | Docker/TLS/projector capabilities must remain unreachable from production extension code. |
| public evidence/dist → user | Secrets, raw identifiers, paths, certificate material, and unsupported claims must be absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06-01 | Tampering | stale controller result | high | mitigate | Deferred-promise truth table exercises generation checks at status, sleep, observer, and release boundaries. |
| T-03-06-02 | Denial of service | concurrent relay ownership | high | mitigate | Shared ownership counter and teardown-before-replacement assertions enforce a maximum of one stream. |
| T-03-06-03 | Spoofing | aggregate proof status | high | mitigate | Required scenarios execute by default; provenance/signature/native/cleanup predicates are fail-closed. |
| T-03-06-04 | Information disclosure | evidence/dist/sentinel | high | mitigate | Shared sanitizer plus high-entropy sentinel scans over canonical evidence and built output. |
| T-03-06-05 | Elevation of privilege | test-only infrastructure import | high | mitigate | Static browser-graph/manifest tests keep projector, Docker, OpenSSL, CA, and simulation code out of production. |
| T-03-SC | Tampering | package supply chain | high | mitigate | Add no package/install task and preserve existing dependency/image pins. |
</threat_model>

<verification>
- Table-driven controller tests exercise every verifier-identified interruption and release boundary.
- Full validation consumes receipt-backed canonical evidence and executes all analogue behavior tests.
- UI/boundary checks prove required copy/signatures and prevent authority/TLS/secret regressions.
- The corrected MVP story remains the phase goal for the subsequent goal-backward verifier rerun.
</verification>

<success_criteria>
- No status-failure or release/generation behavior remains unverified.
- The canonical evidence is provably generated from real/simulation receipts with post-cleanup provenance.
- Every required observed signature is safe and visible; every simulation remains Unverified.
- The complete build/test/validate/compose gate passes and is ready for Phase 3 re-verification.
</success_criteria>

<output>
Create `.planning/phases/03-open-panel-recovery-proof/03-06-SUMMARY.md` when done.
</output>
