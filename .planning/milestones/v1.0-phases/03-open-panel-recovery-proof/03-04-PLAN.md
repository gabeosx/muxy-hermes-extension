---
phase: 03-open-panel-recovery-proof
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - scripts/qualify-real.mjs
  - scripts/run-recovery-fixture.mjs
  - public/evidence/recovery-v1.json
  - test/host-fixture.test.js
  - test/recovery-fixture.test.js
autonomous: true
gap_closure: true
requirements:
  - RECV-03
  - RECV-04
  - RECV-05
  - DEPL-02
  - DEPL-03
  - EVID-01
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh host-native proof correlates an initial native panel receipt with a distinct recreated-panel status-only recovery receipt and publishes only after the pinned runtime and every owned resource are gone."
    - "A fresh Docker proof correlates one native panel's connection and interrupted/restored observer receipts, includes a separately executed refused/unreachable signature, and preserves `panelLifecycle: open`."
    - "The host and Docker rows become complete only through Plan 03-03's projector; a signal, timeout, stale receipt, native mismatch, failed cleanup, or absent port check leaves the row non-complete."
    - "Users see authoritative status/final output and incomplete-history/approval-detail warnings, never a claim that observer restoration replayed everything."
  artifacts:
    - path: "scripts/qualify-real.mjs"
      provides: "Pinned host recovery challenge/receipt orchestrator and post-cleanup evidence projection"
    - path: "scripts/run-recovery-fixture.mjs"
      provides: "Disposable Docker interruption/refusal qualifier and post-cleanup evidence projection"
    - path: "public/evidence/recovery-v1.json"
      provides: "Receipt-backed host-native and Docker observed rows"
    - path: "test/host-fixture.test.js"
      provides: "Host challenge, distinct-panel, receipt-correlation, timeout, and cleanup regression coverage"
    - path: "test/recovery-fixture.test.js"
      provides: "Docker interruption/refusal, receipt-correlation, bounded cleanup, and publication regression coverage"
  key_links:
    - from: "scripts/qualify-real.mjs"
      to: "scripts/project-recovery-observation.mjs"
      via: "Distinct native-panel recovery receipt plus fixture and cleanup receipts publish the host row"
      pattern: "projectRecoveryObservation|host_native_loopback"
    - from: "scripts/run-recovery-fixture.mjs"
      to: "scripts/project-recovery-observation.mjs"
      via: "Same-panel interrupted/restored receipt plus refused-port and cleanup facts publish the Docker row"
      pattern: "projectRecoveryObservation|docker_published_loopback"
    - from: "scripts/run-recovery-fixture.mjs"
      to: "public/evidence/recovery-v1.json"
      via: "Canonical evidence is written only after compose/proxy/runtime cleanup and port-absence checks"
      pattern: "scrubbed_removed|cleanupDigest"
  prohibitions:
    - "No deployment selector, topology inference, workspace-path coupling, or topology-specific production transport branch may be added."
    - "No bearer, URL, run ID, headers, raw event/output/error text, subprocess output, journal path, workspace path, or filesystem path may persist in receipts or public evidence."
    - "No simulated or incomplete observation may substitute for the real host-native or Docker native-panel receipt predicates."
---

## Resume status — 2026-08-18

The implementation and automated-test portions of this plan already exist in commits `e44a107`, `6506196`, `cfcd6eb`, `e2b0f5b`, `f58a553`, and `cc7482a`. Do not rewrite or replay those tasks. The remaining work is the fresh native Muxy host/Docker receipt capture, projection of the two real rows, post-cleanup absence proof, focused/full validation, and `03-04-SUMMARY.md`.

The canonical `public/evidence/recovery-v1.json` correctly remains fail-closed until that live run completes.

<objective>
Republish the real host-native and Docker recovery rows from fresh native receipt bundles with cleanup proven before publication.

Purpose: Close the empirical provenance gaps for DEPL-02, DEPL-03, RECV-03 through RECV-05, and EVID-01.
Output: Bounded host and Docker qualification state machines, correlated native receipts, refusal/interruption/restoration evidence, and verified teardown.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/muxy-extension/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-open-panel-recovery-proof/03-RESEARCH.md
@.planning/phases/03-open-panel-recovery-proof/03-VALIDATION.md
@.planning/phases/03-open-panel-recovery-proof/03-VERIFICATION.md
@.planning/phases/03-open-panel-recovery-proof/03-PATTERNS.md
@.planning/phases/03-open-panel-recovery-proof/03-03-SUMMARY.md
</context>

<interfaces>
Both qualifiers own verifier challenge lifecycle, fixture lifecycle, receipt consumption, safe correlation, and publication. The panel still receives only a URL and one-time token through the existing UI; the harness never injects product state or calls a private UI API. Host proof requires two different panel digests: a successful initial connection/stream receipt from the existing Phase 1 challenge and a recreated-panel recovery receipt from Plan 03-03. Docker proof requires the same panel digest across successful connection and same-panel recovery receipts. Each qualifier builds its fixture receipt from controlled structural counters and builds its cleanup receipt only after child/process/container/network/root/journal/port absence checks. Only then may it invoke `projectRecoveryObservation`.
</interfaces>

## Artifacts this phase produces

- A fresh host receipt bundle proving pinned native streaming plus distinct-panel status-only recovery.
- A fresh Docker receipt bundle proving interruption, observer restoration, authoritative status, and refusal/unreachable behavior.
- Cleanup receipts tied to the exact resources each qualifier created.
- Canonical host/Docker evidence rows projected from those bundles, not edited by hand.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run a receipt-backed host-native stream and recreated-panel recovery proof</name>
  <precondition>`/private/tmp/hermes-qualification.DBfST8/NousResearch-hermes-agent-bbc2051/hermes` exists, is executable, and passes the existing pinned 0.20.2 / 2026.8.16 identity check before any runtime is launched.</precondition>
  <files>scripts/qualify-real.mjs, public/evidence/recovery-v1.json, test/host-fixture.test.js</files>
  <read_first>
    - scripts/qualify-real.mjs — retain explicit executable identity, isolated runtime, origin handoff, deterministic model, bounded readiness, and finally cleanup.
    - src/probe.js — use its existing connection challenge/receipt for the first panel; do not weaken support eligibility or overwrite its fixed receipt.
    - src/recovery-receipt.js — issue the exact recreated-panel recovery challenge and consume only its safe receipt.
    - scripts/project-recovery-observation.mjs — publish only through the correlated receipt interface from Plan 03-03.
    - test/host-fixture.test.js — extend the owned-root and idle-socket teardown tests rather than introducing a second host lifecycle.
  </read_first>
  <behavior>
    - "The qualifier issues an initial connection challenge, waits for one eligible panel receipt, then issues a recreated-panel recovery challenge and requires a different panel digest."
    - "The second native panel uses freshly entered URL/token plus a manual Run ID and calls status only; no event observer, previous activity, or approval detail is reconstructed."
    - "Pinned executable mismatch, stale/duplicate panel digest, wrong challenge digest, nonterminal status, automatic observer evidence, timeout, signal, or cleanup failure prevents publication."
    - "The fixture receipt carries only pinned version/revision, safe capability/event/status shapes, and digests; the run ID and all content/network/path/process values remain ephemeral."
    - "The host row receives `panel_recreated` only after the recovery receipt and all owned processes, ports, runtime roots, challenges, receipts, tokens, and journals are absent."
  </behavior>
  <action>Extend the existing interactive host state machine with a recovery-proof mode; do not create a parallel launcher. Resolve the exact pinned runtime and Muxy tuple, create a fresh 0700 owned root, issue the existing connection challenge in the active project, launch the deterministic model and loopback Gateway after exact-origin capture, and output only the bounded handoff facts needed for native Computer Use. Consume the initial safe connection receipt, preserve its digest in memory, and issue the separate Plan 03-03 challenge for `host_native_loopback`/`recreated_panel`. Close and reopen the actual Muxy panel, reconnect with the one-time token, enter the displayed run ID manually, and invoke Recover status. Require the recovery receipt's panel digest to differ from the initial receipt and its safe lifecycle/status facts to prove detached status-only recovery. On success or every failure branch, stop/await Gateway and model children, close sockets, scrub/delete the exact owned runtime/challenge/receipt/journal paths, and check both ports free; create the cleanup receipt only after those checks. Invoke the projector after cleanup and retain `eventHistoryConfidence: incomplete` plus unavailable approval detail. The user-facing/native operation may be driven with Computer Use; if macOS is locked, stop at the explicit native verification boundary rather than fabricate a receipt.</action>
  <verify>
    <automated>node --test test/host-fixture.test.js test/recovery-provenance.test.js test/recovery-evidence.test.js &amp;&amp; npm run build</automated>
    <human-check>In native Muxy, observe ordered harmless host activity in panel one, close/reopen the panel, re-enter the one-time URL/token and manual Run ID in panel two, recover authoritative status/final output, and confirm no prior activity, approval detail, or automatic observer appears.</human-check>
  </verify>
  <acceptance_criteria>
    - "Tests cover success plus mismatched executable, duplicate panel, stale challenge, missing receipt, timeout/signal, failed port check, and cleanup-failed branches."
    - "A successful run updates only the host row through `projectRecoveryObservation` and the row's provenance digests match the consumed receipt bundle."
    - "The qualifier exits with every task-owned resource absent and never removes the external pinned executable/source tree it did not create."
  </acceptance_criteria>
  <done>DEPL-02 is backed by a fresh pinned host process, two distinct actual Muxy panels, a recreated-panel status-only receipt, and post-cleanup projected evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run a receipt-backed Docker interruption, restoration, and unreachable-port proof</name>
  <precondition>`docker compose -f fixtures/simulations/docker-compose.yml config --quiet` succeeds and the pinned Hermes image digest remains unchanged.</precondition>
  <files>scripts/run-recovery-fixture.mjs, public/evidence/recovery-v1.json, test/recovery-fixture.test.js</files>
  <read_first>
    - scripts/run-recovery-fixture.mjs — reuse its fixed loopback routes, learned run ID, one-shot cut, buffering control, socket/request ownership, and bounded close.
    - fixtures/simulations/docker-compose.yml — keep the pinned image and loopback-only published port; Docker remains test-harness authority only.
    - src/curl-relay.js — reuse `buildBearerConfig` for the harness refusal probe so bearer remains in stdin and curl keeps normal URL/diagnostic behavior.
    - src/recovery-receipt.js — require the same native panel digest and safe interrupted/restored/status facts.
    - scripts/project-recovery-observation.mjs — update the Docker row only after receipt correlation and cleanup.
  </read_first>
  <behavior>
    - "The qualifier uses a unique loopback port, compose project name, owned Hermes home, proxy port, and qualification root, so concurrent or stale fixtures cannot cross-talk."
    - "A real unused/refused loopback port is exercised through the stdin-bearer curl contract and reduced to the `refused_or_unreachable` signature without retaining the port, URL, token, or raw curl output."
    - "The native panel observes the first event, the proxy cuts only the first fixed event stream, a later subscription restores observation, and authoritative terminal status is reconciled with the incomplete-history warning visible."
    - "Connection and recovery receipts carry the same panel digest; observer attempts remain bounded and the Docker row keeps `panelLifecycle: open`."
    - "Containers, network, compose project, proxy sockets/upstream requests/timers, temp home/root, challenges/receipts/journals, and all allocated ports are absent before publication."
  </behavior>
  <action>Add a bounded Docker qualification mode to the existing recovery fixture script. The harness—not the extension—allocates unique ports and an exact owned runtime root, starts the pinned compose Gateway, starts the one-shot loopback proxy, issues the existing connection challenge and Plan 03-03 same-panel recovery challenge, then outputs only the safe native handoff. Exercise a genuinely unused loopback endpoint with `/usr/bin/curl`, `--config -`, and `buildBearerConfig`; record only the normalized refusal/unreachable signature. Through Computer Use, connect the final built Muxy panel to the proxy, run the harmless delayed task, observe the interruption/recovery state and authoritative terminal status, and wait for the challenge-bound recovery receipt. Correlate it with the same panel digest from the initial connection receipt and require interrupted plus restored signatures; do not require or claim Docker-specific panel recreation. Teardown in `finally`, wait for compose removal, destroy proxy ownership sets, delete exact roots and verifier files, and check every allocated port free before building the cleanup receipt and projecting the Docker row. Never inspect Docker from production code, print bearer/request data, or translate a fixture failure into an observed row.</action>
  <verify>
    <automated>node --test test/recovery-fixture.test.js test/recovery-provenance.test.js test/recovery-evidence.test.js &amp;&amp; docker compose -f fixtures/simulations/docker-compose.yml config --quiet</automated>
    <human-check>In one native Muxy panel, confirm first activity, visible interruption/bounded best-effort recovery, restored observer activity if delivered, and authoritative terminal status; inspect the evidence row and confirm it says the panel remained open and history may be incomplete.</human-check>
  </verify>
  <acceptance_criteria>
    - "Tests execute refusal, interruption, restoration, timeout, active-stream shutdown, stale receipt, failed cleanup, and parallel unique-resource cases."
    - "A successful qualification projects refusal/unreachable, observer interrupted, and observer restored signatures with `panelLifecycle: open` and digest-only provenance."
    - "The task exits with no owned container, network, process, socket, request, timer, temp root, journal, verifier file, or bound port remaining."
  </acceptance_criteria>
  <done>DEPL-03 is backed by a fresh disposable Docker/Muxy receipt bundle covering refusal, interruption, restoration, authoritative status, and verified cleanup without a topology or replay claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host/Docker fixture → native Muxy | One-time URL/token handoff crosses into the unchanged panel; lifecycle remains test-owned. |
| native panel → qualifier | Challenge-bound digest/enumeration receipts cross back without raw run or transport data. |
| qualifier cleanup → public evidence | No positive row may publish before exact resource absence checks. |
| external pinned runtime → task-owned root | Identity is verified, but the external runtime itself is never deleted by the task. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04-01 | Spoofing | native-panel receipt | high | mitigate | Expiring challenges, distinct/same panel digest predicates, fixture correlation, and no caller-authored native flag. |
| T-03-04-02 | Information disclosure | qualifier handoff/logs | high | mitigate | Token file is 0600 and ephemeral; stdout/evidence use structural labels/digests only; sentinel scans cover durable artifacts. |
| T-03-04-03 | Tampering | runtime/image identity | high | mitigate | Exact host version/revision verification and pinned Docker image digest before launch. |
| T-03-04-04 | Denial of service | processes/proxy/compose | medium | mitigate | Bounded waits, unique ports/roots/projects, owned socket/request destruction, and unconditional finally cleanup. |
| T-03-04-05 | Elevation of privilege | fixture lifecycle | high | mitigate | Docker/process authority remains exclusively in test scripts and is absent from manifest/production source. |
| T-03-SC | Tampering | package supply chain | high | mitigate | Add no package/install task and reuse only Node built-ins, system curl, and the existing pinned image. |
</threat_model>

<verification>
- Host tests prove identity, challenge ordering, distinct-panel recovery, correlation, bounded failure, and exact-owned cleanup.
- Docker tests prove refusal, one-shot interruption, restoration, same-panel correlation, bounded close, unique resources, and post-cleanup publication ordering.
- Native checks use the final built Muxy panel and harmless deterministic fixtures; any absent native observation fails the corresponding real row.
- Projected evidence passes strict schema/redaction tests and preserves status-authoritative/incomplete-history copy.
</verification>

<success_criteria>
- DEPL-02 and DEPL-03 claims are generated from fresh correlated native receipt bundles.
- Refusal/unreachable, observer interruption/restoration, and panel recreation are inspectable structural facts.
- Host and Docker cleanup claims have code-level provenance and are emitted only after resources are absent.
- Docker remains `panelLifecycle: open`; neither row claims lossless replay or topology detection.
</success_criteria>

<output>
Create `.planning/phases/03-open-panel-recovery-proof/03-04-SUMMARY.md` when done.
</output>
