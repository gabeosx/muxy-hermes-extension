---
phase: 01-verified-gateway-connectivity
plan: 11
type: execute
wave: 10
depends_on:
  - 01-10
files_modified:
  - scripts/qualify-real.mjs
  - fixtures/host-native/fixture.json
  - test/real-qualification.test.js
  - public/evidence/transport/direct-webkit-negative-v1.json
  - public/evidence/transport/direct-webkit-negative-v1.md
autonomous: true
gap_closure: true
requirements:
  - CONN-02
  - CONN-05
  - DEPL-01
  - DEPL-02
  - SEC-01
  - SEC-02
  - SEC-04
  - EVID-01
  - EVID-02
  - EVID-03
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-05 and D-06, the repository runner starts, observes, and tears down an isolated host-native fixture while the extension sees only its deployment-neutral URL/token contract."
    - "Per D-01 through D-04 and D-09, host-native becomes Supported only from two actual fresh Muxy receipt-backed sessions on one resolved version tuple with authenticated incremental streaming and cleanup."
    - "Every missing prerequisite, timeout, signal, mismatch, incomplete stage, or cleanup failure remains Unverified or records the exact safe reproducible failure."
    - "Per D-16, the reproducible direct-WebKit non-arrival result is a paired redacted historical artifact and never a positive relay stage."
    - "All host processes, tokens, challenges, receipts, workspaces, fixture homes, and journals are removed after each attempt."
  artifacts:
    - path: "scripts/qualify-real.mjs"
      provides: "Bounded host-native two-session verifier, fixture receipt correlator, evidence publisher, and unconditional cleanup"
      exports: ["qualifyRealDeployment", "runHostQualification"]
    - path: "fixtures/host-native/fixture.json"
      provides: "Isolated deterministic no-tool Hermes qualification contract"
    - path: "test/real-qualification.test.js"
      provides: "Host lifecycle, two-session correlation, publication, and cleanup coverage"
    - path: "public/evidence/transport/direct-webkit-negative-v1.json"
      provides: "Redacted machine-readable historical direct-WebKit negative result"
    - path: "public/evidence/transport/direct-webkit-negative-v1.md"
      provides: "Human-readable projection of the same negative result"
  key_links:
    - from: "scripts/qualify-real.mjs"
      to: ".muxy-hermes-qualification/current/panel-session.json"
      via: "runner issues one-use ordinal challenge, consumes the actual panel receipt, then requires panel recreation before the next ordinal"
      pattern: "qualificationId|sessionOrdinal|panelSessionIdHash"
    - from: "scripts/qualify-real.mjs"
      to: "src/evidence.js"
      via: "matching host fixture and panel/relay receipts call the verified evidence builder directly"
      pattern: "buildVerifiedEvidenceRecord|writeEvidencePair"
    - from: "public/evidence/transport/direct-webkit-negative-v1.json"
      to: "public/evidence/transport/direct-webkit-negative-v1.md"
      via: "both projections share one safe source record and digest"
      pattern: "direct_webkit|not_implemented|receiptDigest"
  prohibitions:
    - requirement_id: "DEPL-01"
      category: "product-integrity"
      statement: "The extension may not inspect, infer, select, start, stop, or label host topology; fixture identity remains runner-only."
    - requirement_id: "SEC-01"
      category: "information-disclosure"
      statement: "Host logs, receipts, evidence, errors, and paired negative artifacts may not retain bearer, endpoint, content, path, raw subprocess, or workspace data."
---

<objective>
Complete the host-native two-session verifier and preserve the direct-WebKit non-arrival result as separate paired historical evidence.

Purpose: Produce the first real support-eligible deployment record through the approved relay while retaining the disproved transport premise truthfully.
Output: Bounded host runner, deterministic fixture contract, receipt-backed evidence publication, unconditional cleanup, and paired direct-WebKit negative artifacts.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md
@.planning/phases/01-verified-gateway-connectivity/COVERAGE.md
@.planning/phases/01-verified-gateway-connectivity/01-10-SUMMARY.md
</context>

<interfaces>
Use Plans 08-10 challenge, receipt, verified evidence, verdict, and index contracts. The runner alone owns deployment condition, version resolution, fixture lifecycle, deterministic model observations, correlation, and publication. The panel receives only the same URL/token form plus an opaque challenge.
</interfaces>

## Artifacts this phase produces

- CLI: `node scripts/qualify-real.mjs --fixture host-native --out public/evidence` plus bounded session timeout and an explicit non-publishing keep-fixture debug option.
- Safe outcomes: two host session receipts, one fixture receipt, one verifier bundle, paired evidence, and a derived host row.
- Historical transport pair: exact tested versions/date, request class, listener non-arrival, and `implementationStatus: not_implemented` without endpoint or Origin disclosure.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete one host-native two-session verifier from fixture start through paired evidence</name>
  <precondition>The current stable Muxy app and a harness-owned Hermes executable are available; `MUXY_APP_PATH`, `HERMES_QUALIFICATION_EXECUTABLE`, and a disposable `API_SERVER_KEY` source resolve without printing secret values.</precondition>
  <read_first>
    - scripts/qualify-real.mjs — host-only interactive scaffold and version/origin-era seams
    - scripts/resolve-versions.mjs — installed/latest version tuple resolver
    - fixtures/host-native/fixture.json — isolated deterministic fixture contract
    - src/evidence.js — Plan 09 verified receipt publication API
    - src/verdict.js — Plan 10 two-session classifier
    - .planning/phases/01-verified-gateway-connectivity/COVERAGE.md — reconciled deterministic request and no-tool stream sequence
  </read_first>
  <files>scripts/qualify-real.mjs, fixtures/host-native/fixture.json, test/real-qualification.test.js</files>
  <behavior>
    - "The runner starts a fresh temporary Hermes home, empty workspace, deterministic model stub, and loopback Gateway, then removes all of them in finally on success, failure, timeout, or signal."
    - "It consumes ordinals 1 and 2 from distinct panel instances and rejects nonce replay, tuple mismatch, stale receipt, missing cleanup, or fixture-correlation mismatch."
    - "Only after both verified receipts correlate does it publish paired evidence and derive the host row."
    - "The fixture observes request count, authenticated capability/stream statuses, two delayed deltas, terminal/no-tool shapes, unchanged workspace, and no unexpected egress."
  </behavior>
  <action>Replace the signal-waiting scaffold with a bounded state machine that resolves versions, creates the isolated fixture, issues the ordinal-1 challenge, waits for and consumes one actual panel receipt, requires a distinct panel webview, and repeats for ordinal 2. Build the fixture receipt from observed request/timing/status/frame/no-tool/workspace/egress/cleanup facts and correlate it with both panel receipts. Publish through `buildVerifiedEvidenceRecord`; never route positive evidence through the generic failure adapter. Retire all browser-Origin capture from host qualification. In signal, timeout, success, and error paths, stop Hermes/model processes, scrub token/challenge/receipt/journal files, and delete temporary homes/workspaces. Apply D-01 through D-06, D-09, and D-17 through D-21.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/real-qualification.test.js test/host-fixture.test.js test/evidence-provenance.test.js test/verdict.test.js</automated>
    <human-check>Run the host command, complete session 1 in the actual Muxy panel, close/reopen for session 2, and confirm paired correlated evidence or a precise Unverified/Unsupported record followed by complete process/token/challenge/receipt/journal/workspace cleanup.</human-check>
  </verify>
  <acceptance_criteria>
    - "The automated harness proves bounded start/readiness/challenge/receipt/publication/teardown ordering and every cleanup branch."
    - "Two distinct actual-panel receipts on one tuple are required for host Supported."
    - "Any missing precondition, incomplete stage, mismatch, or cleanup failure remains ineligible."
  </acceptance_criteria>
  <done>The host-native row is backed by two actual Muxy relay sessions or an exact safe incomplete/failure record, with complete cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the direct-WebKit non-arrival result as paired historical transport evidence</name>
  <read_first>
    - scripts/qualify-real.mjs — Task 1 bounded runner and safe publication utilities
    - public/evidence/schema-v1.json — existing historical artifact conventions
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-12 through D-16
    - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md — reconciled historical-negative and stop copy
    - test/real-qualification.test.js — Task 1 fixture/harness test utilities
  </read_first>
  <files>scripts/qualify-real.mjs, public/evidence/transport/direct-webkit-negative-v1.json, public/evidence/transport/direct-webkit-negative-v1.md, test/real-qualification.test.js</files>
  <behavior>
    - "The historical record contains exact tested version tuple, date, request class, listener non-arrival, reproduction outcome, and no-implementation declaration."
    - "JSON and Markdown are paired redacted projections of one source observation."
    - "The record contains no bearer, URL, host, IP, path/query, raw header/body, panel Origin, workspace path, or content."
    - "The classifier cannot treat the artifact as a curl-relay success stage or real deployment support record."
  </behavior>
  <action>Add paired-projection and classifier-ineligibility tests. Convert the reproducible controlled-listener non-arrival observation into one safe versioned source record and render matching JSON/Markdown projections with exact tested versions/date, request class, observation result, and `implementationStatus: not_implemented`. Preserve the record as D-16 historical evidence; exclude it from the current relay's required stages, host receipt bundle, and current-pair stop count unless the separate explicit change-required signal is present.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/real-qualification.test.js test/evidence-provenance.test.js test/verdict.test.js test/stop-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "JSON and Markdown validate as matching safe projections of the reproducible non-arrival result."
    - "No positive relay stage, support verdict, or independent stop is derivable from the artifact."
    - "All D-14/D-15 forbidden fields remain absent."
  </acceptance_criteria>
  <done>Direct WebKit remains durable truthful negative evidence without conflicting with the approved relay path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runner → host lifecycle | Harness-only process authority must stay outside extension code. |
| actual panel → verifier bundle | Human-driven sessions must correlate to issued challenges without topology data. |
| fixture logs/files → evidence | Raw process, secret, endpoint, model, and workspace facts require safe projection. |
| historical negative → current classifier | Disproved transport evidence must not become a positive relay predicate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-11-01 | Elevation of privilege | host runner | high | mitigate | Keep process authority in allowlisted harness code and statically reject panel imports/manifest expansion. |
| T-01-11-02 | Spoofing / Replay | two panel sessions | high | mitigate | Bind exact tuple, ordinal, nonce, distinct panel identity, fixture receipt, and cleanup digest. |
| T-01-11-03 | Information disclosure | runtime/evidence/history | high | mitigate | Use disposable input, allowlisted receipts, sentinel scans, and unconditional cleanup. |
| T-01-11-04 | Tampering | direct-WebKit artifact | medium | mitigate | Generate paired projections from one safe record and test classifier ineligibility. |
| T-01-SC | Tampering | package supply chain | high | mitigate | Add no package and use the existing harness/toolchain. |
</threat_model>

<verification>
- Host harness tests prove two-session correlation, real fixture observations, paired publication, and every cleanup path.
- Human host qualification exercises the actual Muxy relay and panel recreation.
- Historical direct-WebKit JSON/Markdown validate, remain redacted, and cannot satisfy the relay classifier.
</verification>

<success_criteria>
- Host-native has executable actual two-session qualification.
- Every host attempt produces truthful safe evidence or remains Unverified.
- Direct WebKit remains preserved historical negative evidence with no positive-path conflict.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-11-SUMMARY.md` when done.
</output>
