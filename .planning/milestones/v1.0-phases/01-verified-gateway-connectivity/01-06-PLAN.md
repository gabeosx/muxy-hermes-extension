---
phase: 01-verified-gateway-connectivity
plan: 06
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
files_modified:
  - src/main.ts
  - src/styles.css
  - src/probe.ts
  - src/evidence.ts
  - src/stop-gate.ts
  - scripts/validate-phase.mjs
  - test/stop-gate.test.ts
  - test/phase-boundary.test.ts
  - package.json
autonomous: true
requirements:
  - CONN-05
  - DEPL-01
  - DEPL-02
  - DEPL-03
  - DEPL-04
  - DEPL-05
  - DEPL-06
  - SEC-01
  - SEC-02
  - SEC-04
  - SEC-05
  - EVID-01
  - EVID-02
  - EVID-03
  - EVID-04
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The panel loads the safe evidence index and renders all five deployment conditions with exact Supported, Unsupported, or Unverified text, version details, and secret-safe reasons."
    - "Per D-01 through D-04, only the two real classes can display Supported and only after two fresh complete real-panel sessions; every simulated remote class is always Unverified in classifier, JSON, Markdown, index, and UI."
    - "Per D-09 through D-11, exact last-verified versions remain visible in details, support carries until observed regression, and a reproducible latest-pair failure takes precedence without deleting history."
    - "Per D-12 through D-15, copy/report/view actions expose only schema-validated safe Markdown/JSON and allowlisted event metadata, never raw transport data."
    - "If either real qualification demonstrates unsafe exact-origin direct transport or failed incremental delivery, or any Muxy source/bridge/provider change is judged necessary, the panel renders the non-dismissible Muxy change required alert, emits the smallest redacted bridge contract, and execution stops before any such change."
    - "The final phase boundary validator proves the extension has no Docker, SSH, process, terminal, Git-write, filesystem-write, storage, background-helper, topology-detection, certificate-bypass, or Muxy source-change authority."
  artifacts:
    - path: "src/stop-gate.ts"
      provides: "Pure activation rule and minimal redacted Muxy streaming-bridge contract projection"
      exports: ["evaluateStopGate", "buildBridgeContract", "StopGateState", "BridgeContract"]
    - path: "src/main.ts"
      provides: "Live evidence matrix, redacted report copy, bridge-contract view, and blocking alert"
    - path: "scripts/validate-phase.mjs"
      provides: "Aggregate build/test/evidence/redaction/authority/simulation phase gate"
    - path: "test/stop-gate.test.ts"
      provides: "Fail-closed alert/contract/no-upstream-action coverage"
    - path: "test/phase-boundary.test.ts"
      provides: "Cross-artifact requirement and prohibition enforcement"
  key_links:
    - from: "src/main.ts"
      to: "public/evidence/index.json"
      via: "panel loads only the sanitized index and report links copied into dist by Vite public assets"
      pattern: "evidence/index\.json"
    - from: "src/main.ts"
      to: "src/stop-gate.ts"
      via: "latest real-class safety/stream failures activate one non-dismissible alert and safe actions"
      pattern: "evaluateStopGate|buildBridgeContract"
    - from: "scripts/validate-phase.mjs"
      to: "public/evidence/index.json"
      via: "aggregate gate validates all five rows, real evidence prerequisites, simulation forcing, report pairs, and redaction"
      pattern: "validate-phase|schemaVersion"
  edge_cases:
    - id: "EVID-03-boundary"
      status: resolved
      statement: "The stop gate activates only for reproducible required exact-origin/incremental-stream failure on a real qualification lane or a discovered need for Muxy/bridge/provider change; missing/partial infrastructure remains Unverified without inventing a bridge requirement."
    - id: "EVID-03-precision"
      status: resolved
      statement: "The bridge contract contains exactly target request class, stream/abort contract, consent, SSRF/private-host, TLS, token-redaction, CORS responsibilities, safe observed failure, acceptance test, and no-implementation declaration."
    - id: "EVID-04-unclassified"
      status: resolved
      statement: "On stop-gate activation, the executor emits the safe artifacts, alerts the user, returns a blocking authorization checkpoint, and performs no later task or upstream/source/provider/bridge change."
  prohibitions:
    - requirement_id: "EVID-04"
      category: "scope-integrity"
      status: "unverified"
      flagged: true
      statement: "MUST NOT automatically modify Muxy, implement a native bridge or sidecar, register Hermes as an agent/provider, or continue expansion after the transport-stop gate activates."
    - requirement_id: "EVID-03"
      category: "transparency"
      status: "unverified"
      flagged: true
      statement: "MUST NOT make the bridge contract sound implemented, approved, or sufficient beyond the specific redacted failure evidence it contains."
    - requirement_id: "DEPL-04"
      category: "product-integrity"
      status: "unverified"
      flagged: true
      statement: "MUST NOT render any locally simulated SSH-forwarded, direct-HTTPS, or remote-workspace record as Supported, including copied reports and empty/loading/error fallbacks."
---

## Phase Goal

**As a** Hermes Gateway user, **I want to** see the complete evidence matrix and an explicit stop when direct transport is unsafe, **so that** the proof ends with a trustworthy verdict instead of silently expanding authority or upstream scope.

<objective>
Integrate evidence into the panel, implement the redacted upstream-change stop contract, and enforce the full Phase 1 boundary.

Purpose: Close EVID-03/EVID-04 and make the phase's positive and negative outcomes equally explicit, testable, and safe.
Output: Five-row evidence UI, stop-gate/bridge-contract module, non-dismissible alert/actions, aggregate phase validator, and final boundary tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/muxy-extension/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-RESEARCH.md
@.planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md
@.planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md
@.planning/phases/01-verified-gateway-connectivity/01-02-SUMMARY.md
@.planning/phases/01-verified-gateway-connectivity/01-03-SUMMARY.md
@.planning/phases/01-verified-gateway-connectivity/01-04-SUMMARY.md
@.planning/phases/01-verified-gateway-connectivity/01-05-SUMMARY.md
</context>

<interfaces>
Consume Plan 02's safe panel states, Plan 03's `EvidenceRecord`/index/verdict types, and the real/simulation records from Plans 04/05. `StopGateState` must be computed only from safe classified evidence and the explicit `requiresMuxyChange` harness signal.
</interfaces>

## Artifacts this phase produces

- Paths: `src/stop-gate.ts`, integrated `src/main.ts`/`src/styles.css`/`src/probe.ts`/`src/evidence.ts`, `scripts/validate-phase.mjs`, `test/stop-gate.test.ts`, `test/phase-boundary.test.ts`, and final generated `dist/evidence/index.json` plus linked safe reports.
- Symbols: `StopGateState`, `BridgeContract`, `evaluateStopGate`, `buildBridgeContract`, `loadEvidenceIndex`, `renderDeploymentMatrix`, `copyRedactedReport`, and `renderTransportStopAlert`.
- Commands: `npm run validate`, `node scripts/validate-phase.mjs`, `node --test test/stop-gate.test.ts test/phase-boundary.test.ts`, and the full existing build/unit/Compose validation sequence.
- Fields: `active`, `triggerCode`, `failedRealClass`, `failedStage`, `requiresMuxyChange`, `targetRequestClass`, `streamAbortContract`, `consentResponsibilities`, `ssrfPrivateHostResponsibilities`, `tlsResponsibilities`, `tokenRedactionResponsibilities`, `corsResponsibilities`, `acceptanceTest`, and `implementationStatus: not_implemented`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the evidence matrix and fail closed at the Muxy-change boundary</name>
  <read_first>
    - public/evidence/index.json — Plan 03 safe matrix/history contract populated by Plans 04/05
    - src/evidence.ts — safe evidence/report model
    - src/main.ts — Plan 02 approved panel state shell
    - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md — evidence rows, copy actions, and exact transport-stop alert
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-01 through D-15 and extension-only stop boundary
  </read_first>
  <files>src/stop-gate.ts, src/main.ts, src/styles.css, src/evidence.ts, test/stop-gate.test.ts</files>
  <behavior>
    - "Five canonical rows render loading, empty, unavailable, partial, populated, overflow, and long-text states from sanitized index data only."
    - "Host-native/Docker display classifier output; SSH-forward/direct-HTTPS/remote-workspace display Unverified for every simulated record."
    - "A real exact-origin or incremental-stream required-check failure, or requiresMuxyChange true, activates the exact non-dismissible alert and moves focus to its heading."
    - "Copy failure report and View bridge contract expose only safe projected content and independent loading/error/retry states; neither action changes scope or dismisses the alert."
    - "The contract uses only the enumerated minimum fields and states that no implementation occurred."
  </behavior>
  <action>Write the matrix, stop activation, safe-copy, and contract-boundary tests before implementation. Add `evaluateStopGate` to distinguish a reproducible real-lane direct-safety/stream failure or explicit upstream-change need from missing/partial/simulated evidence. Add `buildBridgeContract` as a projection from safe failure codes into the exact minimum fields listed in the edge criterion; it must contain no endpoint, Origin string, token/header, workspace path, raw body/log, or content-bearing frame. Load `/evidence/index.json` through ordinary panel fetch as a local built asset and render the five approved rows with exact verdict vocabulary, versions, safe reasons, history/details, loading/error/retry states, wrapping, and keyboard reachability. When the stop gate is active, render the exact `Muxy change required` alert, assertive announcement/focus, `Copy failure report`, and `View bridge contract` actions with safe independent progress/errors. The alert remains visible and offers no install, registration, permission, helper, or source-change action. After emitting the report/contract, the executor must alert the user and return a blocking authorization checkpoint immediately; do not run Task 2 or implement any upstream change.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/stop-gate.test.ts test/evidence.test.ts test/verdict.test.ts test/ui-contract.test.ts</automated>
    <human-check>Inspect all five rows in current Muxy, force a safe synthetic real-lane stream failure, and confirm the non-dismissible alert, focus, copy/view actions, redaction, exact no-change copy, and absence of any implementation/approval action.</human-check>
  </verify>
  <acceptance_criteria>
    - "Real positive evidence renders according to classifier rules; all simulated remote-class rows remain Unverified everywhere."
    - "Stop activation produces a schema-safe report and minimum contract, visibly pauses Phase 1, and exposes no upstream mutation action."
    - "If activated by actual qualification evidence, execution halts after this task for explicit user authorization and no Muxy/bridge/provider/source change occurs."
  </acceptance_criteria>
  <done>The user sees a complete truthful matrix; an unsafe direct result ends in a redacted report/contract and explicit stop, never an automatic workaround.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the complete Phase 1 build, evidence, security, and scope boundary</name>
  <precondition>No actual real-qualification stop gate from Task 1 is active; if it is active, Task 1 must already have halted execution for explicit user authorization.</precondition>
  <read_first>
    - package.json — current build/test scripts and least-privilege manifest
    - scripts/validate-dist.mjs — Plan 01 packaging/permission checks
    - public/evidence/schema-v1.json — evidence schema
    - public/evidence/index.json — final five-row matrix
    - .planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md — full requirement-to-test map and sampling gates
    - .planning/phases/01-verified-gateway-connectivity/COVERAGE.md — allowed API surface and explicit opt-outs
  </read_first>
  <files>scripts/validate-phase.mjs, test/phase-boundary.test.ts, package.json</files>
  <action>Create a non-watch `npm run validate` command that builds, runs all Node tests, structurally validates dist/manifest, validates every JSON/Markdown/index link and schema record, confirms all five canonical rows, and checks source/dist/API usage against COVERAGE.md and the locked authority boundary. The validator must parse manifests/evidence structurally and scan only production bundles/source where appropriate; it rejects any unapproved endpoint call, native HTTP/EventSource/helper/background path, storage use, Docker/SSH/process/terminal/Git/filesystem authority, topology detection, certificate bypass, workspace-path field, run-control UI, simulated Supported verdict, or forbidden evidence field. Include a high-entropy sentinel exercise that flows through probe error, report, copy, dist, and evidence outputs and must remain absent. Assert exact-origin and real two-session prerequisites from evidence, paired report completeness, latest-version metadata, and stop-gate contract shape. Keep test fixtures outside shipped dist except sanitized public evidence.</action>
  <verify>
    <automated>npm run validate</automated>
    <human-check>Reload the final dist in current stable Muxy; complete the end-of-phase UI and real/simulation checks in 01-VALIDATION.md and confirm the evidence index/report links are readable and secret-safe.</human-check>
  </verify>
  <acceptance_criteria>
    - "npm run validate performs the full non-watch build/unit/static/schema/redaction/scope suite and exits nonzero on every seeded boundary violation."
    - "All 21 Phase 1 requirement IDs have a passing automated check or required real-Muxy human evidence, with no three-task automated-feedback gap."
    - "Final dist contains the panel manifest/assets and sanitized evidence only; no test harness, raw observation, secret, workspace path, or infrastructure authority ships."
    - "Simulated remote classes remain Unverified in source, tests, JSON, Markdown, index, copied report, and panel UI."
  </acceptance_criteria>
  <done>The positive path passes the complete phase gate; the negative path has already stopped safely with its explicit user-visible change contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| evidence assets → panel DOM/copy | Even sanitized remote-derived values must remain text-safe and bounded. |
| real/simulated records → user verdict | Classification and fallback UI can overstate support. |
| failed transport → upstream scope | A failure could silently authorize broader Muxy or local-process power. |
| source/test/harness → shipped dist | Validation-only authority or raw fixtures could leak into the extension bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information disclosure | report copy, bridge view, dist | high | mitigate | Render/copy schema-safe projections only and run sentinel checks across DOM view models, dist, and evidence. |
| T-01-02 | Elevation of privilege | matrix/stop activation | high | mitigate | Validate exact-origin evidence and block wildcard/null/reflection from positive classification or success copy. |
| T-01-03 | Tampering / Information disclosure | HTTPS verdict | high | mitigate | Aggregate gate requires trusted HTTPS for non-loopback and rejects any bypass field/API/UI. |
| T-01-04 | Elevation of privilege | stop gate and shipped manifest | high | mitigate | Non-dismissible stop, immediate blocking authorization return, structural manifest/source checks, and no mutation action prevent infrastructure/Muxy authority expansion. |
| T-01-05 | Information disclosure | evidence/report/bridge assets | high | mitigate | Schema allowlists, paired validation, safe text rendering, and forbidden-field/sentinel scans exclude secret/raw payload data. |
</threat_model>

<verification>
- `npm run validate` is the final automated gate and has no watch mode.
- Actual Muxy human checks complete the real WebKit, theme/scale/focus/reduced-motion, two-session, simulation, and stop-alert observations.
- A real stop-gate result halts before Task 2 and before any Muxy/bridge/provider/source change.
</verification>

<success_criteria>
- Positive outcome: publish-valid panel, complete safe matrix, two real qualified rows as evidence permits, and three simulated rows Unverified.
- Negative outcome: safe report plus minimum contract, visible non-dismissible alert, explicit user authorization stop, and zero upstream change.
- In both outcomes, bearer/raw payload/workspace/infrastructure boundaries remain intact.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-06-SUMMARY.md` when done.
</output>
