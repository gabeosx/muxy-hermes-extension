---
phase: 01-verified-gateway-connectivity
plan: 12
type: execute
wave: 11
depends_on:
  - 01-11
  - 01-13
files_modified:
  - scripts/qualify-real.mjs
  - fixtures/simulations/docker-compose.yml
  - fixtures/simulations/scenarios.json
  - test/docker-qualification.test.js
autonomous: true
gap_closure: true
requirements:
  - CONN-02
  - CONN-05
  - DEPL-01
  - DEPL-03
  - SEC-01
  - SEC-02
  - SEC-04
  - EVID-01
  - EVID-02
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-07, the runner owns a unique Compose project, loopback publish, health/readiness, disposable secret injection, and unconditional container/network/volume/temp cleanup."
    - "Per D-01 through D-04, Docker Supported requires two actual fresh Muxy receipts on one exact Muxy/Hermes/image-digest tuple through the unchanged URL/token relay path."
    - "Refused-port, controlled mid-stream interruption, restored success, and cleanup outcomes are observed through relay receipts and recorded only as safe stage/reason metadata."
    - "Success, failure, timeout, signal, and cleanup-error paths leave no Compose resource, secret, challenge, receipt, journal, or temporary workspace."
    - "Panel source and dist contain no Docker command, image, port, condition, or lifecycle knowledge."
  artifacts:
    - path: "scripts/qualify-real.mjs"
      provides: "Docker adapter over Plan 11's verifier state machine"
      exports: ["runDockerQualification"]
    - path: "fixtures/simulations/docker-compose.yml"
      provides: "Pinned loopback Gateway/model services with unique-project lifecycle"
    - path: "fixtures/simulations/scenarios.json"
      provides: "Refusal, interruption, restoration, and cleanup expectations"
    - path: "test/docker-qualification.test.js"
      provides: "Compose lifecycle, two-session, fault, recovery, boundary, and cleanup coverage"
  key_links:
    - from: "scripts/qualify-real.mjs"
      to: "fixtures/simulations/docker-compose.yml"
      via: "Docker adapter owns up/readiness/fault/restoration/down under a unique project"
      pattern: "compose|projectName|down"
    - from: "scripts/qualify-real.mjs"
      to: ".muxy-hermes-qualification/current/panel-session.json"
      via: "same verifier challenge/receipt contract as host-native, with Docker identity kept runner-side"
      pattern: "docker-loopback|sessionOrdinal|panelSessionIdHash"
    - from: "scripts/qualify-real.mjs"
      to: "src/evidence.js"
      via: "two correlated panel receipts plus fixture cleanup receipt publish through the verified builder"
      pattern: "buildVerifiedEvidenceRecord|fixtureReceipt"
  prohibitions:
    - requirement_id: "DEPL-01"
      category: "product-integrity"
      statement: "The panel may not receive or infer Docker condition, image, port, command, or lifecycle information."
    - requirement_id: "SEC-01"
      category: "information-disclosure"
      statement: "Compose environment, logs, receipts, reports, and cleanup errors may not expose the bearer, endpoint, content, raw process data, or workspace path."
---

<objective>
Extend the real verifier with Docker-owned lifecycle, two actual Muxy sessions, refusal/interruption/restoration evidence, and unconditional cleanup.

Purpose: Close the Docker delivery gap without adding topology behavior or lifecycle authority to the extension.
Output: Unique-project Compose tracer, reusable Docker verifier adapter, fault/recovery observations, paired evidence, and focused cleanup tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md
@.planning/phases/01-verified-gateway-connectivity/01-11-SUMMARY.md
</context>

<interfaces>
Reuse Plan 11's bounded verifier state machine and Plans 08-10 receipt/evidence/verdict contracts. Docker is a runner adapter only: it supplies a loopback Gateway URL and disposable bearer to the same panel form and binds image/Compose observations only in the fixture receipt.
</interfaces>

## Artifacts this phase produces

- CLI: `node scripts/qualify-real.mjs --fixture docker-loopback --out public/evidence` plus bounded session timeout and non-publishing debug retention.
- Real outcomes: two panel receipts, exact image/version tuple, refusal/interruption/restoration receipt set, cleanup receipt, paired evidence, and Docker row.
- Resource boundary: unique Compose project and temp roots that are verified absent after every terminal path.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove Docker up, readiness, unchanged panel access, and down through one bounded slice</name>
  <precondition>Docker Engine and Compose are available, the immutable Hermes image digest resolves, and the actual Muxy panel can access the loopback published port.</precondition>
  <read_first>
    - scripts/qualify-real.mjs — Plan 11 bounded host verifier and cleanup state machine
    - fixtures/simulations/docker-compose.yml — current loopback Gateway/model services
    - fixtures/simulations/scenarios.json — current declared Docker conditions
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-06, D-07, D-17 through D-21
    - test/real-qualification.test.js — reusable verifier and cleanup test patterns
  </read_first>
  <files>scripts/qualify-real.mjs, fixtures/simulations/docker-compose.yml, fixtures/simulations/scenarios.json, test/docker-qualification.test.js</files>
  <behavior>
    - "The adapter creates a unique project and temp root, injects disposable config at runtime, waits boundedly for health, exposes only a loopback port, and obtains one real safe panel receipt."
    - "The panel receives the same URL/token fields and no Docker metadata or code branch."
    - "Success, readiness failure, timeout, signal, and receipt failure all execute down-with-volumes plus temp/challenge/receipt/journal cleanup."
    - "The runner verifies that its named container, network, volume, secret, and temp root are absent before returning."
  </behavior>
  <action>Write a failing lifecycle tracer over a fake Compose command adapter, then add a Docker adapter to the existing verifier instead of a fixture guard. Generate a unique project name and temp home, start the pinned Gateway/model services with loopback-only publish and runtime-only disposable key, use bounded health/readiness checks, issue one real panel challenge, and reduce observed fixture/relay cleanup into the shared receipt contract. Wrap all paths in one outer finalizer that stops and downs with volumes, removes temp/challenge/receipt/journal material, and verifies no named resource remains. Keep every Docker fact in harness code per D-06, D-07, D-18, D-19, and D-21.</action>
  <verify>
    <automated>docker compose -f fixtures/simulations/docker-compose.yml config --quiet &amp;&amp; node --test test/docker-qualification.test.js test/real-qualification.test.js test/evidence-provenance.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The tracer proves up/readiness/panel receipt/down ordering through the same relay interface as host-native."
    - "Every seeded failure executes and verifies cleanup before returning."
    - "Static boundary assertions find no Docker knowledge in panel source or dist."
  </acceptance_criteria>
  <done>One Docker relay session can execute end to end under runner-owned lifecycle and leave no resource behind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete two-session Docker qualification with refusal, interruption, and restoration evidence</name>
  <precondition>The Task 1 Compose tracer passes and the actual Muxy panel can be closed/reopened between qualification ordinals.</precondition>
  <read_first>
    - scripts/qualify-real.mjs — Task 1 Docker adapter and Plan 11 two-session state machine
    - fixtures/simulations/scenarios.json — refusal/interruption/restoration expectations
    - src/evidence.js — verified fixture/panel receipt bundle API
    - src/verdict.js — exact two-session Docker predicate
    - .planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md — missing Docker qualification and fault evidence
  </read_first>
  <files>scripts/qualify-real.mjs, fixtures/simulations/scenarios.json, test/docker-qualification.test.js</files>
  <behavior>
    - "Ordinals 1 and 2 come from distinct actual panel instances on the same exact Muxy/Hermes/image tuple."
    - "After the eligible pair, the runner observes a refused loopback port, controlled mid-stream container stop, and restored success without panel code changes."
    - "Fault observations use safe relay stage/reason and cleanup receipts; they cannot reuse the support pair's identities."
    - "A cleanup failure makes the attempt ineligible even when stream stages passed."
  </behavior>
  <action>Extend the Docker adapter through both fresh panel ordinals and correlate the exact image/version tuple, fixture observations, relay digests, and cleanup receipt before verified publication. After the eligible pair, execute a bounded refused-port case, stop the Gateway during an incremental stream, observe terminal cancellation/status/cleanup, restore the same fixture, and prove a new successful relay without changing extension code. Publish only safe receipt-backed stage/reason facts, rederive the Docker row, and run the same unconditional cleanup finalizer after every scenario. Apply D-01 through D-07 and D-17 through D-21.</action>
  <verify>
    <automated>docker compose -f fixtures/simulations/docker-compose.yml config --quiet &amp;&amp; node --test test/docker-qualification.test.js test/transport-diagnostics.test.js test/evidence-provenance.test.js test/verdict.test.js</automated>
    <human-check>Run the Docker qualification, complete two fresh actual Muxy sessions, allow refusal/interruption/restoration, and confirm paired evidence plus Compose resource checks show complete cleanup.</human-check>
  </verify>
  <acceptance_criteria>
    - "Docker Supported requires two correlated actual-panel receipts on one exact current tuple."
    - "Refusal, interruption, restoration, and cleanup results are durable only as safe receipt-backed metadata."
    - "Every terminal path leaves no Compose resource or qualification/runtime artifact."
  </acceptance_criteria>
  <done>The Docker row has real two-session and fault/recovery evidence with runner-owned lifecycle and verified cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runner → Docker daemon | Harness commands create and remove isolated resources. |
| loopback fixture → actual panel | The panel must remain topology-neutral and bearer-safe. |
| fault observations → evidence | Raw container/process/log data must reduce to safe receipt stages. |
| cleanup result → eligibility | Residual resource risk invalidates an otherwise passing attempt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12-01 | Elevation of privilege | Docker adapter | high | mitigate | Keep commands in the verifier, use unique projects, and statically reject panel/manifest lifecycle authority. |
| T-01-12-02 | Information disclosure | environment/logs/receipts | high | mitigate | Inject disposable secrets, avoid raw output, allowlist receipts, and scrub all temp material. |
| T-01-12-03 | Tampering | image/version/session correlation | high | mitigate | Bind immutable image digest, exact tuple, two distinct panel identities, and fixture cleanup digest. |
| T-01-12-04 | Denial of service | interrupted fixture | medium | mitigate | Bound every wait, restore deliberately, and run cleanup under signals/timeouts/errors. |
| T-01-SC | Tampering | package supply chain | high | mitigate | Use existing pinned image/toolchain and add no package. |
</threat_model>

<verification>
- Compose configuration and Docker harness suites prove loopback binding, immutable identity, two-session correlation, faults, restoration, and cleanup.
- Actual Muxy verification completes two fresh sessions through the unchanged panel contract.
- Panel source/dist remain free of Docker/topology/lifecycle knowledge.
</verification>

<success_criteria>
- Docker loopback has executable real qualification and paired evidence.
- Refusal/interruption/restoration are observed truthfully.
- No Docker resource, secret, or qualification artifact survives a terminal path.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-12-SUMMARY.md` when done.
</output>
