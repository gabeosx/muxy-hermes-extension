---
quick_id: 260821-pe1
mode: quick-full
description: Fix queue status fallback so worker enrichment failures do not hide valid Kanban stats; add regression coverage and push the existing PR branch
must_haves:
  truths:
    - Valid Kanban stats render queue pressure even when active-worker enrichment is unavailable or invalid.
    - A genuinely unavailable or malformed Kanban stats response still renders the truthful queue-unavailable state.
    - The canonical source branch and marketplace review PR contain the same tested production fix and regression coverage.
  artifacts:
    - src/dashboard-operations.js
    - test/dashboard-operations.test.js
  key_links:
    - DashboardOperationsClient.load projects /api/plugins/kanban/stats independently from /workers/active.
    - The marketplace sparse copy matches the reviewed canonical source files.
---

# Quick task 260821-pe1

## Task 1: Add the regression test and decouple optional worker enrichment

**Files:** `test/dashboard-operations.test.js`, `src/dashboard-operations.js`

**Action:** Add a focused test where `/api/plugins/kanban/stats` returns the valid Hermes contract while `/workers/active` returns an error. Require the snapshot to keep queue availability, queue counts, wait age, and a conservative zero active-worker count. Refactor the operations projection so worker telemetry is parsed best-effort and cannot invalidate valid queue stats. Preserve the existing fail-closed behavior for missing, non-2xx, or malformed stats responses.

**Verify:** Run the focused operations tests and confirm both the new worker-failure case and the existing optional-surface/unavailable case pass.

**Done:** Queue pressure remains truthful and visible whenever the authoritative stats contract succeeds, regardless of optional worker telemetry.

## Task 2: Validate, package, and update the review branches

**Files:** `src/dashboard-operations.js`, `test/dashboard-operations.test.js`, GSD quick-task records, `extensions/hermes-agent/src/dashboard-operations.js`, `extensions/hermes-agent/test/dashboard-operations.test.js`

**Action:** Run the full unit suite, build, distribution validation, release validation, and diff checks. Commit only the two source/test files plus required quick-task records on `codex/marketplace-submission`, push that branch, copy the exact reviewed source/test files into the existing `gabeosx/extensions` contribution branch, rerun the marketplace extension's focused/full validation there, commit, and push to PR #134.

**Verify:** All local gates pass; the sparse-copy files are byte-for-byte equal to canonical source; both remote branches advance; PR #134 reports the new commit on `gabeosx:codex/hermes-agent-marketplace-beta`.

**Done:** The tested fix is present in canonical source and the already-submitted marketplace review PR without unrelated worktree files.
