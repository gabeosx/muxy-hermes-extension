---
quick_id: 260821-e4b
mode: quick
description: Replace blurry README and marketplace screenshots with sharp high-density captures
---

# Quick task 260821-e4b

## Task 1: Recapture the real extension UI losslessly at 2× density

**Files:** temporary local visual fixture and raw captures; no production runtime files

**Action:** Render the actual Operations, approval, and project-board components with isolated demo-safe state in a fixed dark Muxy theme. Capture lossless PNGs at 2× device scale so all UI text has enough source pixels for both output sizes.

**Verify:** Raw captures are PNG, contain only allowlisted demo text, and provide at least one source pixel per marketplace output pixel in every embedded UI region.

**Done:** No final composition depends on the earlier JPEG-compressed full-window frames or enlarges a low-resolution UI crop.

## Task 2: Rebuild and validate the six final screenshots

**Files:** `assets/readme/*.png`, `assets/screenshots/*.png`, GSD task records

**Action:** Recreate the existing three compositions from the high-density captures, retaining exact 760×475 README and 1600×1000 marketplace dimensions. Inspect native and mobile-size previews, OCR the outputs, run metadata/string checks, tests, deterministic distribution validation, and full release validation.

**Verify:** Images are visibly sharp at native size, privacy checks pass, `npm test`, `npm run validate:dist`, and `npm run validate` pass.

**Done:** The screenshot compositions remain unchanged in concept but product typography, borders, icons, and controls render cleanly without blur.
