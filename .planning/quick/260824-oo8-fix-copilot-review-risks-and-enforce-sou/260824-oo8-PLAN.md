---
quick_id: 260824-oo8
mode: quick-full
phase: quick-260824-oo8
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
description: Fix Copilot review risks and enforce source-first marketplace release flow
files_modified:
  - src/panel/app.js
  - src/board/app.js
  - src/session-broker.js
  - src/muxy-tabs.js
  - test/panel-project-isolation.test.js
  - test/board-actions.test.js
  - test/session-broker.test.js
  - RELEASING.md
  - scripts/validate-release.mjs
  - test/release-validator.test.js
must_haves:
  truths:
    - "An operations request started for one Muxy project can never publish after the active project or mapped board changes."
    - "A missing mapped board returns to the picker with the specific recovery message instead of a generic availability error."
    - "Destructive board moves explicitly default and cancel to the safe choice and use warning styling."
    - "Every per-project mapping storage key is at most Muxy's 256-character limit, with one shared project-ID boundary."
    - "Release documentation requires merging the source PR first and copying only the exact merged source commit into the extensions fork."
    - "The source and upstream marketplace validation, test, build, and dry-run packaging gates pass before remote PR updates."
  artifacts:
    - path: "src/panel/app.js"
      provides: "Generation-guarded project-scoped operations refreshes"
    - path: "src/board/app.js"
      provides: "Preserved stale-board recovery and explicit safe move confirmation"
    - path: "src/session-broker.js"
      provides: "Storage-safe project mapping keys"
    - path: "RELEASING.md"
      provides: "Source-first marketplace release sequence"
  key_links:
    - from: "src/panel/app.js"
      to: "test/panel-project-isolation.test.js"
      via: "delayed old-project refresh followed by project switch"
      pattern: "operationsRefreshGeneration"
    - from: "src/board/app.js"
      to: "test/board-actions.test.js"
      via: "behavioral stale-board recovery and confirmation options"
      pattern: "recoverMissingMappedBoard"
    - from: "src/muxy-tabs.js"
      to: "src/session-broker.js"
      via: "shared maximum project-ID length derived from the storage prefix"
      pattern: "MAX_PROJECT_ID_LENGTH"
---

# Quick task 260824-oo8

## Task 1: Prevent stale cross-project operations publication

**Files:** `src/panel/app.js`, `test/panel-project-isolation.test.js`

**Action:** Add a refresh generation guard and serialized refresh queue. Invalidate the current generation before clearing or changing active-project mapping state, schedule the new project immediately after any authenticated stale request drains, and publish or clear loading state only when the completing request still belongs to the current operations client and generation. Add a delayed-request regression proving Project A cannot overwrite Project B.

**Verify:** `node --test test/panel-project-isolation.test.js`

**Done:** No old-project operation result or completion state is rendered after a project switch.

## Task 2: Close the board recovery, dialog, and storage boundary gaps

**Files:** `src/board/app.js`, `src/session-broker.js`, `src/muxy-tabs.js`, `test/board-actions.test.js`, `test/session-broker.test.js`

**Action:** Make missing-board recovery return whether it handled the error and skip generic error handling when it did. Give Blocked/Done confirmations explicit safe defaults and warning styling. Export one project-ID length limit derived from the 256-character storage-key maximum and use it in active-project resolution and mapping validation. Cover recovery, dialog options, and 239/240-character boundaries behaviorally.

**Verify:** `node --test test/board-actions.test.js test/session-broker.test.js`

**Done:** The specific recovery message survives, destructive moves default safely, and mapping keys cannot exceed the host contract.

## Task 3: Enforce and execute the source-first release workflow

**Files:** `RELEASING.md`, `scripts/validate-release.mjs`, `test/release-validator.test.js`

**Action:** Document and validator-enforce source validation, source PR merge, exact merged-commit pinning, fork-only marketplace staging, upstream checks, and the prohibition on direct upstream/source-of-truth edits. Run the complete source gates, commit the task atomically, push only the source branch, merge its PR to `gabeosx/muxy-hermes-extension`, then copy the exact merged commit into a clean `gabeosx/extensions` fork branch and update PR #134 after upstream validation and dry-run packaging pass.

**Verify:** `npm test && npm run build && npm run validate`, followed by upstream `node scripts/build.mjs hermes-agent`, `node scripts/validate.mjs hermes-agent`, and `node scripts/pack.mjs --dry-run hermes-agent` in the sparse checkout.

**Done:** The source PR is merged first, the marketplace PR contains the exact reviewed source snapshot, all gates pass, and no commit is made directly to `muxy-app/extensions`.
