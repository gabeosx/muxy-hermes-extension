---
quick_id: 260821-k4x
mode: quick-full
phase: 07-marketplace-submission
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
description: Prepare the Hermes Agent marketplace submission with a store-first hero, release governance, CI, and exact-package validation
requirements: [MKT-01, MKT-02, MKT-03, MKT-04, MKT-08]
files_modified:
  - artwork/marketplace-hero.svg
  - assets/screenshots/hero.png
  - package.json
  - scripts/validate-dist.mjs
  - test/ui-contract.test.js
  - CHANGELOG.md
  - RELEASING.md
  - README.md
  - .github/workflows/ci.yml
  - scripts/validate-release.mjs
  - test/release-validator.test.js
  - .planning/REQUIREMENTS.md
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Muxy store carousel opens with a purpose-built Hermes hero whose primary message remains readable at approximately 442x276, followed by Operations, Approvals, and Kanban in that order (D-01, D-02)."
    - "The published manifest and deterministic dist package contain one valid icon and exactly four sanitized 1600x1000 PNG listing images, without broadening runtime permissions (D-01, D-02)."
    - "Maintainers have a documented immutable-version release flow with an Unreleased changelog section, patch/minor rules, rollback guidance, and no npm-publishing path (D-03)."
    - "GitHub CI validates the locked Node 20 package with read-only repository authority, while full topology qualification remains an explicit release gate."
    - "The locally verified package is ready for the root agent to make the source repository public, copy only allowed source into the Muxy sparse checkout, run upstream tooling, and open a draft PR without publishing it (D-04)."
    - "Existing user-owned untracked files remain untouched and unstaged (D-05)."
  artifacts:
    - path: "artwork/marketplace-hero.svg"
      provides: "Deterministic, editable vector source for the store hero"
    - path: "assets/screenshots/hero.png"
      provides: "Exact-size first marketplace screenshot"
    - path: "package.json"
      provides: "Four-image marketplace ordering and immutable 0.1.0 identity"
    - path: "scripts/validate-dist.mjs"
      provides: "Source/dist asset inventory, exact-dimension, ordering, and deterministic-build enforcement"
    - path: "CHANGELOG.md"
      provides: "Unreleased and initial 0.1.0 release history"
    - path: "RELEASING.md"
      provides: "Versioning, validation, upstream submission, rollback, and post-merge smoke procedure"
    - path: ".github/workflows/ci.yml"
      provides: "Node 20 test and deterministic release-validation workflow"
    - path: "scripts/validate-release.mjs"
      provides: "Release-governance, secret, version, and clean-copy assertions"
  key_links:
    - from: "package.json"
      to: "assets/screenshots/hero.png"
      via: "muxy.marketplace.screenshots[0]"
      pattern: "assets/screenshots/hero\\.png"
    - from: "scripts/copy-manifest.mjs"
      to: "dist/assets/screenshots/hero.png"
      via: "recursive assets copy during npm run build"
      pattern: "cp\\(resolve\\(root, \"assets\"\\)"
    - from: "scripts/validate-dist.mjs"
      to: "package.json"
      via: "frozen ordered screenshot list, source/dist manifest equality, and exact distribution allowlist"
      pattern: "REQUIRED_SCREENSHOTS"
    - from: ".github/workflows/ci.yml"
      to: "scripts/validate-release.mjs"
      via: "npm run validate on Node 20 after npm ci"
      pattern: "npm run validate"
    - from: "scripts/validate-release.mjs"
      to: "CHANGELOG.md, RELEASING.md, package.json, package-lock.json"
      via: "release-governance validation"
      pattern: "validateReleaseGovernance"
---

<objective>
Prepare the exact local source and listing package that the root agent can submit to the Muxy extensions repository as a draft pull request.

Purpose: Make the first store image work as a legible product hero at the store's observed thumbnail size, preserve the three existing feature images, and add the lightweight versioning and release controls needed to maintain an immutable marketplace package without introducing npm publishing.

Output: A deterministic hero asset and four-image listing, release documentation, read-only CI, strengthened local validation, synchronized planning requirements, and an explicit root-agent handoff for public visibility, upstream validation, and draft-PR creation (D-01 through D-05).
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/muxy-extension/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@package.json
@scripts/copy-manifest.mjs
@scripts/validate-dist.mjs
@scripts/validate-release.mjs
@scripts/qualify-release.mjs
@test/ui-contract.test.js
@test/release-validator.test.js
@README.md

<decisions>
- D-01: The first marketplace screenshot is a purpose-built store hero optimized for the observed approximately 442x276 presentation.
- D-02: Operations, Approvals, and Kanban remain after the hero, producing four screenshots total.
- D-03: Add CHANGELOG.md, RELEASING.md, and one GitHub CI workflow; do not add npm publishing or publish to the marketplace.
- D-04: The root agent owns repository visibility, fork/sparse-checkout setup, push, upstream validation, and draft-PR creation after local execution.
- D-05: Preserve every pre-existing untracked file and never stage it.
</decisions>

<working_tree_guard>
At planning time the following user-owned paths were untracked: `.agents/`, `.gsd/`, `.planning/debug/cron-next-run-accuracy.md`, `.planning/research/.cache/6ff98be1089b1a6d49a4632efa7aba1b0039997367c4fae0f57b8ca08729baf2.json`, and `skills-lock.json`. Do not edit, remove, stage, or commit them. Use explicit pathspecs for every commit and re-check `git status --short` before and after work per D-05.
</working_tree_guard>

<official_marketplace_contract>
Muxy's contribution contract permits one to six PNG screenshots at exactly 1600x1000, requires a listing icon and README, runs the extension build, and ships only `dist/`. From the upstream repository root the authoritative checks are `node scripts/validate.mjs hermes-agent` and `node scripts/pack.mjs --dry-run hermes-agent`; these are root-agent follow-up checks because the local executor does not own the upstream checkout (D-04).
</official_marketplace_contract>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Put a legible store hero through the complete source-to-dist listing path</name>
  <files>artwork/marketplace-hero.svg, assets/screenshots/hero.png, package.json, scripts/validate-dist.mjs, test/ui-contract.test.js</files>
  <action>Create `artwork/marketplace-hero.svg` as the deterministic 1600x1000 editable source and render its checked-in PNG counterpart to `assets/screenshots/hero.png`. Use static vector/HTML-style composition and repository-native visual language rather than image generation: the exact headline is “Hermes, inside Muxy.” and the exact support line is “Start agent work, handle approvals, and keep projects moving.” Include the existing Hermes icon language and one dominant, sanitized product-surface crop or faithful vector treatment; keep the headline and product promise large enough to read when downsampled to 442x276, and use no personal paths, credentials, endpoints, account data, or external SVG references. Inspect both the 1600x1000 image and a 442x276 downsample with `view_image`, revising until the small presentation communicates the extension without expansion. Per D-01 and D-02, change `muxy.marketplace.screenshots` to the exact order `hero.png`, `operations.png`, `agent-approval.png`, `project-board.png`; do not change version, permissions, commands, panel, tab, dependencies, or the three existing feature images. Update `REQUIRED_SCREENSHOTS` and the frozen marketplace assertion so source and dist require exactly those four 1600x1000 PNGs in that order while README continues to use its three 760x475 feature images. Use explicit pathspecs and leave the protected untracked paths untouched per D-05.</action>
  <verify>
    <automated>node --test test/ui-contract.test.js &amp;&amp; npm run validate:dist</automated>
    <human-check>Inspect `assets/screenshots/hero.png` at native size and a temporary 442x276 downsample: headline and support line are readable without opening the image, the product identity is immediate, the composition has no clipped text or private data, and the next three carousel images remain unchanged.</human-check>
  </verify>
  <done>The manifest's first listing image is the exact-size deterministic hero, all four ordered images survive the build into the declared deterministic dist inventory, and the focused contract test passes (D-01, D-02, D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Document immutable versioning and the release handoff</name>
  <files>CHANGELOG.md, RELEASING.md, README.md, test/release-validator.test.js</files>
  <action>Add a concise Keep-a-Changelog-style `CHANGELOG.md` with an `Unreleased` section and the initial `0.1.0` marketplace beta entry. Add `RELEASING.md` that makes `package.json` the version source, requires the lockfile root version to match, and defines the agreed pre-1.0 policy: patch releases for fixes, security/documentation/listing corrections, and minor releases for features, permission changes, newly supported deployment shapes, or breaking changes. Document that published `hermes-agent@version` pairs are immutable; correction releases never rebuild an existing version; repository tags use `hermes-agent-vX.Y.Z` because `v1.0` already names a project milestone. Specify the release sequence: promote changelog notes and update both version files, run tests/validation/qualification, prepare the allowlisted sparse-checkout source, run Muxy's upstream validation and dry-run pack, open a draft PR, and only after upstream merge tag the exact source commit and smoke-test the signed store build. State explicitly per D-03 and D-04 that there is no npm publish step and that the current work stops at a draft PR. Include the upstream-copy exclusions: `dist/`, `.planning/`, `.qualification/`, `.agents/`, `.gsd/`, `.github/`, `node_modules/`, receipts, credentials, generated qualification data, local caches/logs, and `skills-lock.json`; retain source fixtures and release docs needed for review. Link `RELEASING.md` and `CHANGELOG.md` from README's Development section without replacing the three focused README screenshots. Add focused release-document contract assertions to `test/release-validator.test.js` before writing the documents, covering the required headings, commands, version policy, exclusions, and draft-only boundary. Preserve the protected untracked paths per D-05.</action>
  <verify>
    <automated>node --test test/ui-contract.test.js test/release-validator.test.js</automated>
  </verify>
  <done>Maintainers can select, validate, submit, tag, correct, and roll back a marketplace release from one concise procedure, with no npm or marketplace publication performed by this task (D-03, D-04, D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce release governance in CI and the local release gate</name>
  <files>.github/workflows/ci.yml, scripts/validate-release.mjs, test/release-validator.test.js, .planning/REQUIREMENTS.md</files>
  <behavior>
    - Test 1: package and lockfile versions are identical, the package remains private, and no npm publication lifecycle script exists.
    - Test 2: the changelog contains both Unreleased and the current package version, while the release guide contains the patch/minor policy, required local/upstream commands, immutable-version rule, rollback, draft-PR boundary, and no npm-publish path.
    - Test 3: clean-copy validation excludes `.qualification` runtime receipts and every existing protected metadata root while retaining the checked-in qualification harness used by `npm run qualify`.
    - Test 4: the single GitHub workflow uses Node 20, top-level read-only `contents` permission with no additional grants, `npm ci`, `npm test`, and `npm run validate`; `actions/checkout` and `actions/setup-node` each use an exact 40-hex-character commit SHA with an adjacent comment naming intended major version `v4`, and an explicitly requested manual job may run `npm run qualify` without repository secrets.
  </behavior>
  <action>Write the failing governance tests first, then add `validateReleaseGovernance` to `scripts/validate-release.mjs` and call it from both structural and full validation. Validate `private: true`, exact package/lockfile version equality, absence of publication lifecycle scripts, required changelog/release-guide contracts, and the CI workflow's bounded commands/permissions without retaining command output or secrets. Add `.qualification` to the clean-copy excluded roots but do not exclude the checked-in `qualification/` harness. Create one GitHub Actions workflow for pushes, pull requests, and manual dispatch using Node 20, a committed lockfile, top-level `permissions: { contents: read }`, no additional grants, no repository secrets, `npm ci`, `npm test`, and `npm run validate`; keep `npm run qualify` as an opt-in manual job because it owns disposable Docker/SSH/HTTPS infrastructure. Resolve current official commit SHAs for `actions/checkout` and `actions/setup-node`, use each action only as `owner/action@<full 40-hex-character commit SHA>`, and place an adjacent `# v4` comment on each pin. Make `test/release-validator.test.js` and `validateReleaseGovernance` parse/assert those exact pin shapes, their `v4` comments, top-level read-only contents permission, absence of other workflow permissions, and absence of secrets/write/publish/deploy steps. Do not add or update npm packages, dependencies, lockfile entries, telemetry, permissions, publishing steps, releases, tags, or deployments per D-03 and D-04. Update only MKT-02 in `.planning/REQUIREMENTS.md` from three screenshots to the user-approved hero plus three feature images, keeping its requirement ID stable; the quick plan and SUMMARY are the record of the four-image decision. Preserve the protected untracked paths and stage only the task's explicit files per D-05.</action>
  <verify>
    <automated>node --test test/release-validator.test.js test/ui-contract.test.js &amp;&amp; npm test &amp;&amp; npm run validate</automated>
  </verify>
  <done>The source repository has one bounded CI workflow, the local release validator enforces version/docs/clean-copy contracts, the planning ledger matches the four-image decision, and the complete local deterministic release gate passes without npm or marketplace publication (D-01, D-02, D-03, D-04, D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artwork and screenshots to marketplace package | Checked-in visual assets become public store content and must not leak private data or execute active SVG content. |
| Repository pull request to GitHub Actions | Untrusted branch contents execute on a hosted runner and must receive no write authority or secrets. |
| Local source repository to public GitHub/upstream sparse checkout | A private development tree crosses into public repositories; generated evidence, credentials, caches, and user-owned metadata must remain behind. |
| Source package to Muxy build/sign pipeline | Only declared deterministic `dist/` files and the exact reviewed manifest may enter the signed extension. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-K4X-01 | Information Disclosure | `assets/screenshots/hero.png`, public source copy | high | mitigate | Use controlled demo-safe copy, reject active/external SVG references, visually inspect native and 442x276 renders, keep release secret scanning active, and exclude runtime receipts/credentials before D-04 handoff. |
| T-K4X-02 | Tampering | `package.json` to `dist/package.json` and listing assets | high | mitigate | Preserve exact source/dist manifest equality, ordered asset assertions, exact PNG dimensions, complete allowlist inventory, and byte-identical double builds in `validate-dist.mjs`. |
| T-K4X-03 | Elevation of Privilege | `.github/workflows/ci.yml` | medium | mitigate | Set workflow `permissions: contents: read`, provide no secrets, perform no write/publish/deploy step, and use only the existing committed dependency lockfile. |
| T-K4X-04 | Spoofing | version/tag/release identity | medium | mitigate | Require package/lockfile version parity, current-version changelog coverage, `hermes-agent-vX.Y.Z` tags on the exact reviewed commit, and immutable correction releases. |
| T-K4X-05 | Repudiation | release preparation and root-agent handoff | low | mitigate | Record exact local and upstream commands in RELEASING.md, retain CI results and draft PR history, and keep publication outside this executor's authority per D-04. |
| T-K4X-SC | Tampering | `npm ci` and GitHub Actions supply chain | high | mitigate | Add no packages, preserve `package-lock.json`, use `npm ci`, keep the existing audit gate at zero high/critical findings, and pin official GitHub actions to reviewed immutable commit SHAs with their major versions noted in comments. |
</threat_model>

<verification>
## Local executor gates

Run from `/Users/gabe/muxy-hermes-extension` after all three tasks:

1. `git status --short` — every pre-existing untracked path listed in `<working_tree_guard>` is still present and unstaged; only the explicit plan files are modified/added.
2. `npm test`
3. `npm run validate`
4. `npm run qualify` — must end with `passed_supported_beta_matrix` and cleanup proof; retain no active marker, secret, listener, container, network, volume, SSH process, or task root.
5. `git diff --check --` followed by the explicit plan file pathspecs only.

## Root-agent external/upstream gates after local execution (D-04)

The local executor stops before these actions. The root agent then:

1. Re-runs the release secret scan before changing `gabeosx/muxy-hermes-extension` visibility to public.
2. Creates or refreshes the authenticated `gabeosx/extensions` fork and a clean partial/sparse checkout containing `scripts/` and `extensions/hermes-agent/`.
3. Copies only the allowlisted source described in `RELEASING.md`; confirms `dist/`, planning/agent metadata, `.qualification`, receipts, credentials, generated qualification data, local caches/logs, the source-repository workflow, and protected untracked files are absent.
4. Runs `(cd extensions/hermes-agent &amp;&amp; npm ci &amp;&amp; npm test &amp;&amp; npm run build)`.
5. From the upstream checkout root, installs the upstream tooling and runs `node scripts/validate.mjs hermes-agent` and `node scripts/pack.mjs --dry-run hermes-agent`.
6. Confirms the packed artifact's manifest version, four-image ordering, icon, README, file inventory, and digests match the locally reviewed package, then commits only `extensions/hermes-agent`, pushes the contribution branch, and opens a draft—not ready and not merged—pull request.
7. Re-runs the required GSD code/security/UI/Nyquist and milestone gates against the final source commit; any blocker or unaccepted medium finding keeps the PR in draft.

No step publishes to npm, merges the PR, publishes the marketplace package, creates a GitHub Release, or performs the post-merge signed-build smoke test.
</verification>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan coverage | Status | Notes |
|--------|----|-----------------------|---------------|--------|-------|
| GOAL | quick | Exact local marketplace package ready for root-agent submission | Tasks 1-3 plus root-agent gates | COVERED | Local executor produces and proves the package; D-04 reserves public/upstream mutations for the root agent. |
| GOAL | Phase 7 | Signed store artifact passes clean-profile smoke testing | none | EXCLUDED | Dependency conflict: requires upstream merge and marketplace publication, which the user explicitly did not authorize in this task. |
| REQ | MKT-01 | Author/categories/GitHub/icon/listing paths | Task 1 | COVERED | Frozen metadata remains asserted while screenshot ordering changes. |
| REQ | MKT-02 | Icon plus listing screenshots | Tasks 1 and 3 | COVERED | Existing three-image wording is superseded by D-01/D-02 and synchronized to four images. |
| REQ | MKT-03 | Complete README contract | Task 2 | COVERED | Existing sections remain; release links are added. |
| REQ | MKT-04 | Dist assets and local/upstream validation | Tasks 1 and 3 plus root-agent gates | COVERED | Local dist is executor-owned; upstream scripts are root-owned per D-04. |
| REQ | MKT-05 | GSD reviews/audits have no unresolved blocker or medium | root-agent gate 7 | COVERED | Must run against the final local source commit before the draft advances. |
| REQ | MKT-06 | Clean `gabeosx` sparse-checkout contribution and upstream PR | root-agent gates 1-6 | COVERED | External mutation is deliberately outside the local executor per D-04. |
| REQ | MKT-07 | Signed store build smoke tests | none | EXCLUDED | Dependency conflict: only possible after merge/publication; remains pending. |
| REQ | MKT-08 | No telemetry, rollback docs, immutable 0.1.0 | Tasks 2 and 3 | COVERED | Governance validator and docs enforce the contract. |
| RESEARCH | marketplace listing | One icon and one-to-six exact 1600x1000 PNG screenshots | Task 1 | COVERED | Four images satisfy the official limit. |
| RESEARCH | packaging | Only deterministic `dist/` ships and contains the manifest/assets | Task 1 | COVERED | Existing recursive asset copy is exercised by strengthened allowlist validation. |
| RESEARCH | upstream contribution | Validate and dry-run pack from sparse checkout | root-agent gates 2-6 | COVERED | Uses the official `validate.mjs` and `pack.mjs --dry-run` commands. |
| CONTEXT | D-01 | Purpose-built readable first hero | Task 1 | COVERED | Exact thumbnail inspection is required. |
| CONTEXT | D-02 | Retain three feature images after hero | Task 1 | COVERED | Exact ordering is asserted. |
| CONTEXT | D-03 | Changelog, release guide, one CI workflow; no publishing | Tasks 2 and 3 | COVERED | No publication path is added. |
| CONTEXT | D-04 | Root owns public/fork/push/draft PR | root-agent gates | COVERED | Executor stops after local verification. |
| CONTEXT | D-05 | Preserve untracked files | All tasks | COVERED | Explicit guard and pathspec discipline are recorded. |
</source_audit>

<success_criteria>
1. The first listed screenshot is a sanitized 1600x1000 hero whose exact product message remains legible at 442x276; Operations, Approvals, and Kanban remain unchanged in positions two through four.
2. Source and dist manifests are identical, the deterministic dist inventory contains the icon and four declared screenshots, and permissions/dependencies/version remain frozen.
3. CHANGELOG.md, RELEASING.md, README.md, and the single bounded GitHub workflow implement the agreed versioning and release procedure without npm publishing.
4. `npm test`, `npm run validate`, and `npm run qualify` pass locally with qualification cleanup proven.
5. Planning requirements accurately state four screenshots, and the root agent has exact, bounded instructions for public-repository conversion, sparse-checkout packaging, Muxy upstream validation/build/dry-run pack, audits, and a draft-only PR.
6. Protected untracked files are byte-for-byte preserved and unstaged.
</success_criteria>

<output>
Create `.planning/quick/260821-k4x-prepare-the-hermes-agent-marketplace-sub/260821-k4x-SUMMARY.md` when the local executor completes; the root agent continues with the external/upstream gates afterward.
</output>
