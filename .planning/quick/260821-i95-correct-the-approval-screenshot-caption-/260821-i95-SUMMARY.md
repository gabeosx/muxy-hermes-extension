---
quick_id: 260821-i95
status: complete
completed: 2026-08-21
commit: 679b806
---

# Accurate approval screenshot caption

Both approval images now explain the choice without implying that Allow once and Deny are the only controls.

## Delivered

- Replaced the misleading caption with: "Review each requested action, then choose how long to allow it — or deny it."
- Preserved the full approval interface, including Allow once, Allow for session, Always allow, and Deny.
- Kept the README image at 760x475 and the marketplace image at 1600x1000.
- Rejected built-in image-edit variants that changed the canvas or softened the interface, then used a deterministic text-layer replacement for the final assets.

## Visual and privacy verification

- Apple Vision OCR recognized the corrected three-line caption and all four approval controls in both images.
- Pixel comparison confirmed every changed pixel is confined to the original caption region.
- Manual native-size inspection found no visible background seam, UI change, blur, or text collision.
- Embedded-string checks found no personal identifiers, hosts, URLs, credentials, tokens, or run/session identifiers.

## Repository verification

- `npm test`: 78/78 passed.
- `npm run build`: passed.
- `npm run validate:dist`: deterministic 17-file distribution passed.
- `npm run validate`: passed with zero high/critical audit findings.
- `git diff --check`: passed.
