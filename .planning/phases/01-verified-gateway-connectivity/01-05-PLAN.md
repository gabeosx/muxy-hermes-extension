---
phase: 01-verified-gateway-connectivity
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - fixtures/simulations/docker-compose.yml
  - fixtures/simulations/scenarios.json
  - fixtures/simulations/proxy-nginx.conf
  - fixtures/simulations/tls/README.md
  - scripts/qualify-simulations.mjs
  - test/simulated-ssh-workspace.test.ts
  - test/simulated-https.test.ts
autonomous: true
requirements:
  - DEPL-04
  - DEPL-05
  - DEPL-06
  - SEC-02
  - SEC-04
  - EVID-01
  - EVID-02
estimate:
  tokens: 33000
  raw_tokens: 33000
  tasks: 2
  confidence: low
user_setup:
  - service: local-test-pki
    why: "The Docker HTTPS simulation must exercise normal WebKit certificate validation without adding a bypass."
    env_vars:
      - name: SIM_TLS_CERT_PATH
        source: "A certificate for the documented loopback test hostname chained to a CA the operator has deliberately trusted for this fixture"
      - name: SIM_TLS_KEY_PATH
        source: "The matching local test key, stored outside the repository"
must_haves:
  truths:
    - "Per D-08, Docker simulations exercise SSH local-forward loss/restoration, HTTPS certificate/auth/CORS/proxy buffering, and remote-workspace namespace separation, but all three deployment classes remain Unverified."
    - "Per D-01/D-02, no simulated pass can establish Supported and no absent local certificate/infrastructure can establish Unsupported; those outcomes remain Unverified with an explicit reason."
    - "The simulation runner uses the same built panel, GatewayClient, URL/token contract, evidence schema, and safe diagnostics as the real lanes; no topology branch appears in extension code."
    - "The SSH-forward simulation loses/restores only its Docker proxy while preserving the Gateway container, allowing evidence to distinguish observed proxy unreachability from Gateway state without claiming a real tunnel."
    - "The HTTPS simulation uses normal platform trust, separately exercises certificate rejection, bearer auth, exact-origin CORS, and unbuffered first-chunk timing, and provides no certificate bypass."
    - "The remote-workspace simulation places a unique path sentinel in the harness/container namespace and proves it is absent from every panel request and durable evidence record."
  artifacts:
    - path: "fixtures/simulations/docker-compose.yml"
      provides: "Isolated Gateway, TCP-forward analogue, HTTPS reverse proxy, and namespace-mismatch services"
    - path: "fixtures/simulations/scenarios.json"
      provides: "Machine-readable simulation identity, required observations, and forced-Unverified policy"
    - path: "fixtures/simulations/proxy-nginx.conf"
      provides: "Explicit streaming proxy configuration plus controlled buffering-failure mode"
    - path: "scripts/qualify-simulations.mjs"
      provides: "Harness-only simulation lifecycle and safe evidence adapter"
    - path: "test/simulated-ssh-workspace.test.ts"
      provides: "Tunnel-loss/restoration, forced-Unverified, and workspace-sentinel tests"
    - path: "test/simulated-https.test.ts"
      provides: "TLS/auth/CORS/buffering and no-bypass tests"
  key_links:
    - from: "scripts/qualify-simulations.mjs"
      to: "fixtures/simulations/scenarios.json"
      via: "runner loads declared simulation semantics and forces realPath false/simulation true"
      pattern: "realPath|simulation|Unverified"
    - from: "scripts/qualify-simulations.mjs"
      to: "scripts/run-validation.mjs"
      via: "safe observations use the shared evidence pipeline and classifier"
      pattern: "run-validation"
    - from: "fixtures/simulations/proxy-nginx.conf"
      to: "test/simulated-https.test.ts"
      via: "tests assert explicit unbuffered configuration and a separate buffering failure scenario"
      pattern: "proxy_buffering|X-Accel-Buffering"
  edge_cases:
    - id: "DEPL-04-unclassified"
      status: resolved
      statement: "The local proxy is explicitly labeled a simulation, loss/restoration is reproducible, and every emitted SSH-forward verdict is forced Unverified until a real SSH path is tested."
    - id: "DEPL-05-concurrency"
      status: resolved
      statement: "Buffered and unbuffered proxy scenarios use isolated Compose service/port identities and independent evidence run IDs so concurrent results cannot overwrite or cross-label one another."
    - id: "DEPL-06-unclassified"
      status: resolved
      statement: "A unique remote-workspace path sentinel is absent from request URLs, headers, bodies, panel state, and evidence while the simulated row remains Unverified."
  prohibitions:
    - requirement_id: "DEPL-04"
      category: "transparency"
      status: "unverified"
      flagged: true
      statement: "MUST NOT call the Docker TCP-proxy exercise a real SSH tunnel test or use it to establish SSH-forwarded support."
    - requirement_id: "DEPL-05"
      category: "transparency"
      status: "unverified"
      flagged: true
      statement: "MUST NOT call a loopback Docker TLS/reverse-proxy exercise a real remote-network HTTPS qualification or present it as Supported."
    - requirement_id: "DEPL-06"
      category: "privacy"
      status: "unverified"
      flagged: true
      statement: "MUST NOT send, infer, display, hash, or persist a Muxy workspace path as part of Gateway connectivity."
---

## Phase Goal

**As a** Hermes Gateway user, **I want to** inspect realistic failure and namespace simulations without inflated support claims, **so that** untested remote deployment classes remain truthful and safe.

<objective>
Exercise all three deferred real-path classes locally through Docker while enforcing their permanent Phase 1 Unverified status.

Purpose: Cover D-08 and DEPL-04/05/06 failure boundaries without acquiring real remote infrastructure or introducing topology-specific extension behavior.
Output: Docker simulation matrix, tunnel/proxy/namespace scenarios, safe runner, and forced-Unverified tests/evidence.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-RESEARCH.md
@.planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md
@.planning/phases/01-verified-gateway-connectivity/01-02-SUMMARY.md
@.planning/phases/01-verified-gateway-connectivity/01-03-SUMMARY.md
</context>

<interfaces>
Use Plan 02's unchanged panel/client request contract and Plan 03's `EvidenceRecord`/`classifyVerdict` interfaces. Every simulation observation must set `realPath: false`, `simulation: true`, and the canonical deployment condition before classification.
</interfaces>

## Artifacts this phase produces

- Paths: `fixtures/simulations/docker-compose.yml`, `fixtures/simulations/scenarios.json`, `fixtures/simulations/proxy-nginx.conf`, `fixtures/simulations/tls/README.md`, `scripts/qualify-simulations.mjs`, `test/simulated-ssh-workspace.test.ts`, `test/simulated-https.test.ts`, and dynamic redacted simulation report pairs under `public/evidence/runs/`.
- Symbols: `SimulationScenario`, `loadSimulationMatrix`, `runTunnelAnalogue`, `runRemoteWorkspaceSentinel`, `runHttpsProxyScenario`, `assertSimulationVerdict`, and `qualifySimulation`.
- Commands: `node scripts/qualify-simulations.mjs --scenario ssh-forward`, `--scenario remote-workspace`, `--scenario https-proxy`, and focused Node/Compose checks.
- Fields: `simulation`, `realPath`, `scenarioId`, `proxyState`, `gatewayState`, `tlsOutcome`, `certificateClass`, `authOutcome`, `corsOutcome`, `bufferingOutcome`, `workspaceSentinelAbsent`, and forced `verdict: Unverified`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exercise simulated SSH loss/restoration and remote-workspace separation</name>
  <reversibility rating="costly">Per D-08, replacing local simulations with real remote qualification would add infrastructure, credentials, and real-path acceptance work across this milestone.</reversibility>
  <precondition>Docker/Compose are available and the Plan 03 evidence tests pass.</precondition>
  <read_first>
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-01/D-02/D-05/D-08 and D-12 through D-15
    - .planning/phases/01-verified-gateway-connectivity/01-RESEARCH.md — simulated-class and namespace constraints
    - src/verdict.ts — Plan 03 forced-Unverified classification rules
    - scripts/run-validation.mjs — safe evidence boundary
  </read_first>
  <files>fixtures/simulations/docker-compose.yml, fixtures/simulations/scenarios.json, scripts/qualify-simulations.mjs, test/simulated-ssh-workspace.test.ts</files>
  <behavior>
    - "SSH-forward analogue runs the unchanged panel against a loopback TCP proxy to Hermes, records loss and restoration by stopping/starting only the proxy, and never claims an actual SSH connection."
    - "Gateway remains independently observable in the harness during proxy loss, but the panel reports only its browser-visible request/stream outcome."
    - "Remote-workspace scenario uses a deliberately different container workspace sentinel and verifies no request/evidence field contains it."
    - "Every attempt is realPath false, simulation true, and Unverified regardless of passed checks."
  </behavior>
  <action>Write the forced-Unverified, loss/restoration ordering, no-topology-branch, and workspace-sentinel tests first. Define canonical scenario records for `ssh-forward` and `remote-workspace`. Create a Compose topology with a resolved Hermes container plus a separately controllable loopback-published TCP proxy that stands in for a local forward; the runner starts the stack, records a baseline capability/stream attempt, stops only the proxy during a stream, records the browser-visible interruption, restores the proxy, and repeats without changing client code. For the namespace scenario, give the container a unique remote-style workspace path that never enters the panel's input/model, scan the ephemeral captured request structure for the sentinel, and store only `workspaceSentinelAbsent: true|false`. Route all observations through Plan 03 and assert Unverified before and after loss/restoration. The runner and reports must say `Docker simulation`, never real SSH or real remote workspace.</action>
  <verify>
    <automated>docker compose -f fixtures/simulations/docker-compose.yml config &amp;&amp; npm run build &amp;&amp; node --test test/simulated-ssh-workspace.test.ts test/verdict.test.ts test/evidence.test.ts</automated>
    <human-check>Run the two simulation scenarios in the real Muxy panel and confirm both matrix rows stay Unverified, explicitly say they are local Docker simulations, and expose no workspace path.</human-check>
  </verify>
  <acceptance_criteria>
    - "Tunnel analogue loss/restoration is reproducible and reported from observed transport facts without topology detection."
    - "Remote-workspace sentinel is absent from every request and durable artifact."
    - "No execution path can classify either simulated deployment class as Supported."
  </acceptance_criteria>
  <done>Users can inspect truthful simulated SSH-forward and remote-workspace observations that remain Unverified by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise TLS validation, exact CORS, authentication, and proxy buffering locally</name>
  <precondition>SIM_TLS_CERT_PATH and SIM_TLS_KEY_PATH point to a loopback-hostname certificate/key whose CA the operator deliberately trusts for this fixture; no trust-store or certificate-bypass change is performed by the runner.</precondition>
  <read_first>
    - fixtures/simulations/docker-compose.yml — Task 1 simulation services
    - fixtures/simulations/scenarios.json — forced-Unverified scenario contract
    - scripts/qualify-simulations.mjs — Task 1 runner and evidence adapter
    - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md — non-loopback HTTPS and no-bypass copy
    - .planning/phases/01-verified-gateway-connectivity/01-RESEARCH.md — TLS/CORS/proxy buffering obligations
  </read_first>
  <files>fixtures/simulations/docker-compose.yml, fixtures/simulations/proxy-nginx.conf, fixtures/simulations/tls/README.md, scripts/qualify-simulations.mjs, test/simulated-https.test.ts</files>
  <behavior>
    - "Invalid/untrusted certificate case fails under normal WebKit validation and offers no bypass."
    - "Trusted fixture-certificate case exercises bearer auth and exact-origin CORS on capabilities and SSE through the HTTPS proxy."
    - "Unbuffered configuration yields observable incremental chunks; controlled buffering configuration fails the first-chunk/timing gate."
    - "Isolated buffered/unbuffered attempts keep distinct IDs/ports and remain Unverified because the network path is local simulation."
  </behavior>
  <action>Write certificate, no-bypass, auth, exact-origin, buffering, isolation, redaction, and forced-Unverified tests first. Extend Compose with an HTTPS reverse proxy using only certificate/key paths supplied at runtime from outside the repository. Configure one unbuffered service according to SSE proxy requirements and one deliberately buffered failure service with separate identities. Add an invalid-certificate scenario that relies on normal WebKit failure; neither runner nor panel may modify trust settings or ignore validation. Extend the simulation runner to exercise capabilities and the harmless SSE probe through each service, collect safe TLS/auth/CORS/first-chunk outcomes, and keep concurrent scenario observations isolated. Record the local nature and force direct-remote-HTTPS to Unverified even when the trusted/unbuffered lane passes. Document operator certificate setup/removal responsibilities without storing key material or a certificate identity in evidence.</action>
  <verify>
    <automated>docker compose -f fixtures/simulations/docker-compose.yml config &amp;&amp; npm run build &amp;&amp; node --test test/simulated-https.test.ts test/verdict.test.ts test/evidence.test.ts</automated>
    <human-check>In current Muxy, run invalid-certificate, trusted/unbuffered, and trusted/buffered scenarios; confirm normal certificate rejection has no bypass, buffering is visible as a failed streaming gate, and the Direct remote HTTPS row remains Unverified.</human-check>
  </verify>
  <acceptance_criteria>
    - "TLS, bearer auth, exact-origin CORS, and streaming-buffer behavior each receive an observed safe result."
    - "The runner never changes certificate trust, commits key/certificate identity, or adds a client bypass."
    - "Direct remote HTTPS stays Unverified in code, evidence, report copy, and UI regardless of simulation outcome."
  </acceptance_criteria>
  <done>Users can inspect local HTTPS/reverse-proxy evidence without confusing it for a real remote qualification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| simulation runner → Docker services | Harness owns lifecycle and fault injection unavailable to the extension. |
| Muxy WebKit → TLS proxy/Gateway | Certificate, bearer, exact-origin CORS, and buffering are exercised under browser policy. |
| local simulation → deployment verdict | Passing local conditions could be mislabeled as real remote support. |
| workspace/cert/raw traffic → evidence | Namespace and certificate identity must remain ephemeral. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information disclosure | simulation env and request capture | high | mitigate | Runtime-only bearer/cert paths, workspace sentinel assertions, and Plan 03 sanitizer prevent secret/path retention. |
| T-01-02 | Elevation of privilege | simulated CORS | high | mitigate | Require exact observed origin on each route; wildcard/null/reflection fails even though verdict remains Unverified. |
| T-01-03 | Tampering / Information disclosure | HTTPS proxy | high | mitigate | Normal WebKit trust only, explicit invalid-cert test, externally supplied trusted fixture cert, and no bypass/trust mutation in code. |
| T-01-04 | Elevation of privilege | Docker/proxy fault injection | high | mitigate | Scripts-only lifecycle adapters; unchanged panel contract and static authority checks prevent SSRF/infrastructure control expansion. |
| T-01-05 | Information disclosure | proxy logs, workspace namespace, SSE | high | mitigate | Keep raw traffic ephemeral and commit only booleans, trust class, safe timing/shape/hash metadata. |
</threat_model>

<verification>
- Both focused Node suites and Compose structural validation pass.
- Simulation rows are asserted Unverified at classifier, JSON, Markdown, index, and UI-consumer boundaries.
- No workspace sentinel, TLS key/certificate identity, bearer, endpoint identity, or raw payload reaches durable evidence.
</verification>

<success_criteria>
- All three deferred real-path classes have reproducible local simulations and explicit Unverified verdicts.
- TLS/CORS/auth/buffering and proxy loss/restoration boundaries are exercised without client exceptions.
- No workspace path or infrastructure authority crosses into the panel.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-05-SUMMARY.md` when done.
</output>
