---
phase: 01-verified-gateway-connectivity
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - src/curl-relay.js
  - src/panel/app.js
  - test/curl-relay.test.js
  - test/ui-contract.test.js
  - fixtures/simulations/docker-compose.yml
  - fixtures/simulations/scenarios.json
  - scripts/qualify-simulations.mjs
  - test/simulated-relay.test.js
autonomous: false
requirements:
  - CONN-02
  - CONN-05
  - DEPL-02
  - DEPL-03
  - DEPL-04
  - DEPL-05
  - DEPL-06
  - SEC-01
  - SEC-02
  - SEC-04
  - EVID-01
  - EVID-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The actual Muxy panel launches only argv-form /usr/bin/curl operations, and the bearer appears only in exec stdin—not argv, URL, environment, journal, rendered state, or the Muxy audit summary."
    - "Exactly one audited exec owns a live SSE stream; file.changed plus muxy.files.read delivers incremental journal additions without exec polling."
    - "The active journal is capped at 4 MiB, scrubbed before removal, and stale extension-owned journals are scrubbed on the next panel startup before any real user content is accepted."
    - "A remembered curl grant is disclosed as executable-wide authority and denial remains a safe, retryable relay failure."
    - "Host-native and Docker use identical panel/client code; SSH, HTTPS, and remote-workspace simulations remain Unverified and never establish real-path support."
    - "No Muxy/Hermes source change, provider registration, external daemon, public ingress, Docker control in the panel, or background stream owner is introduced."
  artifacts:
    - path: "src/curl-relay.js"
      provides: "Real-panel relay, incremental journal, and stale cleanup boundary"
    - path: "test/curl-relay.test.js"
      provides: "Secret, audit, one-exec, journal-limit, and cleanup tests"
    - path: "fixtures/simulations/scenarios.json"
      provides: "Forced-Unverified simulation contract for SSH/HTTPS/remote-workspace conditions"
    - path: "scripts/qualify-simulations.mjs"
      provides: "Harness-only simulation evidence adapter using the shared relay observations"
  key_links:
    - from: "src/curl-relay.js"
      to: "package.json"
      via: "commands:exec, files:read/write, and file.changed are the exact required Muxy authorities"
      pattern: "commands:exec|files:read|files:write|file.changed"
    - from: "scripts/qualify-simulations.mjs"
      to: "scripts/run-validation.mjs"
      via: "all simulated observations remain realPath false and simulation true"
      pattern: "realPath|simulation|Unverified"
  prohibitions:
    - requirement_id: "SEC-01"
      category: "information-disclosure"
      status: "unverified"
      flagged: true
      statement: "MUST NOT place bearer material in curl argv, URL, environment, journal, audit summary, source, storage, diagnostics, or evidence."
    - requirement_id: "DEPL-04"
      category: "transparency"
      status: "unverified"
      flagged: true
      statement: "MUST NOT label local Docker network interruption as a real SSH-tunnel qualification."
    - requirement_id: "DEPL-05"
      category: "transparency"
      status: "unverified"
      flagged: true
      statement: "MUST NOT label local proxy/TLS behavior as real remote-HTTPS support."
---

## Phase Goal

**As a** Hermes Gateway user, **I want to** explicitly authorize and inspect the relay that carries my run, **so that** local streaming works without hidden Muxy changes or noisy exec polling.

<objective>
Prove the approved curl/journal relay in the actual Muxy panel, then exercise the same unchanged contract across the local Docker simulations.

Purpose: Convert Plan 04's unit-tested fallback into an observed Muxy integration before exposing real user run content or assigning deployment support.
Output: Startup stale-journal cleanup, real-panel relay evidence, audit sentinel check, and forced-Unverified local simulation results.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-04-SUMMARY.md
</context>

<interfaces>
Use Plan 04's `CurlRelay` and `ConnectionProbe` exactly. A simulation may supply a different URL and harness lifecycle, but extension code receives no topology label or control. Plan 03 evidence remains fail-closed until its origin-era schema is migrated in Plan 06.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove relay cleanup and one-stream behavior in the actual Muxy panel</name>
  <read_first>
    - src/curl-relay.js — approved relay and cleanup boundary
    - src/panel/app.js — consent disclosure and panel lifecycle
    - test/curl-relay.test.js — current one-exec and secret-sentinel coverage
    - .planning/phases/01-verified-gateway-connectivity/01-04-SUMMARY.md — unresolved real-panel and stale-cleanup gates
  </read_first>
  <files>src/curl-relay.js, src/panel/app.js, test/curl-relay.test.js, test/ui-contract.test.js</files>
  <behavior>
    - "On panel startup, enumerate only `.muxy-hermes-runtime`; overwrite every regular journal file with an empty string before removing its extension-owned run directory. A missing root is a no-op; unexpected entries fail closed and are not deleted."
    - "The panel does not start a new probe until stale cleanup has completed or produced a visible safe failure."
    - "A controlled stream produces incremental panel observations while one curl exec remains active, then scrubs/removes its journal."
    - "A sentinel bearer is absent from displayed curl argv, Muxy audit summary, worktree files after cleanup, DOM/result state, dist, and committed evidence."
  </behavior>
  <action>Write stale-cleanup, unsafe-entry refusal, and panel-start gating tests first. Implement cleanup using only `muxy.files.list/read/write/delete` within the fixed runtime root; never accept a caller-provided cleanup root and never follow links or delete an unexpected file shape. Rebuild/reload Muxy, run the isolated harmless fixture, authorize the displayed argv-form curl and scrub/remove operations, and verify one active exec plus incremental file.changed delivery. Inspect only the redacted audit line needed to assert the sentinel is absent; do not persist the raw audit log.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/curl-relay.test.js test/transport-tracer.test.js test/probe-state.test.js test/ui-contract.test.js &amp;&amp; node scripts/validate-dist.mjs</automated>
    <human-check>In Muxy 1.5.0 build 945, the controlled fixture reaches Connection verified, the stream counter advances before completion, the displayed/audited argv contains no token, and the runtime journal is gone after cleanup.</human-check>
  </verify>
  <acceptance_criteria>
    - "Startup stale cleanup is fixed-root, scrub-before-remove, and fail-closed for unexpected entries."
    - "The actual panel proves one exec plus native file reads; no repeated exec audit entries appear."
    - "The bearer sentinel is absent from argv, audit summary, files, UI state, dist, and evidence."
  </acceptance_criteria>
  <done>The relay is safe enough to carry the controlled development fixture through actual Muxy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise the unchanged relay across local Docker simulations</name>
  <precondition>Task 1's actual Muxy tracer passes. Docker/Compose are available.</precondition>
  <read_first>
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-08 and D-16 through D-21
    - scripts/run-validation.mjs — safe evidence boundary
    - src/verdict.js — forced-Unverified classifier
    - .planning/phases/01-verified-gateway-connectivity/01-04-SUMMARY.md — latest stable pair and relay boundary
  </read_first>
  <files>fixtures/simulations/docker-compose.yml, fixtures/simulations/scenarios.json, scripts/qualify-simulations.mjs, test/simulated-relay.test.js</files>
  <behavior>
    - "Docker-published loopback exercises the same URL/token/relay path as host-native and documents refusal plus mid-stream interruption without panel Docker authority."
    - "SSH-forward analogue, local HTTPS/proxy, and remote-workspace namespace conditions are labeled simulations, use realPath false/simulation true, and remain Unverified regardless of passing observations."
    - "A unique workspace sentinel is absent from request bodies, journal metadata, diagnostics, and durable evidence."
    - "Missing local TLS material leaves HTTPS Unverified rather than adding certificate bypass or blocking the host/Docker relay proof."
  </behavior>
  <action>Write a single forced-Unverified scenario-matrix suite first. Reuse the existing isolated Hermes fixture and add only harness-owned Docker/proxy lifecycle. Run refusal/interruption/restoration observations through the shared safe evidence adapter. Do not create real remote infrastructure, modify trust stores, or make the panel aware of Docker/SSH/proxy/workspace topology.</action>
  <verify>
    <automated>docker compose -f fixtures/simulations/docker-compose.yml config &amp;&amp; npm run build &amp;&amp; node --test test/simulated-relay.test.js test/verdict.test.js test/evidence.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Host/Docker use identical extension code and only the supplied URL differs."
    - "All three simulated remote classes are permanently Unverified by construction."
    - "No workspace path, certificate/key identity, token, endpoint identity, or raw payload reaches durable evidence."
  </acceptance_criteria>
  <done>Representative deployment conditions have truthful relay evidence without remote infrastructure or topology-specific client behavior.</done>
</task>

</tasks>

<verification>
- Actual Muxy relay tracer passes before Docker expansion.
- Full build/tests/dist validation pass.
- Simulated rows remain Unverified and secret/path sentinels remain absent.
</verification>

<success_criteria>
- The real panel demonstrates one audited curl stream plus native journal reads and cleanup.
- Host/Docker share one relay implementation; remote simulations cannot establish support.
- No Muxy/Hermes changes, provider registration, daemon, public ingress, or background stream ownership is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-05-SUMMARY.md` when done.
</output>
