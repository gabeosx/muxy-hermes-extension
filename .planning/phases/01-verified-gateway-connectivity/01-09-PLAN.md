---
phase: 01-verified-gateway-connectivity
plan: 09
type: execute
wave: 8
depends_on:
  - 01-08
files_modified:
  - src/evidence.js
  - scripts/run-validation.mjs
  - public/evidence/schema-v2.json
  - test/evidence-provenance.test.js
autonomous: true
gap_closure: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
  - SEC-01
  - SEC-02
  - EVID-01
  - EVID-02
  - EVID-03
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-05 and D-12 through D-15, only a correlated verifier, panel, relay, fixture, and cleanup receipt bundle can create support-eligible paired evidence."
    - "The durable schema uses the reconciled `url`, `relay`, `authentication`, `capabilities`, `stream`, and `cleanup` stages; direct WebKit remains historical negative evidence and browser Origin is not a curl-relay success predicate."
    - "The generic validation CLI cannot accept caller-authored positive observations, stage maps, freshness, provenance, or SSE metadata."
    - "Schema-version 1 artifacts remain readable history but cannot establish support under the approved relay architecture."
    - "Failure and incomplete attempts remain publishable only through an allowlisted support-ineligible builder."
  artifacts:
    - path: "src/evidence.js"
      provides: "Receipt-bundle validation, allowlist projection, paired publication, history compatibility, and separate verified/unverified builders"
      exports: ["buildVerifiedEvidenceRecord", "buildUnverifiedEvidenceRecord", "validateEvidenceRecord", "renderEvidenceMarkdown"]
    - path: "public/evidence/schema-v2.json"
      provides: "Machine-readable verifier-receipt evidence contract"
    - path: "scripts/run-validation.mjs"
      provides: "Failure/incomplete adapter that cannot author support-eligible observations"
    - path: "test/evidence-provenance.test.js"
      provides: "Receipt correlation, replay, mixed-pair, CLI bypass, pairing, concurrency, and redaction coverage"
  key_links:
    - from: "src/probe.js"
      to: "src/evidence.js"
      via: "Plan 08 panel-session receipt is accepted only inside a matching verifier/fixture/relay bundle"
      pattern: "qualificationId|receiptDigest|fixtureReceiptDigest"
    - from: "src/evidence.js"
      to: "public/evidence/schema-v2.json"
      via: "allowlisted projection validates before paired JSON/Markdown publication and index handoff"
      pattern: "schemaVersion|requiredStages|proofSource"
    - from: "scripts/run-validation.mjs"
      to: "src/evidence.js"
      via: "generic failure mode calls only the support-ineligible builder"
      pattern: "buildUnverifiedEvidenceRecord"
  prohibitions:
    - requirement_id: "EVID-02"
      category: "evidence-integrity"
      statement: "Caller-authored JSON, one session, duplicate panel identities, mixed version tuples, simulations, missing cleanup, or historical browser-origin claims may not establish Supported."
    - requirement_id: "SEC-01"
      category: "information-disclosure"
      statement: "Receipts, reports, indexes, CLI errors, and concurrency paths may not retain secret, endpoint, content, path, raw subprocess, or audit data."
---

<objective>
Publish verifier-owned receipt bundles through a versioned relay-stage evidence contract and close the arbitrary-positive CLI boundary.

Purpose: Convert Plan 08's safe runtime handoff into durable paired evidence without letting callers manufacture support claims.
Output: Schema-version 2 evidence API/schema, historical reader compatibility, support-ineligible failure adapter, and focused provenance tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md
@.planning/phases/01-verified-gateway-connectivity/01-08-SUMMARY.md
</context>

<interfaces>
Consume Plan 08's `QualificationChallenge`, `RelayReceipt`, and `PanelSessionReceipt`. The verified builder additionally requires a verifier-owned `FixtureReceipt` and exact resolved version tuple. Its output is the sole support-eligible interface consumed by Plan 10; the generic CLI is not a positive-evidence path.
</interfaces>

## Artifacts this phase produces

- Durable schema fields: `schemaVersion: 2`, `proofSource`, `qualificationId`, hashed panel/relay identities, receipt digests, exact version tuple, required relay stages, safe frame metadata, and cleanup outcome.
- Publication interface: paired JSON/Markdown atomic write and append-only index handoff, with schema-version 1 read compatibility.
- CLI boundary: failure/incomplete input only; no arbitrary positive observation ingestion.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish one correlated verifier receipt bundle as paired schema-versioned evidence</name>
  <reversibility rating="costly">Changing the durable evidence schema requires version-aware readers and preservation of historical report/index references.</reversibility>
  <read_first>
    - src/evidence.js — current schema-version 1 allowlist, paired writes, and atomic index lock
    - public/evidence/schema-v1.json — historical contract retained read-only
    - test/evidence.test.js — existing publication and redaction coverage
    - .planning/phases/01-verified-gateway-connectivity/01-08-SUMMARY.md — actual receipt types and reconciled stage vocabulary
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-05 and D-09 through D-19
  </read_first>
  <files>src/evidence.js, public/evidence/schema-v2.json, test/evidence-provenance.test.js</files>
  <behavior>
    - "A verified record requires matching challenge, panel, relay, fixture, cleanup, exact tuple, ordinal, and digest values."
    - "2xx authenticated incremental stream plus terminal frame and successful cleanup populate relay stages; no browser-origin stage exists."
    - "Every mismatch, replay, duplicate panel, mixed version, unsafe field, incomplete stage, or concurrent collision fails before a durable positive write."
    - "JSON and Markdown are paired atomically, history remains append-only, and schema-version 1 records remain historical-only."
  </behavior>
  <action>Start with failing provenance, replay, mixed-version, raw-field, report-pair, and concurrent-index tests. Add schema version 2 with `url`, `relay`, `authentication`, `capabilities`, `stream`, and `cleanup`; validate one-use qualification, exact tuple, ordinal, distinct panel identity, relay/fixture digests, 2xx status, incremental allowlisted frames, terminal outcome, and successful cleanup before projecting durable fields. Implement `buildVerifiedEvidenceRecord(verifierReceipt)` and preserve paired atomic publication, append-only history, safe error codes, and schema-version 1 read compatibility. Project only D-14/D-15 allowlisted facts and keep D-16's direct-WebKit result outside the positive stage contract. Apply D-05 and D-09 through D-19.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/evidence-provenance.test.js test/evidence.test.js test/qualification-receipt.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "One matching receipt bundle produces exactly one paired JSON/Markdown record and append-only index handoff."
    - "Every mismatched, replayed, mixed-version, unsafe, or incomplete bundle fails without a durable positive artifact."
    - "Historical schema-version 1 reads still work but never satisfy the new relay predicate."
  </acceptance_criteria>
  <done>Durable positive evidence originates only from the correlated relay session and fixture that actually ran.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the generic validation CLI's caller-authored positive path</name>
  <read_first>
    - scripts/run-validation.mjs — arbitrary input boundary
    - src/evidence.js — Task 1 verified and unverified builder contracts
    - test/evidence-provenance.test.js — Task 1 provenance and pairing harness
    - .planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md — forgeable evidence gap
  </read_first>
  <files>scripts/run-validation.mjs, src/evidence.js, test/evidence-provenance.test.js</files>
  <behavior>
    - "The former arbitrary input mode is rejected and cannot choose proof source, real-path eligibility, freshness, stage success, or SSE evidence."
    - "A separate failure/incomplete adapter hard-codes support ineligibility and accepts only safe reason/stage metadata."
    - "Caller data cannot select the verified builder or fabricate verifier, panel, relay, fixture, or cleanup receipts."
    - "Safe failure records retain paired publication and redaction guarantees."
  </behavior>
  <action>Add failing CLI-bypass tests before changing the adapter. Remove arbitrary observation ingestion and expose only an explicit failure/incomplete mode that constructs `buildUnverifiedEvidenceRecord` input from an allowlisted reason, failed stage, exact resolved tuple, and simulation/real-attempt category while hard-coding support ineligibility. Reject caller fields for real-path status, proof source, session freshness, stage booleans, receipt identities, and stream metadata. Keep `--out` target validation, paired publication, safe errors, and current report history. Apply D-02, D-05, D-12, D-14, and D-15.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/evidence-provenance.test.js test/evidence.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The old arbitrary input interface and every positive-field bypass fail nonzero."
    - "Failure/incomplete attempts remain publishable but are structurally support-ineligible."
    - "No secret or raw observation reaches reports, indexes, stdout, stderr, or errors."
  </acceptance_criteria>
  <done>The generic CLI can record a safe incomplete/failure outcome but cannot manufacture support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| receipt bundle → durable evidence | Only correlated safe receipts may create positive claims. |
| CLI caller → evidence builder | Untrusted arguments must not choose support predicates. |
| schema-version 1 history → current classifier | Historical origin-era records must remain readable without becoming eligible. |
| dependency graph → build | Schema work uses existing Node APIs only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09-01 | Spoofing | positive evidence builder | high | mitigate | Require verifier/panel/relay/fixture/cleanup correlation and exact tuple/ordinal identity. |
| T-01-09-02 | Tampering / Replay | paired publication and index | high | mitigate | Validate digests, refuse replay/collision, and preserve atomic lock-guarded append-only writes. |
| T-01-09-03 | Information disclosure | schema/report/error projection | high | mitigate | Use allowlists and sentinel tests across successful, failed, concurrent, and malformed paths. |
| T-01-09-04 | Elevation of privilege | generic CLI | high | mitigate | Remove caller-controlled positive observations and route only to the support-ineligible builder. |
| T-01-SC | Tampering | package supply chain | high | mitigate | Add no dependency/install task and retain the existing lockfile. |
</threat_model>

<verification>
- Evidence/provenance tests prove correlated publication, redaction, history compatibility, concurrency, and replay refusal.
- CLI bypass tests prove caller-authored JSON or positive stage data cannot create eligible evidence.
- The schema contains no browser-origin success stage and retains direct-WebKit evidence as separate history.
</verification>

<success_criteria>
- Support-eligible durable evidence has one verifier-owned receipt provenance path.
- Failure/incomplete records remain safe and publishable without eligibility.
- Plan 10 receives a stable schema-version 2 compatibility handoff for verdict and stop classification.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-09-SUMMARY.md` when done.
</output>
