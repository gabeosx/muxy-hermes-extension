---
phase: 01-verified-gateway-connectivity
plan: 13
type: execute
wave: 10
depends_on:
  - 01-10
files_modified:
  - scripts/qualify-simulations.mjs
  - fixtures/simulations/docker-compose.yml
  - fixtures/simulations/scenarios.json
  - fixtures/simulations/https-proxy.py
  - test/simulated-relay.test.js
autonomous: true
gap_closure: true
requirements:
  - CONN-04
  - DEPL-01
  - DEPL-04
  - DEPL-05
  - DEPL-06
  - SEC-01
  - SEC-02
  - SEC-04
  - EVID-01
  - EVID-02
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-08, SSH-forward, direct-HTTPS, and remote-workspace exercises execute locally and remain simulation receipts with Unverified verdicts."
    - "The HTTPS/proxy harness exercises default certificate refusal, bearer authentication, CORS response-header variants, and buffered versus unbuffered delivery without changing panel TLS trust."
    - "CORS observations are simulation facts only and cannot become host/Docker positive stages or direct remote-HTTPS support evidence."
    - "SSH interruption/restoration and remote-workspace namespace/no-path cases execute rather than printing declarations."
    - "Temporary certificates, keys, tokens, endpoint identities, workspace sentinels, payloads, and fixture resources are removed and absent from durable evidence."
  artifacts:
    - path: "fixtures/simulations/https-proxy.py"
      provides: "Repository-owned TLS/auth/CORS/buffering proxy using Python standard library"
    - path: "scripts/qualify-simulations.mjs"
      provides: "Bounded HTTPS, SSH-forward, and remote-workspace scenario executor with forced-Unverified publication"
    - path: "fixtures/simulations/docker-compose.yml"
      provides: "Isolated Gateway/model/proxy simulation network"
    - path: "fixtures/simulations/scenarios.json"
      provides: "Executable simulation inputs and expected safe outcomes"
    - path: "test/simulated-relay.test.js"
      provides: "TLS/auth/CORS/buffering/interruption/namespace/redaction/cleanup coverage"
  key_links:
    - from: "fixtures/simulations/docker-compose.yml"
      to: "scripts/qualify-simulations.mjs"
      via: "runner controls proxy certificate/auth/CORS/buffering and interruption scenarios"
      pattern: "https-proxy|direct-remote-https|ssh-local-forward"
    - from: "scripts/qualify-simulations.mjs"
      to: "src/evidence.js"
      via: "all scenario outcomes call only the support-ineligible simulation receipt builder"
      pattern: "simulation_receipt|buildUnverifiedEvidenceRecord"
    - from: "fixtures/simulations/scenarios.json"
      to: "test/simulated-relay.test.js"
      via: "each declared scenario has an executed observation, forced verdict, cleanup, and sentinel assertion"
      pattern: "direct_remote_https|remote_workspace|Unverified"
  prohibitions:
    - requirement_id: "DEPL-05"
      category: "transparency"
      statement: "A local TLS/proxy exercise may not claim direct remote-HTTPS support or add a production certificate bypass."
    - requirement_id: "DEPL-06"
      category: "information-disclosure"
      statement: "Workspace namespace or path identity may not enter a Gateway request, receipt, report, index, or diagnostic."
    - requirement_id: "DEPL-04"
      category: "transparency"
      statement: "A local interruption proxy may not claim real SSH-forwarded support."
---

<objective>
Execute HTTPS/proxy, SSH-forward, and remote-workspace analogues locally while forcing all three remote deployment classes to Unverified.

Purpose: Close the missing simulation behavior without overstating real-path coverage or weakening production TLS/secret boundaries.
Output: Repository-owned proxy, bounded scenario runner, forced-Unverified receipts, CORS-as-simulation evidence, namespace/redaction checks, and cleanup tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md
@.planning/phases/01-verified-gateway-connectivity/01-08-SUMMARY.md
@.planning/phases/01-verified-gateway-connectivity/01-10-SUMMARY.md
</context>

<interfaces>
Use Plan 08's reconciled contract and Plan 09/10 support-ineligible evidence and forced-Unverified projection. Simulation runners may exercise harness-only trust and proxy controls, but the panel remains unchanged and receives no topology or certificate-bypass option.
</interfaces>

## Artifacts this phase produces

- CLI: `node scripts/qualify-simulations.mjs --scenario ssh-local-forward|direct-remote-https|remote-workspace --out public/evidence`.
- HTTPS facts: certificate refusal, harness-only CA trust, authentication, CORS header variants, and first-chunk timing for buffered/unbuffered responses.
- Remote analogue facts: availability/interruption/restoration and namespace/path absence, always marked `realPath: false`, `simulation: true`, `proofSource: simulation_receipt`, and `Unverified`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute one HTTPS proxy path through TLS, auth, CORS simulation, streaming timing, and cleanup</name>
  <precondition>Docker/Compose and the system OpenSSL executable are available to the harness; no host trust-store change is authorized.</precondition>
  <read_first>
    - fixtures/simulations/docker-compose.yml — existing isolated Gateway/model network
    - fixtures/simulations/scenarios.json — direct HTTPS declarations
    - scripts/qualify-simulations.mjs — current declaration-only output
    - src/verdict.js — forced-Unverified rules
    - .planning/phases/01-verified-gateway-connectivity/COVERAGE.md — reconciled CORS simulation-only contract
  </read_first>
  <files>fixtures/simulations/docker-compose.yml, fixtures/simulations/scenarios.json, fixtures/simulations/https-proxy.py, scripts/qualify-simulations.mjs, test/simulated-relay.test.js</files>
  <behavior>
    - "The harness generates per-run CA/server material in a temp mount, proves unchanged panel refusal of untrusted TLS, and removes the material without trust-store mutation."
    - "Inside the controlled network, harness-only CA trust exercises bearer authentication, Access-Control-Allow-Origin variants, and unbuffered/buffered first-chunk timing."
    - "Passing local proxy facts remain a support-ineligible direct-HTTPS simulation receipt and render Unverified."
    - "No panel curl flag, manifest permission, endpoint branch, certificate bypass, or raw proxy output is added."
  </behavior>
  <action>Write failing scenario execution tests, then add a Python standard-library HTTP/TLS reverse proxy to the existing pinned simulation network. Generate per-run CA/server material with OpenSSL into a temp mount. Exercise the unchanged panel's default certificate refusal, then use harness-only CA trust inside the isolated network for authentication, controlled CORS-header variants, and first-chunk-before-completion comparisons under buffered/unbuffered modes. Upgrade the direct-HTTPS runner path to bounded execution, safe receipt projection, forced Unverified classification, and unconditional certificate/secret/container/journal cleanup. Apply D-08, D-14 through D-19, and D-21.</action>
  <verify>
    <automated>docker compose -f fixtures/simulations/docker-compose.yml config --quiet &amp;&amp; node --test test/simulated-relay.test.js test/evidence-provenance.test.js test/verdict.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "TLS refusal, auth, CORS variants, buffering, and cleanup are actual safe observations."
    - "The direct-HTTPS row remains Unverified regardless of passing local facts."
    - "No trust-store mutation, production bypass, secret, certificate, endpoint identity, or raw payload remains."
  </acceptance_criteria>
  <done>The HTTPS/proxy analogue is executable and truthful without becoming a real-path support claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute SSH interruption and remote-workspace namespace simulations under the same forced-Unverified contract</name>
  <read_first>
    - scripts/qualify-simulations.mjs — Task 1 bounded executor and receipt publisher
    - fixtures/simulations/scenarios.json — SSH/workspace declarations
    - fixtures/simulations/docker-compose.yml — Task 1 proxy/Gateway network
    - src/evidence.js — support-ineligible simulation builder
    - .planning/REQUIREMENTS.md — DEPL-04 and DEPL-06 acceptance language
  </read_first>
  <files>scripts/qualify-simulations.mjs, fixtures/simulations/docker-compose.yml, fixtures/simulations/scenarios.json, test/simulated-relay.test.js</files>
  <behavior>
    - "The SSH analogue observes proxy availability, controlled interruption, terminal relay outcome/cleanup, restoration, and a new successful stream."
    - "The remote-workspace analogue uses unique namespace/path sentinels and proves they are absent from Gateway requests, receipts, evidence, diagnostics, and logs."
    - "Both outcomes are support-ineligible simulations and render Unverified."
    - "Every terminal path removes proxy resources, temp roots, sentinels, tokens, challenges, receipts, and journals."
  </behavior>
  <action>Extend the bounded runner to execute the SSH-forward analogue through proxy availability, controlled interruption, relay cleanup, restoration, and a fresh successful stream. Execute the remote-workspace analogue with unique namespace/path sentinels that must never reach Gateway requests or any receipt/evidence/diagnostic projection. Publish both only through the support-ineligible simulation builder and assert `realPath: false`, `simulation: true`, `proofSource: simulation_receipt`, and Unverified. Do not create a tunnel, remote infrastructure, workspace branch, or deployment selector. Apply D-08, D-14, D-15, D-17, D-18, and D-21.</action>
  <verify>
    <automated>docker compose -f fixtures/simulations/docker-compose.yml config --quiet &amp;&amp; node --test test/simulated-relay.test.js test/evidence-provenance.test.js test/verdict.test.js test/phase-boundary.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "SSH interruption/restoration and remote namespace/no-path assertions execute rather than merely render declarations."
    - "Both remote analogue rows remain Unverified in records and safe index projection."
    - "No sentinel, endpoint, secret, raw payload, or fixture resource survives cleanup."
  </acceptance_criteria>
  <done>All deferred remote classes have executable local simulation evidence and remain explicitly Unverified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| proxy/TLS fixture → client observations | Harness trust controls must not weaken production panel policy. |
| local analogue → deployment verdict | Simulated success must never masquerade as a real path. |
| workspace namespace → Gateway/evidence | Local/remote path identities must remain outside requests and durable data. |
| fixture lifecycle → cleanup | Temporary trust, secret, container, and journal material must be removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13-01 | Spoofing | remote support claims | high | mitigate | Hard-code support-ineligible simulation provenance and force all three rows Unverified. |
| T-01-13-02 | Information disclosure | TLS, token, namespace, logs | high | mitigate | Use temp material, sentinel scans, safe receipt projection, and unconditional cleanup. |
| T-01-13-03 | Elevation of privilege | certificate handling | high | mitigate | Keep CA trust harness-only and prohibit panel flags, trust-store mutation, and permission changes. |
| T-01-13-04 | Tampering | CORS/buffering interpretation | medium | mitigate | Record only controlled simulation facts and exclude them from real-path support predicates. |
| T-01-SC | Tampering | package supply chain | high | mitigate | Use Python standard library, system OpenSSL, existing pinned images, and no install task. |
</threat_model>

<verification>
- Compose/proxy tests execute TLS/auth/CORS/buffering plus SSH interruption and workspace namespace scenarios.
- Evidence/verdict tests force every remote analogue to Unverified and reject real-path promotion.
- Cleanup and sentinel tests prove no trust, secret, endpoint, path, or raw payload artifact remains.
</verification>

<success_criteria>
- Required local simulations are executable and secret-safe.
- CORS remains a simulation observation only.
- Real SSH-forward, direct remote HTTPS, and remote-workspace qualification remain deferred and Unverified exactly per D-08.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-13-SUMMARY.md` when done.
</output>
