---
phase: 01-verified-gateway-connectivity
plan: 10
type: execute
wave: 9
depends_on:
  - 01-09
files_modified:
  - src/verdict.js
  - src/stop-gate.js
  - public/evidence/index.json
  - test/verdict.test.js
  - test/stop-gate.test.js
autonomous: true
gap_closure: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
  - DEPL-04
  - DEPL-05
  - DEPL-06
  - EVID-01
  - EVID-02
  - EVID-03
  - EVID-04
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 through D-04, Supported requires two verified schema-version 2 receipt records from distinct fresh actual-Muxy panels on one exact current version tuple and condition."
    - "The positive predicate requires relay grant, authentication, capabilities, 2xx incremental stream, terminal result, and cleanup; it does not require browser Origin or CORS."
    - "Schema-version 1, simulations, ineligible failures, one session, duplicate IDs, mixed tuples, or incomplete cleanup remain Unverified."
    - "Per D-10 and D-11, historical support carries until the latest current pair has a receipt-backed reproducible required-check failure."
    - "Only two distinct current-pair failures with the same relevant safe reason/stage can activate the Muxy-change stop gate; stale, duplicate, mixed, simulated, and unrelated failures cannot."
  artifacts:
    - path: "src/verdict.js"
      provides: "Receipt-backed two-session classifier and safe five-row index projection"
      exports: ["classifyVerdict", "updateEvidenceIndex", "successfulQualification"]
    - path: "src/stop-gate.js"
      provides: "Current-pair reproducible-failure selector"
      exports: ["evaluateStopGate", "reproducibleCurrentPairFailure"]
    - path: "public/evidence/index.json"
      provides: "Schema-version 2 five-row browser-safe evidence index"
    - path: "test/verdict.test.js"
      provides: "Support/unsupported/unverified truth table"
    - path: "test/stop-gate.test.js"
      provides: "Current/stale/duplicate/mixed/simulation/unrelated stop truth table"
  key_links:
    - from: "src/evidence.js"
      to: "src/verdict.js"
      via: "only Plan 09 validated schema-version 2 records enter support classification"
      pattern: "receiptDigest|proofSource|qualificationId"
    - from: "src/verdict.js"
      to: "public/evidence/index.json"
      via: "version-history rules derive the current five-row safe projection"
      pattern: "latestPair|history|verdict"
    - from: "src/verdict.js"
      to: "src/stop-gate.js"
      via: "current resolved pair plus distinct receipt identities and matching failure stage/reason gate stopping"
      pattern: "latestPair|panelSessionIdHash|failedStage"
  prohibitions:
    - requirement_id: "EVID-02"
      category: "transparency"
      statement: "A simulation, origin-era record, incomplete receipt, or caller-ineligible record may not render Supported or Unsupported."
    - requirement_id: "EVID-04"
      category: "scope-integrity"
      statement: "A stale, duplicate, simulated, unrelated, or non-current-pair failure may not activate a stop or authorize an upstream change."
---

<objective>
Classify schema-version 2 curl-relay evidence and restrict stop activation to a reproducible failure on the current resolved pair.

Purpose: Complete the compatibility handoff from safe evidence to truthful support and stop outcomes before qualification runners or native UI consume it.
Output: Receipt-backed classifier, five-row index projection, current-pair stop selector, and exhaustive verdict/stop truth tables.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md
@.planning/phases/01-verified-gateway-connectivity/01-09-SUMMARY.md
</context>

<interfaces>
Consume only Plan 09's validated evidence record interface. `proofSource: verifier_receipt` plus receipt correlation makes a record eligible for real-path classification; `simulation_receipt`, schema-version 1, and the generic failure adapter are never positive. Plan 11/12 runners publish through this contract, and Plan 14 renders the stop result without reimplementing selection.
</interfaces>

## Artifacts this phase produces

- Symbols: `successfulQualification`, `classifyVerdict`, `updateEvidenceIndex`, `reproducibleCurrentPairFailure`, and `evaluateStopGate`.
- Index fields: current exact pair, historical record references, safe current outcome, safe failure stage/reason, and forced-Unverified remote projections.
- Handoff: Plan 14 receives an already-classified safe stop state; Plan 15 revalidates it but does not define a second classifier.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one valid two-session receipt pair through the classifier into the safe index</name>
  <read_first>
    - src/verdict.js — origin-era qualification predicate and pair grouping
    - public/evidence/index.json — current five-row browser-safe index
    - src/evidence.js — Plan 09 validated evidence interface
    - test/verdict.test.js — existing version carry-forward and simulation cases
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-01 through D-04 and D-09 through D-16
  </read_first>
  <files>src/verdict.js, public/evidence/index.json, test/verdict.test.js</files>
  <behavior>
    - "Supported requires ordinals 1 and 2, distinct panel and qualification receipts, identical current tuple/condition, verified provenance, passed relay stages, 2xx stream, incremental non-tool frames, terminal outcome, and cleanup."
    - "Schema-version 1, simulation, ineligible failure mode, one session, duplicates, mixed tuple, missing cleanup, or any unverified stage remains Unverified."
    - "A reproducible required-check failure on the latest pair becomes Unsupported; older support remains retained history."
    - "SSH, direct HTTPS, and remote workspace rows are forced Unverified in the browser-safe projection."
  </behavior>
  <action>Write the full support/unverified/unsupported table first. Replace the origin-era predicate with Plan 09's verified receipt eligibility plus explicit relay, authentication, capabilities, incremental stream, terminal, and cleanup checks. Preserve D-01/D-02/D-03/D-09/D-10/D-11 history semantics and exact resolved tuples. Update the index contract to schema version 2 while retaining append-only history references and forcing the three remote simulations to Unverified. Treat the paired direct-WebKit result as transport history only. Apply D-01 through D-04 and D-09 through D-16.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/verdict.test.js test/evidence-provenance.test.js test/evidence.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Only the exact two-session verified curl-relay pair reaches Supported."
    - "Every simulation, stale schema, duplicate, incomplete, mixed, or caller-ineligible case remains Unverified."
    - "Latest reproducible failure takes precedence without deleting prior support history."
  </acceptance_criteria>
  <done>The five-row index derives truthful current and historical outcomes from the approved relay receipts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restrict the Muxy-change stop gate to a reproducible current-pair failure</name>
  <read_first>
    - src/stop-gate.js — history-counting stop rule
    - src/verdict.js — Task 1 latest-pair and receipt identity projection
    - test/stop-gate.test.js — existing failure-count cases
    - .planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md — stale/duplicate historical failure warning
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-02, D-11, D-16, and D-21
  </read_first>
  <files>src/stop-gate.js, test/stop-gate.test.js</files>
  <behavior>
    - "Two distinct verified receipt identities and ordinals on the exact current pair with the same relevant relay/stream safety stage and safe reason activate the stop."
    - "A single failure, duplicate panel, stale pair, mixed tuple, simulation, historical browser-origin result, or unrelated stage/reason cannot activate it."
    - "The explicit requires-Muxy-change signal remains a separate fail-closed input and does not turn arbitrary failures into reproducible evidence."
    - "The stop output contains only allowlisted report/bridge-contract fields and never authorizes mutation."
  </behavior>
  <action>Add current/stale/duplicate/mixed/simulation/schema-history/unrelated truth-table cases. Implement `reproducibleCurrentPairFailure` over the index's resolved latest pair, require two distinct verified receipt identities and ordinals with one matching relevant safe reason/stage, and use it from `evaluateStopGate`. Keep the explicit `requiresMuxyChange` input separate, preserve redacted report and not-implemented bridge-contract projection, and leave all upstream action prohibited per D-21.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/stop-gate.test.js test/verdict.test.js test/evidence-provenance.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Only two current-pair receipt-backed matching failures activate the stop."
    - "Stale, duplicate, mixed, simulated, origin-era, unrelated, and single failures remain inactive."
    - "The active stop exposes no mutation, installation, registration, daemon, provider, or permission-expansion action."
  </acceptance_criteria>
  <done>The stop gate blocks only on reproducible current safety evidence and cannot be triggered by unrelated history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| validated evidence → verdict | Eligibility and pair identity determine support claims. |
| current/history index → stop gate | Old or unrelated records must not block current work. |
| simulation/history → browser projection | Non-real records must remain visibly Unverified. |
| dependency graph → build | Classification uses existing project code only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10-01 | Spoofing | Supported predicate | high | mitigate | Require validated receipt provenance, exact pair, distinct ordinals/identities, and all relay stages. |
| T-01-10-02 | Tampering / Repudiation | version history and latest pair | high | mitigate | Preserve append-only history and deterministically derive the latest resolved pair. |
| T-01-10-03 | Elevation of privilege | stop selection | high | mitigate | Require two matching current-pair failures and keep explicit change-required input separate. |
| T-01-10-04 | Information disclosure | safe index/stop output | high | mitigate | Use only Plan 09 allowlisted fields and test copied report/contract projections. |
| T-01-SC | Tampering | package supply chain | high | mitigate | Add no dependency/install task and retain the existing lockfile. |
</threat_model>

<verification>
- Verdict tests prove exact two-session relay eligibility, history carry-forward, current regression precedence, and forced remote Unverified outcomes.
- Stop tests prove current-pair identity/stage/reason matching and reject every stale/duplicate/mixed/simulated/unrelated alternative.
- Browser-origin history is not a positive stage or independent stop trigger.
</verification>

<success_criteria>
- Support classification exactly matches the verifier-owned relay receipt contract.
- Stop classification exactly matches the latest reproducible relevant failure contract.
- Plan 11-15 can consume stable index and stop interfaces without reinterpreting provenance.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-10-SUMMARY.md` when done.
</output>
