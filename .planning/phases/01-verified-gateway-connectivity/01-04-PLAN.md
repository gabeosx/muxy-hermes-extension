---
phase: 01-verified-gateway-connectivity
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - scripts/resolve-versions.mjs
  - scripts/qualify-real.mjs
  - fixtures/host-native/fixture.json
  - fixtures/host-native/README.md
  - fixtures/docker-compose.yml
  - fixtures/docker/hermes.env.example
  - fixtures/docker/README.md
  - test/host-fixture.test.ts
  - test/docker-fixture.test.ts
autonomous: true
requirements:
  - CONN-02
  - CONN-05
  - DEPL-02
  - DEPL-03
  - SEC-02
  - SEC-04
  - EVID-01
  - EVID-02
user_setup:
  - service: muxy
    why: "Both support-eligible lanes require the actual current stable Muxy panel and fresh panel sessions."
    dashboard_config:
      - task: "Install/open the current stable Muxy app and enable the built extension from dist."
        location: "Muxy → Extensions"
  - service: hermes
    why: "The host-native lane requires the current stable Hermes executable and the Docker lane requires a resolved release revision/image."
    env_vars:
      - name: API_SERVER_KEY
        source: "Operator-managed development fixture secret; never print or commit it"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-06, repository validation tooling may launch/stop the resolved host-native Hermes fixture, but the extension bundle and manifest contain no process authority or harness import."
    - "Per D-07, the repository owns a Docker Compose Hermes fixture pinned to the resolved release revision/image digest and publishes only a loopback host port; the runner starts and tears it down."
    - "Per D-09, each qualification resolves the latest stable Muxy and Hermes releases immediately before execution and records exact installed Muxy version, Hermes version/revision or image digest, and UTC date."
    - "Per D-03 and D-04, host-native and Docker can become Supported only after two complete fresh Muxy panel sessions on the same resolved pair each prove exact-origin bearer auth, capabilities, and a real incremental Hermes SSE stream."
    - "Per D-01/D-02, one pass, partial execution, missing Muxy/Hermes, or inconclusive origin/stream evidence remains Unverified; a reproducible required-check failure becomes Unsupported."
    - "Docker refusal and interrupted-stream observations are recorded without the extension inspecting, starting, stopping, or identifying Docker."
  artifacts:
    - path: "scripts/resolve-versions.mjs"
      provides: "Current stable release resolution and installed/runtime identity comparison"
    - path: "scripts/qualify-real.mjs"
      provides: "Harness-only origin capture, fixture lifecycle, two-session checklist, safe observation collection, and evidence emission"
    - path: "fixtures/host-native/fixture.json"
      provides: "Host-native process recipe and required-check declaration"
    - path: "fixtures/docker-compose.yml"
      provides: "Loopback-only resolved Hermes container fixture"
    - path: "test/host-fixture.test.ts"
      provides: "Host lifecycle, origin, redaction, and two-session contract tests"
    - path: "test/docker-fixture.test.ts"
      provides: "Compose bind/digest, refusal/interruption, and extension-authority isolation tests"
  key_links:
    - from: "scripts/resolve-versions.mjs"
      to: "scripts/qualify-real.mjs"
      via: "qualification refuses support classification without an exact resolved/installed version tuple"
      pattern: "muxyVersion|hermesVersion|revision|digest"
    - from: "scripts/qualify-real.mjs"
      to: "scripts/run-validation.mjs"
      via: "ephemeral raw observations are handed to Plan 03's sanitizer/writer, never written directly"
      pattern: "run-validation|buildEvidenceRecord"
    - from: "fixtures/docker-compose.yml"
      to: "scripts/qualify-real.mjs"
      via: "runner owns compose up/down and supplies only the loopback URL to the unchanged panel"
      pattern: "docker compose|127\.0\.0\.1"
  edge_cases:
    - id: "DEPL-02-unclassified"
      status: resolved
      statement: "Host-native qualification requires exact installed/resolved versions, stable exact Origin, every required stage, and two separately launched fresh panel sessions with distinct evidence run IDs."
    - id: "DEPL-03-unclassified"
      status: resolved
      statement: "Docker qualification uses a loopback-only published port and the same panel code; it additionally records refused-port and mid-stream container interruption as observed failures without Docker inspection from the extension."
  prohibitions:
    - requirement_id: "DEPL-02"
      category: "evidence-integrity"
      status: "unverified"
      flagged: true
      statement: "MUST NOT combine retries within one panel lifetime or observations from different version pairs into the two fresh sessions required for Supported."
    - requirement_id: "DEPL-03"
      category: "product-integrity"
      status: "unverified"
      flagged: true
      statement: "MUST NOT expose Docker lifecycle, image, port, or deployment controls in the extension panel; those remain harness-only."
---

## Phase Goal

**As a** Hermes Gateway user, **I want to** qualify host-native and Docker loopback through the actual Muxy panel twice, **so that** Supported means the direct path truly worked on the resolved stable pair.

<objective>
Build and run the two real qualification lanes that are authorized to establish Phase 1 support.

Purpose: Apply D-01 through D-07 and D-09 to real Muxy/WebKit paths while strictly separating fixture authority from the extension.
Output: Stable-version resolver, host-native harness, loopback-only Compose fixture, two-session qualification workflow, negative-path observations, and redacted evidence.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-RESEARCH.md
@.planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md
@.planning/phases/01-verified-gateway-connectivity/01-02-SUMMARY.md
@.planning/phases/01-verified-gateway-connectivity/01-03-SUMMARY.md
</context>

<interfaces>
Consume Plan 02's `ConnectionProbe` safe snapshots and Plan 03's `EvidenceRecord`, `classifyVerdict`, and validation CLI. Fixture scripts may hold raw observations only in memory or OS temporary paths and must submit them through the Plan 03 sanitizer before repository writes.
</interfaces>

## Artifacts this phase produces

- Paths: `scripts/resolve-versions.mjs`, `scripts/qualify-real.mjs`, `fixtures/host-native/fixture.json`, `fixtures/host-native/README.md`, `fixtures/docker-compose.yml`, `fixtures/docker/hermes.env.example`, `fixtures/docker/README.md`, `test/host-fixture.test.ts`, `test/docker-fixture.test.ts`, and dynamic redacted host/Docker report pairs under `public/evidence/runs/`.
- Symbols: `resolveLatestStable`, `readInstalledMuxyVersion`, `resolveHermesRevision`, `resolveImageDigest`, `capturePanelOrigin`, `runHostFixture`, `runDockerFixture`, `recordFreshSession`, and `qualifyRealDeployment`.
- Commands: `node scripts/resolve-versions.mjs`, `node scripts/qualify-real.mjs --fixture host-native`, `node scripts/qualify-real.mjs --fixture docker-loopback`, `docker compose -f fixtures/docker-compose.yml config`, and the focused Node tests.
- Fields: `resolvedAt`, `muxyRelease`, `muxyInstalledVersion`, `hermesRelease`, `hermesRevision`, `hermesImageDigest`, `originClass`, `preflightObserved`, `sseCorsObserved`, `firstChunkMs`, `sessionOrdinal`, `freshPanelSession`, `refusalObserved`, and `interruptionObserved`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Qualify the latest stable host-native Gateway through two fresh Muxy panels</name>
  <precondition>The current stable Muxy app and current stable host-native Hermes executable are installed, API_SERVER_KEY is available without printing it, and the operator can open/close Muxy panel sessions for the human-check steps.</precondition>
  <read_first>
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-01 through D-06 and D-09
    - .planning/phases/01-verified-gateway-connectivity/01-RESEARCH.md — open questions, environment blockers, and real-panel gate
    - scripts/run-validation.mjs — Plan 03 redacted evidence boundary
    - public/evidence/schema-v1.json — safe observation target
    - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md — exact origin/stream verdict presentation
  </read_first>
  <files>scripts/resolve-versions.mjs, scripts/qualify-real.mjs, fixtures/host-native/fixture.json, fixtures/host-native/README.md, test/host-fixture.test.ts</files>
  <behavior>
    - "Resolver queries current official stable releases at execution time, validates installed identities, and emits exact versions/revision/date without secrets."
    - "Harness may start/stop only its declared host-native fixture process and an origin-capture preflight server; no process command or credential crosses into dist/source panel code."
    - "The first capture pass observes the real panel Origin, the harness configures that exact value for Hermes, and the qualification pass verifies preflight plus SSE route CORS without accepting null, wildcard, reflection, or instability."
    - "Two session records require closing and recreating the panel between complete successes and share the exact resolved tuple; otherwise the classifier returns Unverified."
  </behavior>
  <action>Write resolver/lifecycle/two-session tests first. Implement current-stable resolution against official Muxy and Hermes release metadata, record immutable tag/revision/date, and compare it with the installed Muxy bundle and Hermes executable before running; missing or mismatched dependencies emit an Unverified safe record and stop qualification rather than substituting a browser or older binary. Define the host fixture recipe with bounded startup/readiness/teardown and a loopback bind. In `qualify-real.mjs`, keep process authority behind a fixture allowlist, start an origin-capture server to observe the actual panel preflight Origin, require a stable non-null value, restart/configure the harness-owned Hermes process with only that exact origin, then guide/record two complete fresh panel sessions through the same panel URL/token flow. Collect bearer-auth, capability, preflight, SSE response CORS, first-chunk timing, incremental frames, and safe failure facts in memory; pipe them to Plan 03's validation command. Do not emit the bearer, origin text, endpoint identity, raw logs, or event content. Implement D-06 without adding any corresponding extension API.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/host-fixture.test.ts test/evidence.test.ts test/verdict.test.ts</automated>
    <human-check>Run `node scripts/qualify-real.mjs --fixture host-native`; complete session 1, close the panel, open a fresh panel for session 2, then confirm the generated safe report pair either establishes Supported with every required check or truthfully records Unsupported/Unverified and why.</human-check>
  </verify>
  <acceptance_criteria>
    - "The runner records exact current stable/installed identities and refuses to establish support from missing, mismatched, or stale execution inputs."
    - "Two distinct fresh real Muxy panel records prove the same exact-origin authenticated capability and incremental SSE path before host-native becomes Supported."
    - "All process/origin capture authority remains in the harness and every durable output passes the strict evidence sanitizer."
  </acceptance_criteria>
  <done>The host-native row has a reproducible redacted verdict backed by two real-path Muxy sessions or a precise safe failure/incomplete record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify a loopback-published Docker Gateway and its refusal/interruption paths</name>
  <precondition>The current stable Muxy app is installed, Docker/Compose are available, API_SERVER_KEY is available without printing it, and the resolved Hermes release can be built or pulled by digest.</precondition>
  <read_first>
    - fixtures/host-native/fixture.json — Task 1 required-check vocabulary
    - scripts/qualify-real.mjs — Task 1 origin/two-session/evidence workflow
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-01 through D-04, D-07, and D-09
    - .planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md — DEPL-03 refusal/interruption requirements
  </read_first>
  <files>fixtures/docker-compose.yml, fixtures/docker/hermes.env.example, fixtures/docker/README.md, scripts/qualify-real.mjs, test/docker-fixture.test.ts</files>
  <behavior>
    - "Compose binds the resolved Hermes service only to 127.0.0.1, records the immutable revision/image digest, receives key/origin via runtime environment, and leaves no secret in configuration or evidence."
    - "The identical dist panel and GatewayClient path qualifies Docker in two fresh sessions without a deployment selector or extension-side Docker call."
    - "An unreachable published port is recorded as browser request/refusal only when controlled harness facts establish refusal; it cannot be mistaken for auth/CORS."
    - "A harness-triggered mid-stream container stop produces an interrupted-stream observation and complete cleanup without exposing Docker controls to the panel."
  </behavior>
  <action>Write Compose-policy, two-session, refused-port, interrupted-stream, redaction, and authority-isolation tests first. Create a Compose fixture that resolves/builds Hermes at the exact current stable revision when no official immutable image is available, pins the final image digest in the evidence observation, binds the published port explicitly to loopback, and injects the key plus exact captured Origin only at runtime. Extend `qualify-real.mjs` with a Docker fixture adapter behind the same safe observation interface; the adapter owns `docker compose up/down`, readiness, intentional unreachable-port exercise, and one controlled mid-stream stop, while the panel receives only a URL and token and uses the unchanged direct client. Require two complete fresh-panel successes for the same Muxy/Hermes/digest tuple before Supported. Always tear down the named Compose project, including failed attempts, and send all results through the Plan 03 sanitizer.</action>
  <verify>
    <automated>docker compose -f fixtures/docker-compose.yml config &amp;&amp; npm run build &amp;&amp; node --test test/docker-fixture.test.ts test/evidence.test.ts test/verdict.test.ts</automated>
    <human-check>Run `node scripts/qualify-real.mjs --fixture docker-loopback`; complete two fresh panel sessions, then perform the runner's unreachable-port and interrupted-stream cases and inspect the redacted report/index outcomes.</human-check>
  </verify>
  <acceptance_criteria>
    - "The Docker service is loopback-only, immutable by resolved revision/digest, secret-free on disk, and always torn down by the harness."
    - "Docker Supported requires the same two-session exact-origin/auth/capability/incremental-stream proof as host-native."
    - "Refusal and interruption are reproducible observed failures, not topology inference, and panel code contains no Docker authority."
  </acceptance_criteria>
  <done>The Docker-published row has a real Muxy qualification verdict plus safe refusal/interruption evidence through the same client contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture runner → host process/Docker | Repository tooling gains narrowly scoped lifecycle authority that must never reach the extension. |
| real Muxy panel → exact-origin Gateway | Bearer and Origin cross the actual WebKit/CORS boundary. |
| release metadata/image → executed fixture | Mutable tags or mismatched installed versions could falsify evidence. |
| raw logs/frames → evidence writer | Process and container output may contain endpoints, keys, paths, or content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information disclosure | fixture env, runner output, panel input | high | mitigate | Pass API key through runtime environment only, never print it, and run all observations through the sentinel-tested sanitizer. |
| T-01-02 | Elevation of privilege | origin capture and Hermes CORS | high | mitigate | Observe real panel Origin, require stable non-null exact equality on preflight and SSE, and reject wildcard/reflection. |
| T-01-03 | Tampering / Information disclosure | Docker/host transport | high | mitigate | Loopback-only bind for local classes; no certificate bypass concept; non-loopback remains subject to trusted HTTPS in shared client. |
| T-01-04 | Elevation of privilege | process/Compose adapter | high | mitigate | Fixture allowlist and static dist/source tests prove lifecycle code is scripts-only and absent from manifest/panel imports. |
| T-01-05 | Information disclosure | process logs, origin, SSE frames, reports | high | mitigate | Raw values remain memory/temp-only; evidence stores trust class/booleans/metadata/hashes, not origin text, endpoint identity, headers, or payload. |
</threat_model>

<verification>
- Focused Node tests and `docker compose ... config` pass without watch mode.
- Both real lanes are exercised in actual Muxy twice from fresh panels before Supported is possible.
- Source/dist authority checks remain green after harness additions.
- Every attempt produces schema-valid paired evidence or an explicit safe harness failure before a support claim.
</verification>

<success_criteria>
- Host-native and Docker loopback each have reproducible real-path evidence under the current resolved stable pair.
- Exact-origin and incremental SSE are proven route-by-route, not inferred.
- Harness lifecycle power remains structurally absent from the extension.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-04-SUMMARY.md` when done.
</output>
