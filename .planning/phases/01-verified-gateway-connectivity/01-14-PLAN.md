---
phase: 01-verified-gateway-connectivity
plan: 14
type: execute
wave: 10
depends_on:
  - 01-10
files_modified:
  - src/stop-gate.js
  - src/panel/app.js
  - scripts/stage-native-verification.mjs
  - test/stop-gate.test.js
  - test/ui-contract.test.js
autonomous: true
gap_closure: true
requirements:
  - CONN-01
  - CONN-03
  - DEPL-01
  - DEPL-04
  - DEPL-05
  - DEPL-06
  - SEC-01
  - EVID-02
  - EVID-03
  - EVID-04
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A current-pair two-session receipt-backed relay/stream safety failure activates the non-dismissible stop alert; stale, duplicate, mixed, simulated, unrelated, and single failures do not."
    - "The alert receives one-time keyboard focus, remains visible, and exposes independent safe copy/view loading, error, retry, overflow, and long-text states."
    - "All five evidence rows remain visible and keyboard reachable; SSH, direct HTTPS, and remote workspace remain Unverified."
    - "Temporary native-verification builds can exercise populated, evidence-unavailable, and current-pair-stop states without mutating canonical evidence or becoming qualification evidence."
    - "Native theme tokens, scale, focus/hover, vertical wrapping, narrow layout, and reduced-motion behavior remain consistent with the reconciled UI contract."
  artifacts:
    - path: "src/stop-gate.js"
      provides: "Safe browser projection of Plan 10 current-pair stop state"
    - path: "src/panel/app.js"
      provides: "Non-dismissible accessible stop alert and five-row evidence matrix"
    - path: "scripts/stage-native-verification.mjs"
      provides: "Temporary loadable built copies for safe native UI scenarios"
    - path: "test/stop-gate.test.js"
      provides: "Stop-to-panel projection regression coverage"
    - path: "test/ui-contract.test.js"
      provides: "Alert, matrix, action-state, focus, wrapping, and simulation-verdict DOM coverage"
  key_links:
    - from: "public/evidence/index.json"
      to: "src/stop-gate.js"
      via: "schema-version 2 index supplies the exact current-pair receipt failure state"
      pattern: "latestPair|qualificationId|failedStage"
    - from: "src/stop-gate.js"
      to: "src/panel/app.js"
      via: "active safe stop state renders and focuses the non-dismissible alert"
      pattern: "evaluateStopGate|transportStopShell|transport-stop-title"
    - from: "scripts/stage-native-verification.mjs"
      to: "dist/evidence/index.json"
      via: "temporary copied build receives safe UI-only scenario data outside canonical source/dist"
      pattern: "populated|evidence-unavailable|stop-current-pair"
  prohibitions:
    - requirement_id: "EVID-04"
      category: "scope-integrity"
      statement: "The stop alert may not offer, perform, or imply a source change, bridge installation, provider registration, daemon, permission expansion, or automatic continuation."
    - requirement_id: "SEC-01"
      category: "information-disclosure"
      statement: "DOM, clipboard, errors, staged builds, and copied contract/report data may not expose bearer, endpoint, content, raw transport, workspace, or audit data."
---

<objective>
Drive the receipt-backed current-pair stop state into the native panel and create safe temporary builds for actual-Muxy UI verification.

Purpose: Prove the repaired evidence/stop contracts are rendered accessibly without contaminating qualification history or expanding authority.
Output: Stop UI tracer, complete interaction states, five-row matrix safeguards, native verification staging, and focused DOM/static tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/muxy-extension/SKILL.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md
@.planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md
@.planning/phases/01-verified-gateway-connectivity/01-10-SUMMARY.md
</context>

<interfaces>
Consume Plan 10's current-pair selector and safe index projection. The panel renders that result without reclassifying records. Staged UI fixtures are schema-valid copies marked support-ineligible and are written only under a caller-supplied temporary directory.
</interfaces>

## Artifacts this phase produces

- Symbols: `renderTransportStopAlert`, safe matrix loader/projector, and `stageNativeVerification`.
- CLI: `node scripts/stage-native-verification.mjs --scenario populated|evidence-unavailable|stop-current-pair --out <temporary-directory>`.
- Native scenarios: copied publish-valid build plus safe UI-only index for matrix, unavailable/retry, and stop behavior.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive one current-pair receipt-backed failure from the safe index into the native stop alert</name>
  <read_first>
    - src/stop-gate.js — Plan 10 current-pair selector and safe report/contract projection
    - src/panel/app.js — evidence loader, matrix, focus, copy, and bridge-contract actions
    - public/evidence/index.json — schema-version 2 canonical safe shape
    - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md — reconciled evidence/alert interaction contract
    - .planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md — behavior-unverified matrix/alert item
  </read_first>
  <files>src/stop-gate.js, src/panel/app.js, test/stop-gate.test.js, test/ui-contract.test.js</files>
  <behavior>
    - "Two distinct current-pair matching receipt failures activate the alert; every stale, duplicate, mixed, simulated, unrelated, and single alternative is rejected."
    - "The alert is assertive, non-dismissible, focus-targeted once, vertically wrapped, and retains independent copy/view pending/error/retry states."
    - "Copied/viewed data is allowlisted and includes the not-implemented boundary without mutation controls."
    - "All five matrix rows remain visible and keyboard reachable while remote simulations stay Unverified."
  </behavior>
  <action>Write the stop-to-DOM truth table and interaction assertions first. Consume Plan 10's safe selector directly and update the panel only as needed to guarantee non-dismissal, assertive announcement, one-time focus transition, independent action states, safe clipboard/contract projection, keyboard reachability, and vertical wrapping. Preserve the canonical five-row matrix, relay-first connection copy from Plan 08, theme tokens, sizing scale, reduced-motion rules, and no-upstream-action contract. Apply D-02, D-08, D-11, D-14, D-15, D-16, and D-21.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/stop-gate.test.js test/ui-contract.test.js test/verdict.test.js test/evidence-provenance.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The focused suite proves exact stop activation/rejection plus focus, copy, contract, retry, wrapping, and remote-Unverified behavior."
    - "No browser-origin result is rendered as a positive connection or support stage."
    - "No secret, raw content, endpoint identity, workspace path, or mutation action reaches the DOM or clipboard."
  </acceptance_criteria>
  <done>A real current-pair safety failure follows one tested path from verifier receipts to a redacted native stop alert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage safe native-verification builds without touching canonical evidence</name>
  <read_first>
    - scripts/validate-dist.mjs — publish-valid dist structure and exclusions
    - public/evidence/index.json — canonical safe index contract
    - src/panel/app.js — Task 1 matrix/stop UI states
    - test/ui-contract.test.js — Task 1 DOM assertions and scenario fixtures
    - .agents/skills/muxy-extension/SKILL.md — Load Unpacked/build, theme, sizing, focus, and reduced-motion behavior
  </read_first>
  <files>scripts/stage-native-verification.mjs, test/ui-contract.test.js</files>
  <behavior>
    - "The command accepts only populated, evidence-unavailable, and stop-current-pair scenarios plus an explicit temporary output directory."
    - "It copies a clean built extension and changes only the copy's local evidence index."
    - "Every staged record is safe, schema-valid, tagged UI-only/support-ineligible, and preserves remote Unverified rows."
    - "Repository root, public/evidence, canonical dist, source, and paths inside them are rejected targets."
  </behavior>
  <action>Add failing target-safety and scenario-shape tests. Implement a staging command that resolves and validates a caller-supplied output path outside repository/public/dist/source roots, copies the built extension, injects one safe support-ineligible UI scenario into only the copy, and verifies the copied manifest/index. Tag all staged records as native UI fixtures that cannot enter the qualification classifier. Preserve canonical evidence byte-for-byte and include cleanup instructions/output without deleting user-owned directories.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/ui-contract.test.js test/stop-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Each scenario produces a loadable safe copied build in a temporary target."
    - "Canonical source, public evidence, and dist remain unchanged; protected targets are rejected nonzero."
    - "Staged records cannot become qualification evidence or change remote verdicts."
  </acceptance_criteria>
  <done>Actual-Muxy UI states can be inspected safely without contaminating real evidence or shipped artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| safe index → DOM/clipboard | Remote-derived allowlisted values become visible and copyable. |
| current/history → alert | Pair/provenance selection determines whether work is paused. |
| staged fixture → canonical evidence | Human verification data must not contaminate real qualification. |
| copied build → Muxy | Native UI scenarios must remain publish-valid and support-ineligible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14-01 | Tampering / Spoofing | stop-to-UI projection | high | mitigate | Consume Plan 10 selector, test exact receipt identity, and reject stale/duplicate/mixed/simulated alternatives. |
| T-01-14-02 | Information disclosure | DOM/clipboard/actions | high | mitigate | Render/copy allowlisted safe data and run secret/content/path sentinels. |
| T-01-14-03 | Elevation of privilege | stop alert | high | mitigate | Keep actions read-only and prohibit install/register/change/continue authority. |
| T-01-14-04 | Repudiation | staged fixtures | medium | mitigate | Tag UI-only records, reject canonical targets, and prove canonical evidence unchanged. |
| T-01-14-05 | Denial of service | focus/action states | medium | mitigate | Test one-time focus, retryable independent states, wrapping, keyboard access, and reduced motion. |
</threat_model>

<verification>
- Stop/UI suites prove exact activation, safe actions, accessibility states, matrix visibility, and remote Unverified rendering.
- Staging tests prove target safety, schema validity, support ineligibility, and canonical immutability.
- Temporary builds remain loadable through the standard Muxy Load Unpacked path.
</verification>

<success_criteria>
- Receipt-backed current-pair stops are visible, accessible, redacted, and non-mutable.
- All evidence/UI states can be inspected in temporary copied builds.
- Canonical qualification evidence and production authority remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-14-SUMMARY.md` when done.
</output>
