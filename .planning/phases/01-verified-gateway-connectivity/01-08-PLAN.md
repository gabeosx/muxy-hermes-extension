---
phase: 01-verified-gateway-connectivity
plan: 08
type: execute
wave: 7
depends_on:
  - 01-07
files_modified:
  - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md
  - .planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md
  - .planning/phases/01-verified-gateway-connectivity/COVERAGE.md
  - test/contract-reconciliation.test.js
  - src/curl-relay.js
  - src/gateway-client.js
  - src/probe.js
  - src/panel/app.js
  - test/qualification-receipt.test.js
autonomous: true
gap_closure: true
requirements:
  - CONN-02
  - CONN-04
  - CONN-05
  - DEPL-01
  - SEC-01
  - SEC-02
  - EVID-01
  - EVID-02
  - EVID-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-16 through D-21, every authoritative Phase 1 contract names the explicitly consented curl relay as the positive path and preserves direct WebKit as historical negative evidence."
    - "CORS and Origin observations are simulation-only where applicable; actual host/Docker diagnostics and positive qualification do not require or claim browser-origin observation."
    - "01-VALIDATION.md maps Plans 07 through 15 to their real JavaScript paths, automated commands, execution waves, and actual-Muxy checks before downstream receipt classification or final validation consumes it."
    - "Per D-01 through D-05 and D-17 through D-19, one verifier challenge can traverse an actual panel, relay, incremental stream, and awaited cleanup into one safe panel-session receipt."
    - "A receipt contains no bearer, endpoint identity, topology label, raw transport content, workspace path, journal content, or Muxy audit text."
  artifacts:
    - path: ".planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md"
      provides: "Approved curl-relay positive-path copy, states, diagnostics, evidence, and historical direct-WebKit stop semantics"
    - path: ".planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md"
      provides: "Plans 07-15 JavaScript verification map and relay-native manual gates"
    - path: ".planning/phases/01-verified-gateway-connectivity/COVERAGE.md"
      provides: "Curl-relay API coverage with CORS/Origin limited to simulations and historical negative evidence"
    - path: "src/probe.js"
      provides: "Fresh panel-instance receipt bound to an opaque verifier challenge"
    - path: "test/qualification-receipt.test.js"
      provides: "Full-chain safe receipt, replay, mismatch, cleanup, and redaction coverage"
  key_links:
    - from: ".planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md"
      to: ".planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md"
      via: "D-16 through D-21 replace positive browser-origin language with the consented relay contract while retaining the recorded negative result"
      pattern: "curl relay|historical negative|simulation-only"
    - from: ".planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md"
      to: "test/contract-reconciliation.test.js"
      via: "a deterministic contract test proves plan IDs, JavaScript paths, commands, and relay-stage language are current"
      pattern: "01-07|01-15|\.test\.js"
    - from: "src/curl-relay.js"
      to: "src/probe.js"
      via: "GatewayClient reduces safe execution, HTTP, incremental-frame, and cleanup observations into the challenged panel receipt"
      pattern: "executionId|httpStatus|journalOutcome|qualification"
  prohibitions:
    - requirement_id: "SEC-02"
      category: "transparency"
      statement: "The positive curl-relay path may not claim that it observes or proves a browser Origin, preflight, or CORS allowlist."
    - requirement_id: "EVID-02"
      category: "evidence-integrity"
      statement: "A stale browser-origin contract or caller-authored challenge result may not become support-eligible evidence."
---

<objective>
Reconcile the authoritative Phase 1 contracts to the approved curl-relay architecture, then carry one real relay session into a safe verifier-bound receipt.

Purpose: Remove the unsatisfiable origin-era contract before any classifier or final validator consumes it, and establish the provenance handoff used by Plans 09-15.
Output: Updated UI, validation, and API-coverage contracts; a deterministic reconciliation test; and one-use relay/panel receipt capture with redaction tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/muxy-extension/SKILL.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md
@.planning/phases/01-verified-gateway-connectivity/01-VERIFICATION.md
@.planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md
@.planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md
@.planning/phases/01-verified-gateway-connectivity/COVERAGE.md
@.planning/phases/01-verified-gateway-connectivity/01-07-SUMMARY.md
</context>

<interfaces>
Plan 07 supplies awaited `CurlRelay.streamJournal()` execution/status/cleanup results. The reconciled positive-stage vocabulary is `url`, `relay`, `authentication`, `capabilities`, `stream`, and `cleanup`. Browser Origin, preflight, and CORS are not positive stages; CORS response variants remain eligible only as local proxy simulation observations, and the direct-WebKit result remains paired historical negative evidence.
</interfaces>

## Artifacts this phase produces

- Contract artifacts: reconciled `01-UI-SPEC.md`, `01-VALIDATION.md`, and `COVERAGE.md` plus `test/contract-reconciliation.test.js`.
- Receipt artifacts: ephemeral `.muxy-hermes-qualification/current/challenge.json` and `panel-session.json`; safe receipt builders in the existing panel/probe/client/relay chain.
- Handoff: Plan 09 receives only validated safe receipt objects and the reconciled stage vocabulary; it does not reinterpret raw subprocess or browser-origin observations.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reconcile every authoritative Phase 1 contract to the locked relay architecture</name>
  <read_first>
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-16 through D-21 and deferred real remote qualification
    - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md — stale origin/CORS verdict fields and copy
    - .planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md — stale Plans 01-01 through 01-06 TypeScript map and browser-origin manual gates
    - .planning/phases/01-verified-gateway-connectivity/COVERAGE.md — stale direct-WebKit, preflight, and explicit-browser-CORS positive coverage
    - .planning/phases/01-verified-gateway-connectivity/01-07-PLAN.md — implemented relay lifecycle and diagnostics contract
  </read_first>
  <files>.planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md, .planning/phases/01-verified-gateway-connectivity/01-VALIDATION.md, .planning/phases/01-verified-gateway-connectivity/COVERAGE.md, test/contract-reconciliation.test.js</files>
  <behavior>
    - "The UI contract's positive verdict, success/retry copy, Details taxonomy, and stop boundary describe relay grant, authenticated status, incremental stream, and cleanup without requiring a browser-origin result."
    - "The direct-WebKit non-arrival result remains named as paired historical negative evidence, and CORS header behavior appears only in the controlled HTTPS/proxy simulation contract."
    - "The validation strategy maps 01-07 through 01-15, uses `.js` tests and their exact plan commands, reflects waves 5-11, and replaces origin-era host/Docker manual steps with relay consent/audit/session-receipt checks."
    - "Coverage keeps capabilities plus the isolated harmless authenticated stream integrated through curl, retains all run controls as Phase 2 opt-outs, and marks browser preflight/CORS as non-positive simulation or historical-negative observations."
  </behavior>
  <action>Begin with a failing contract test that parses all three planning artifacts. Update the UI specification's verdict inventory, copywriting, consideration rows, and phase-boundary text so the positive path exposes safe endpoint class, relay grant, authentication, capabilities, stream, and cleanup; preserve native theming, sizing, focus, reduced-motion, evidence matrix, and non-dismissible stop behavior. Update the validation strategy with task IDs 01-07-01 through 01-15-02, real JavaScript paths, exact automated commands, waves 5-11, bootstrap/handoff sources, and manual actual-Muxy checks for consent, safe audit summary, two fresh receipts, cancellation, and cleanup. Update API coverage so capabilities and the isolated deterministic chat stream remain integrated through one argv curl execution and bounded journal, while browser preflight/CORS are explicitly simulation-only or historical negative observations. Apply D-16, D-17, D-18, D-19, D-20, and D-21; preserve D-08's remote Unverified boundary and every unaffected UI/API decision.</action>
  <verify>
    <automated>node --test test/contract-reconciliation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The contract test proves all three artifacts use the relay-stage vocabulary and contain no positive-path browser-origin requirement."
    - "The direct-WebKit negative result and controlled CORS simulation remain explicitly documented without becoming support predicates."
    - "Every new gap task has one mapped JavaScript command and the map contains no stale `.ts` path or Plans 01-01 through 01-06 pending status."
  </acceptance_criteria>
  <done>Downstream receipt, verdict, UI, and validation work consume one satisfiable curl-relay contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry one opaque verifier challenge through the actual relay into a safe panel-session receipt</name>
  <read_first>
    - src/curl-relay.js — Plan 07 execution, status, timing, and cleanup result
    - src/gateway-client.js — safe capability and stream result construction
    - src/probe.js — attempt snapshots and panel-lifetime owner
    - src/panel/app.js — startup prepare, terminal probe, and muxy.files access
    - .planning/phases/01-verified-gateway-connectivity/01-CONTEXT.md — D-01 through D-05 and D-17 through D-21
    - .planning/phases/01-verified-gateway-connectivity/01-UI-SPEC.md — Task 1 reconciled receipt-safe UI vocabulary
  </read_first>
  <files>src/curl-relay.js, src/gateway-client.js, src/probe.js, src/panel/app.js, test/qualification-receipt.test.js</files>
  <behavior>
    - "Without a challenge, the production URL/token flow creates no receipt file."
    - "A valid non-expired one-use challenge binds one random panel instance and one session ordinal; replay, ordinal mismatch, unexpected fields, and a second receipt from the same panel fail closed."
    - "A terminal probe writes only an allowlisted receipt after stream finalization and journal cleanup settle."
    - "The receipt omits topology, URL, bearer, raw exec identity, argv/stdin, journal path/content, headers/body, prompt/output, tool data, workspace path, browser-origin claims, and raw errors."
  </behavior>
  <action>Write the failing full-chain receipt test over a fake Muxy bridge, then add fixed-root challenge parsing with an exact allowlist, expiry, one-use nonce, expected ordinal, and no deployment field. Generate one random panel-instance identifier per `ConnectionProbe`. Reduce Plan 07's execution/status/timing/frame/cleanup result into stable SHA-256 digests and explicit relay/authentication/capabilities/stream/cleanup outcomes. After a terminal attempt, write `panel-session.json` through `muxy.files` only when the challenge is valid; refuse replay, collision, malformed input, incomplete cleanup, and any attempt by production code to choose a fixture condition. Keep challenge/receipt files outside the journal root and verifier-owned. Apply D-01, D-03, D-04, D-17, D-18, and D-19.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test test/qualification-receipt.test.js test/transport-lifecycle.test.js test/transport-diagnostics.test.js test/contract-reconciliation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "One valid challenge yields exactly one safe receipt after authenticated capabilities, 2xx incremental streaming, terminal frame, and successful cleanup."
    - "Replay, duplicate panel, expiry, ordinal mismatch, cancellation, non-2xx status, parser failure, and cleanup failure cannot yield a support-eligible receipt."
    - "The sentinel suite proves secret, endpoint, content, path, raw-exec, browser-origin, and topology fields are absent."
  </acceptance_criteria>
  <done>An actual panel session leaves a verifier-correlatable receipt derived from the relay and cleanup that ran.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| locked decisions → authoritative contracts | Stale browser-origin language must not override the approved relay architecture. |
| verifier challenge → panel | An opaque one-use challenge enters the panel without granting topology knowledge. |
| relay observations → receipt | Raw subprocess and Gateway data must become correlated safe digests only after cleanup. |
| existing dependency graph → build | Contract and receipt changes add no package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08-01 | Tampering / Repudiation | UI, validation, and coverage contracts | high | mitigate | Test the locked relay vocabulary, current task map, JavaScript paths, and historical-negative/simulation boundaries before downstream work. |
| T-01-08-02 | Spoofing / Replay | challenge and panel receipt | high | mitigate | Use expiry, one-use nonce, ordinal binding, random panel identity, collision refusal, and digest correlation. |
| T-01-08-03 | Information disclosure | receipt projection | high | mitigate | Apply an allowlist after awaited cleanup and test secret/content/path/origin/topology sentinels. |
| T-01-08-04 | Elevation of privilege | receipt capture | high | mitigate | Reuse the exact panel/exec/files permissions and keep fixture condition/lifecycle authority in the verifier. |
| T-01-SC | Tampering | package supply chain | high | mitigate | Add no install task and require the existing lockfile/dependency graph to remain unchanged. |
</threat_model>

<verification>
- `node --test test/contract-reconciliation.test.js` proves the three authoritative contracts agree with D-16 through D-21.
- The receipt, lifecycle, and diagnostic suites prove one actual relay path produces only safe correlated metadata after cleanup.
- No package, manifest permission, background owner, endpoint, topology branch, browser-origin positive stage, or durable secret surface is added.
</verification>

<success_criteria>
- Downstream plans receive a satisfiable relay-first UI, validation, and coverage contract.
- One actual Muxy relay session can produce one safe verifier-bound panel receipt.
- Direct WebKit remains historical negative evidence and CORS remains simulation-only where applicable.
</success_criteria>

<output>
Create `.planning/phases/01-verified-gateway-connectivity/01-08-SUMMARY.md` when done.
</output>
